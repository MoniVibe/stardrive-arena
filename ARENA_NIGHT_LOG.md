# Arena Night Log

- 2026-06-23 04:20 +03:00 — Added the Phase C contender-ladder core: persisted `ContenderRecord[]`, defensive save/load normalization, deterministic roster seeding from real heavy-gun non-dev warships, a production `CareerLadder.SimulateDuel` headless duel service, and `RunLadderRound()` rating/W-L updates. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 22/22 both runs. Commit `f75185e` pushed to `origin/main`. Next: add the Arena leaderboard popup and challenge flow, reusing the proven contender roster and duel/challenge design plumbing.
- 2026-06-23 04:25 +03:00 — Added the Arena ladder leaderboard popup, hub `LADDER` entry, class/rating sort toggle, and queued contender challenge flow. Selecting a persisted contender now makes the next fight spawn that contender's real design as a one-ship challenge, without changing normal generated rounds. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 23/23 both runs. Commit `8034d8d` pushed to `origin/main`. Next: run career/ladder season simulations across seeds and emit a small `sim-output/` report to start exposing dominant chassis/economy curves.
- 2026-06-23 04:37 +03:00 — Added `ArenaCareerSeasonSimulator`, a deterministic headless ladder-season report writer that emits `arena-career-season.json` under `sim-output/` and reports champion concentration, W/L totals, rating spread, and a dominance verdict. While hardening it, the first full suite attempt hit an existing native MonoGame texture-load access violation before the new test, and a later season exact-report assertion exposed close-duel nondeterminism; fixed the latter by making unresolved close duel survivals use a deterministic design-strength/name tie-break and by using decisive high-vs-low season-lab pairings. Headless proof after the fix: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaCareerSeasonSim_EmitsReport_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 24/24 both runs. Commit `2fdbbc6` pushed to `origin/main`. Next: add seeded fight modifiers/mutators with deterministic application proof.
- 2026-06-23 04:44 +03:00 — Added deterministic veteran fight modifiers: fresh careers still select `None`, while veteran careers derive a stable per-round modifier from career level/cash. Implemented `Swarm Contract`, `Reinforced Hulls`, and `Elite Vanguard` effects through normal enemy spawn count, boss count, and clamped enemy strength/health application; ladder challenges explicitly bypass modifiers. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaFightModifiersApplyDeterministically_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 25/25 both runs. Commit `7cff6d3` pushed to `origin/main`. Next: harden career loading/fuzz old or corrupt YAML and keep static save-path isolation tight.
- 2026-06-23 04:50 +03:00 — Hardened career loading against absent, empty, corrupt/truncated, old-version, and current-version partial YAML. `CareerManager.Load` now rejects version mismatches as fresh careers while still normalizing null arrays and `ActiveVesselId` for current-version partial saves. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors after one transient copy warning cleared on a serial retry; targeted `ArenaCareerLoadFuzz_Headless` passed; the first full arena run hit a transient existing `ArenaRunLosable_Headless` `Ship.UpdateModulePositions` NRE, then fresh reruns passed twice, 26/26 both runs. Commit `b848d69` pushed to `origin/main`. Next: continue hardening with deterministic flake stress or owned-module enforcement around career vessels.
- 2026-06-23 04:57 +03:00 — Added roster normalization at the Arena career persistence boundary. Current-version saves now drop malformed owned-vessel entries with empty designs, repair missing/duplicate `VesselId` values, clamp negative veterancy, repair stale `ActiveVesselId`, sanitize stale/duplicate/flagship fleet ids, and apply the same cleanup before saving dirty in-memory careers. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaCareerRosterNormalization_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 27/27 both runs. Commit `e0c990a` pushed to `origin/main`. Next: harden invalid/missing owned-vessel designs so stale modded saves cannot field fallback ships while banking onto stale owned records, then tighten ladder duel RNG seeding.
- 2026-06-23 05:02 +03:00 — Hardened live Arena handling for owned vessels whose saved `DesignName` no longer resolves after content/mod changes. The screen now falls back from stale active owned vessels to the first currently spawnable owned vessel, grants a starter only when no owned design is spawnable, rejects stale garage selection and fleet toggles, filters stale fleet ids out of live fleet size/state, and never spawns a fallback ship while mapping veterancy banking to a stale owned record. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaOwnedVesselInvalidDesignHardening_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 28/28 both runs. Commit `f5f3656` pushed to `origin/main`. Next: tighten ladder duel RNG seeding and add a rerun signature test so season/ladder reports cannot drift under hidden empire RNG.
- 2026-06-23 16:14 +03:00 — Tightened ladder duel determinism by re-seeding the duel universe after both temporary duel empires are created, ensuring their per-empire RNG streams are deterministic before ship spawn and combat orders. Strengthened `ArenaCareerLadderRound_Headless` to compare same-seed duel duration, alive states, retained strengths, damage, ladder pairings, winners, ratings, wins, and losses across a fresh rerun. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaCareerLadderRound_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 28/28 both runs. Commit `2247e6d` pushed to `origin/main`. Next: begin stretch backlog with a headless-verifiable living contender ecosystem or balance/meta-watch slice.
- 2026-06-23 16:25 +03:00 — Started the stretch backlog with a deterministic living contender ecosystem engine. `ContenderRecord` now carries career seasons, XP, level, and evolution counts; save/load normalization clamps malformed contender career fields and duplicate names. Added `ArenaLivingEcosystemSimulator`, which advances seeded contenders through real ladder bouts, awards career XP/levels, retires old low-rated contenders into rookies, and keeps duel-proven stronger design evolutions, emitting `arena-living-ecosystem.json` under `sim-output/`. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaLivingContenderEcosystem_Headless|ArenaCareerRosterNormalization_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 29/29 both runs. Commit `60ef939` pushed to `origin/main`. Next: add the balance/meta-watch guardian to compare realized win-rate trends across seeds and flag drift.
- 2026-06-23 16:31 +03:00 — Added the Arena balance/meta-watch guardian. `ArenaBalanceMetaWatch` samples legal arena warships deterministically, runs side-swapped `CareerLadder.SimulateDuel` bouts, compares realized win rates against strength-based expectations, flags drift rows, and emits `arena-balance-meta-watch.json` under `sim-output/` for nightly balance triage. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaBalanceMetaWatch_EmitsReport_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 30/30 both runs. Commit `39347f4` pushed to `origin/main`. Next: use realized win-rate evidence for dealership pricing or add contender rivalries/narrative records.
- 2026-06-23 16:36 +03:00 — Added contender rivalry records and deterministic narrative lines. Ladder round results now persist each contender's latest rival name plus rivalry W/L counts, `CareerLadder.RivalryLine` emits seeded-state flavor text, and career normalization clamps malformed rivalry fields from saves. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaContenderRivalries_Headless|ArenaCareerRosterNormalization_Headless` passed; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 31/31 both runs. Commit `2d035e7` pushed to `origin/main`. Next: connect realized win-rate evidence to fair dealership pricing, or deepen the living ecosystem with richer mutation operators and longer seed sweeps.
- 2026-06-23 17:13 +03:00 — Added the first full Arena determinism audit gate over the real `ArenaFightScreen` lifecycle. The audit runs two seeded live-screen trajectories with serial object updates, compares authoritative `SdHash128-v1` hashes plus arena phase/cash/roster digests, and emits `arena-full-determinism-audit.json` under `sim-output/`. It exposed and fixed two drift sources: arena universe generation now reseeds the shared planet-type/moon RNG, and arena empires disable irrelevant 4X strategic planners so background goals cannot perturb the combat sandbox. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; targeted `ArenaFullDeterminismAudit_Headless` passed and produced `firstDivergence=-1`; report SHA-256 stayed `63043563F433FF373A9504F8739ACCD908027686E289F6675FC867FCC2ED47DE` across a fresh focused run; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 32/32 both runs. Commit `6566741` pushed to `origin/main`. Next: extend the audit across longer horizons/round transitions and continue toward fair win-rate-based dealership pricing.
- 2026-06-23 17:34 +03:00 — De-circularized the arena duel/ladder/balance engines. `CareerLadder.SimulateDuel` now resolves close both-alive fights from combat evidence (death, decisive retained strength, damage dealt, retained-health fraction) before falling back to BaseStrength/name, and `CareerLadder.FairDuel` side-swaps every ladder/meta comparison so race bonuses do not bias standings. `ArenaBalanceMetaWatch` now uses the shared fair duel helper and a named `DefaultDriftThreshold=0.10`, with tests proving above/below-threshold flag semantics instead of the old tight-threshold all-flag behavior; season and living ecosystem sims now use fair duel outcomes too. Current combat-honest meta-watch: no sampled design exceeded the 10-point drift gate; top overperformer `Draug-Mk3` drifted +0.060, top underperformer `Beam Drone` drifted -0.032, with `Behemoth` still 8-0 but only +0.014 above strength expectation. Season sim still flags `Corsair-Mk3` as 2/2 champion; living ecosystem produced 1 retirement, 1 rookie, 6 evolutions, and strength growth 893->29697. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors after a transient parallel-build DLL lock cleared on a serial retry; targeted `ArenaCareerLadderRound_Headless|ArenaBalanceMetaWatch_EmitsReport_Headless|ArenaCareerSeasonSim_EmitsReport_Headless|ArenaLivingContenderEcosystem_Headless` passed 4/4; full `vstest.console.exe UnitTests.dll /TestCaseFilter:"FullyQualifiedName~Arena"` passed twice, 32/32 both runs. Commit `bd657e6` pushed to `origin/main`. Next: implement win-rate-based dealership pricing behind the existing `VesselPrice`/catalog path, then extend the determinism audit through round-clear/shop/round-2.
- 2026-06-23 18:06 +03:00 — Added the next arena-career batch. Dealership pricing now goes through the existing `VesselPrice`/`DealershipCatalog` path but uses a cached realized FairDuel pricing curve instead of design `GetCost`; the catalog proof reported 496 evidence-priced vessels, cheapest tier `$50` at win-rate 0.000 (`Missionary mk1-a`) and priciest tier `$1500` at win-rate 1.000 (`Behemoth`), with 18,841 decisive monotonic realized-strength pairs checked. `ArenaFullDeterminismAudit_Headless` now hashes the live screen through opening trajectory -> deterministic round clear -> shop -> public `NextFight()` -> round 2, producing 39 `SdHash128-v1` samples through frame 1082 with `roundClearFrame=721`, `round2StartFrame=722`, and `firstDivergence=-1`. The living ecosystem now seeds a role-diverse roster, uses role/module-aware mutation candidates plus diversity-preserving rookie replacement, and emits `diversityBySeason`; the current readout is 1 retirement, 1 rookie, 12 evolutions, strength 22639->95274, baselineWins 2->5, final spread `CORVETTE:1,FRIGATE:1,BATTLESHIP:2,CAPITAL:2`, dominant design `Battle Drone` (no all-capital collapse). Balance/season sims after the refinements remained stable: meta-watch flagged 0/5 with max drift 0.060 below the 0.10 gate, and season sim still reports `Corsair-Mk3` as 2/2 champion. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; affected proof set `ArenaDealershipWinRatePricing_Headless|ArenaOwnedInventory_Headless|ArenaFullDeterminismAudit_Headless|ArenaLivingContenderEcosystem_Headless|ArenaBalanceMetaWatch_EmitsReport_Headless|ArenaCareerSeasonSim_EmitsReport_Headless` passed 6/6; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 33/33 both runs. Next: push the committed batch, then continue with self-tuning difficulty/economy or deeper balance-CI counter-system sweeps.
- 2026-06-23 18:45 +03:00 — Added smooth career-gated class progression for the Arena career. A shared tier model now maps legal combat craft by both design role and hull role (tier 1 fighter/gunboat/corvette, tier 2 frigate/destroyer, tier 3 cruiser/battleship/capital) with a monotonic career ramp: levels 0-2 tier 1, 3-6 tier 2, 7+ tier 3. Fresh careers now start on armed tier-1 light craft, enemy escorts and bosses are drawn from the same career tier ceiling, low careers never spawn bosses/capitals, dealership buy/catalog/garage/customize/fielding paths all reject out-of-tier designs, and contenders seed from legal combat craft so early challenges can be light-craft legal. Added `ArenaProgressionSimulator` and `arena-progression.json`; current curve: level 0-2 fields `Vulcan Thorn` vs tier-1 `Type I Vulcan` with no boss, level 3-6 fields `Type VII mk1` vs tier-2 `Scythe mk2-d`, and level 7+ fields `Hades mk2-b` with tier-3 `Nightshade mk2-c` escorts plus `Ancient Battleship` boss. The high-career balance proof now shows invested runs clear all 3 rounds with 6 wingmen/repairs while no-investment runs still wipe on round 3. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 35/35 both runs. Next: highest-value follow-up is finite module-inventory enforcement (strip/equip owned parts) because it directly hardens the garage/customize economy now that hull tier and cash gates are real.
- 2026-06-23 19:18 +03:00 — Added the tier-3 boss payoff and persistent Arena perk rewards. Tier-3 careers now periodically stage a distinct round-2 `Remnant Arena Warden` boss encounter, while low-tier careers never resolve one; the current boss/perk report (`game/sim-output/arena-boss-perks.json`) samples `Blockade buster mk5-b` at effective strength 28,359.5, 4.171x the normal late escort `Nightshade mk2-c`. `ArenaCareer.Perks` now persists normalized stackable perk ids, reward offers are deterministic 3-choice sets, and `GrantPerk` applies real bonuses through the existing empire paths: all weapon-tag damage, module/hull health with live ship refresh, cash-per-clear, repair discount, and +1 fleet slot. Added a non-blocking boss reward popup plus a hub `BOSS REWARD` entry; full headless proof covers tier gates, boss toughness, reward persistence, stack persistence, live damage/health/cash/repair/fleet effects, and deterministic JSON emission. Headless proof: `dotnet build StarDrive.csproj -v q -nologo` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v q -nologo` passed 0 warnings/0 errors; affected proof set `ArenaRoguelikeRun_Headless|ArenaRunLosable_Headless|ArenaCareerClassTierProgression_Headless|ArenaProgressionCurve_EmitsReport_Headless|ArenaBossEncountersAndPerks_Headless|ArenaFleetSetup_Headless|ArenaFightModifiersApplyDeterministically_Headless` passed 7/7; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 36/36 both runs. Next: finite module-inventory enforcement (strip/equip owned parts) is still the highest-value hardening item before combat doctrines.
- 2026-06-23 19:40 +03:00 — Added choose-your-fight contracts plus deterministic risk/reward loot. Between-round Arena hub now has optional `FIGHT OPTIONS` that queues one exact next fight without changing the default `NEXT FIGHT` path; low careers generate Safe/Standard/Risky only, while tier-3 careers generate Standard/Risky/Elite and Elite uses the boss encounter model. `ArenaCareer.Salvage` now persists normalized module UID counts and the ITEMS view merges mounted owned-vessel modules with salvaged parts. Winning a selected option grants previewed cash plus deterministic loot rolls: salvage parts, chassis unlocks through the existing hull-unlock path, perk ids from the boss/perk catalog with live perk application, and bonus cash. Current `game/sim-output/arena-fight-options-loot.json` sample: Standard `Nightshade mk2-c` estimates win 0.582, value 480; Risky `Nightshade mk2-c` + `Ancient Battleship` estimates 0.217, value 1180; Elite boss `Blockade buster mk5-b` estimates 0.114, value 2650 and previews `AdvAmplifier x5`, `Terran` chassis, `weapon_matrix`, and `$400` bonus cash. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaFightOptionsRiskRewardLoot_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 37/37 both runs after the final live-perk refresh fix. Next: module-inventory enforcement, consuming `ArenaCareer.Salvage` in the garage/customizer for finite strip/equip parts.
- 2026-06-23 20:58 +03:00 — Added Arena teams, implemented 1v1–5v5 size leagues, deterministic N-per-side team duels, permadeath/backfill, player-team setup hooks, and a power-creep guard for living contender evolution. `ArenaCareer` now persists normalized `ArenaTeam[]` and clamped `PermadeathChance`; `CareerLadder` now has `SimulateTeamDuel`/`FairTeamDuel` for equal-size teams up to 5 per side, with side-swapped aggregate results and per-contender survival. `ArenaLeagues` generates league teams, builds transient player teams, applies deterministic permadeath to downed losers, backfills rookies through the living-ecosystem rookie path, and emits `game/sim-output/arena-leagues.json`. Current leagues report covers 1v1, 2v2, 3v3, 4v4, and 5v5, with `permadeathChance=1`, 5 deaths, 5 replacements, and standings for every bracket. The living ecosystem now reports `evolutionStrengthCap`; current sample keeps final top strength 139,340.4 under cap 163,769.1 while still improving average strength 22,639.3→42,380.7 and baseline wins 2→4. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaTeamsLeaguesPermadeath_Headless` and `ArenaLivingContenderEcosystem_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 38/38 both runs. Next: choose module-inventory enforcement or combat doctrines; for large 10v10+ leagues, add an abstract/statistical resolver before scaling beyond 5v5.
- 2026-06-23 21:24 +03:00 — Opened Arena big leagues for real deterministic background sims, not a statistical resolver. `CareerLadder` now supports team duels up to 100 ships per side, while `ArenaLeagues` exposes supported brackets `1,2,3,4,5,10,20,50,100`, async `RunLeagueSeasonAsync`/`RunLeagueSeasonAndApplyAsync`, deterministic matchup sampling with explicit sampled/skipped counts, strict serial-deterministic mode by default, and an opt-in relaxed background flag for pure flavor fights. Big seasons run independent sandbox duels across a capped worker set, then accumulate standings in match-index order so parallel and serial outcomes have identical signatures. `ArenaFightScreen` now has live-layer wrappers for queued background league seasons. Current `game/sim-output/arena-leagues.json` includes a `bigLeagues` section; the full proof sample covers 10v10 with a 3-of-6 sampled budget (`sampled=3`, `skipped=3`, `totalTicks=1446`), serial-vs-parallel identical standings/outcomes, and 50v50/100v100 one-tick spawn/count probes. Measured focused proof costs on this machine: 10v10 fair duel 351 ms, 20v20 fair duel 309 ms. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaBigLeaguesAsyncParallel_Headless` passed and targeted `ArenaTeamsLeaguesPermadeath_Headless` passed after avoiding parallel vstest config-file contention; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 39/39 both runs. Next: module-inventory enforcement remains the highest-value economy hardening item, now with salvage already filling the owned-module pool.
- 2026-06-23 21:53 +03:00 — Added the first Star Gladiator UI reskin from Figma source file `vaCkdGhdnunpCQoPmRA1a0`. `ArenaTheme` now centralizes the extracted Figma color tokens and maps the design type ramp onto StarDrive fonts (Orbitron-style headings -> Pirulen, Rajdhani body -> Arial, mono labels/numbers -> Visitor/Consolas). The hub now presents the `STAR GLADIATOR` header, Figma tagline, stat chips, and requested pill nav: `DESIGNER`, `BOUT`, `BET`, `SHOP`, `CLIMB`, `BOSS`, `PILOT SOUL`, `MEMORIAL`; existing dealership, garage, fleet, items, leaderboard, fight-options, and boss-reward overlays consume the shared theme. `BET`, `PILOT SOUL`, and `MEMORIAL` are named read-only stub screens for future batches, and `BOSS` queues the existing Elite boss contract only when tier-gated fight options allow it. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final diff state, 39/39 both runs, with the hub-stage test now asserting all eight Figma nav pills. Visual fidelity to Figma was not claimed headlessly; it still needs a human launch/screenshot pass. Next: module-inventory enforcement remains the highest-value economy hardening item, followed by deeper UI implementation for betting, pilot soul, and memorial.
- 2026-06-23 22:24 +03:00 - Fixed the live Arena UI playtest blockers. Garage `Customize` now queues the active vessel design into `ShipDesignScreen` and lets the designer consume it after `LoadContent`, with null guards around pre-load UI hooks so the old `ChangeHull`/`OnDesignChanged` NRE path cannot crash `UpdateGame`. The main-menu Arena button now opens the Star Gladiator hub in an idle paused control-deck phase, with BOUT/fight-option selection starting round 1 explicitly. The Figma `SHOP` pill now opens a new cash-gated combat module/item shop that buys into `ArenaCareer.Salvage`, while the old inline intermission buys are renamed `REPAIR BAY` and the vessel `DEALERSHIP` stays separate. Tightened `ArenaTheme` contrast and button text sizing for the high-contrast light-on-dark Figma direction. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaMenuOpensHubBeforeBout_Headless|ArenaGarageCustomizeQueuesDesignerAfterLoad_Headless|ArenaModuleShopBuysIntoSalvage_Headless|ArenaHubStage_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 42/42 both runs. Visual/flow confirmation still needs a human launch with hub/shop/garage screenshots against Figma. Next: keep UI focused through the screenshot polish pass, then queue BET once the hub/control-deck flow is stable.
- 2026-06-23 23:29 +03:00 - Fixed the Arena popup usability blockers from the screenshots. Module shop, dealership, items, leaderboard, garage, and fleet now use a shared `ScrollList<ArenaPopupListItem>` row path with clipped scroll regions, padded panels, and fixed footer buttons so long catalogs do not overflow into BACK. Removed the garage-to-items modal stack that produced duplicate BACK chrome, and made REPAIR BAY dismiss the hub during intermission so the existing repair controls are not trapped behind it. Garage/Fleet now explicitly sync live ship veterancy before rendering, so Kills/Level reflect what the vessel earned before final run bank; `BankCareer` persistence remains covered. Also hardened `ArenaHubStage_Headless` to use a temp career path after the first full-suite attempt exposed that a real played career could alter the expected round-2 enemy count. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaPopupListsAreScrollableAndFooterBackIsUnique_Headless|ArenaGarageFleetDisplayLiveKillsAndLevel_Headless|ArenaHubStage_Headless` passed 3/3; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 44/44 both runs. Scroll/no-overlap visual confirmation still needs a human launch and screenshots. Next: fight roulette should replace the fixed 3-round demo loop with ongoing scaled encounter choices.
- 2026-06-23 23:56 +03:00 - Replaced the live Arena's fixed 3-round victory loop with an open-ended fight roulette. `FightOption` now carries a deterministic fight type (`Normal`, `Boss`, `LeagueBout`, `ContenderChallenge`), scaled enemy selection uses career tier plus fielded owned-fleet strength for win estimates, and default BOUT/NEXT FIGHT spawns the safest current contract while explicit choices can select Standard/Risky/Elite. Clears now bank career level, reroll the next option set, and return to the hub indefinitely; `TotalRounds` remains only a legacy balance horizon, not a win trigger. Fresh careers default `HardLossEndsRun=false`, so losses bank and return to the hub with downed live ships cleared for respawn; hardcore saves can opt into the old defeat screen. Current proof readout: fresh tier-1 options stay normal/light, tier-3 options unlock boss-type variety and higher average difficulty, four consecutive clears pass the old round-3 cap with distinct contract signatures, and a post-cap loss returns to the hub then respawns the roster. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaFightRouletteOpenEndedLoop_Headless|ArenaFightOptionsRiskRewardLoot_Headless` passed 2/2 and affected arena UI/career group passed 9/9; full `dotnet test UnitTests/SDUnitTests.csproj --no-restore --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 45/45 both runs. Next: combat persistence/refit or finite module-inventory enforcement should close the loop between damage, salvage, and garage customization.
- 2026-06-24 00:27 +03:00 - Fixed the first Arena playtest bug batch. Hub cash chips are now dynamic and update immediately after module/dealership/repair purchases and after hub rebuilds. Garage activation now returns a concrete result/message; owned but below-tier vessels remain gated but the row and click path explain `Requires Tier N (current Tier M)` instead of silently doing nothing. Fight loot/salvage now has an explicit inventory proof from generated option reward -> `ArenaCareer.Salvage` -> ITEMS census/popup row. Repair is now a hub-local `REPAIR ALL` action that repairs surviving ships in-place, deducts/persists cash, stays on the hub, and never exposes the legacy `ROUND CLEARED - SHOP` panel. Loss handling now keeps the player on the same fight step: `CareerLevel`, field tier, `Round`, and `HubRound` do not advance unless a real fight is won. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors after a parallel-build DLL lock cleared on serial retry; targeted `ArenaHubCashDisplayUpdatesAfterBuy_Headless|ArenaGarageActivationExplainsTierGate_Headless|ArenaLootedModulesAppearInInventory_Headless|ArenaHubRepairAllStaysOnHub_Headless|ArenaFightRouletteOpenEndedLoop_Headless|ArenaModuleShopBuysIntoSalvage_Headless|ArenaMenuOpensHubBeforeBout_Headless|ArenaLiveScreenDrive_Headless` passed 8/8; full `dotnet test UnitTests/SDUnitTests.csproj --no-restore --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 49/49 both runs. Next: combat persistence + refit should remove destroyed ships from the garage, carry survivor damage, and make destroyed modules feed the finite refit loop.
- 2026-06-24 00:57 +03:00 - Landed combat persistence + refit. `OwnedVessel` now persists carried hull damage, destroyed module slots, and refit slot overrides; default `PlayerShipsPermadeath=true` removes destroyed fielded vessels from the garage while non-hardcore careers still return to the hub. Each new bout respawns the fielded roster at fresh positions from persisted state, skips destroyed module slots, applies refit overrides, and reapplies carried hull damage. `REPAIR ALL` now charges the quoted damage-scaled hull repair cost, repairs active modules/hull only, and does not resurrect destroyed module slots. Garage rows flag damaged vessels with `[REFIT N]`, and the new refit path consumes salvage into a persistent slot override; ITEMS census now excludes destroyed mounted slots and includes refit overrides plus salvage. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaDestroyedFieldedVesselIsRemovedAndActiveFallsBack_Headless|ArenaSurvivorScarsRepairAndRefitRoundTrip_Headless|ArenaHubRepairAllStaysOnHub_Headless|ArenaLootedModulesAppearInInventory_Headless|ArenaFightRouletteOpenEndedLoop_Headless|ArenaOwnedInventory_Headless|ArenaOwnedItems_Headless` passed 7/7; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 51/51 both runs. Next: arena isolation should add the local rearm node and suppress resupply retreat so ships stop bleeding back into the 4X layer.
- 2026-06-24 01:33 +03:00 - Isolated live arena combat from the 4X resupply-retreat path. `ArenaFightScreen` now maintains a center-local rearm zone during fights, cancels arena participants out of `Resupply`/`Flee`/return-home states back onto local targets, and hard-clamps any ship that crosses the arena combat radius so it cannot path off-screen toward home systems. The proof forces a live arena ship into the old home-system resupply order, zeroes ordnance, verifies local refill plus escape-state cancellation, deliberately pushes it beyond the arena bound, and compares a fresh rerun signature for determinism. Headless proof: `dotnet build StarDrive.csproj` passed 0 warnings/0 errors after a parallel-build DLL lock cleared on serial retry; `dotnet build UnitTests/SDUnitTests.csproj` passed with one transient copy retry warning during the initial parallel build and then the focused rebuilt test passed; targeted `ArenaLocalRearmContainsLowOrdnanceShips_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 52/52 both runs. Next: fame/roster or the hub menu/save-load UX can build on the now-contained arena loop.
- 2026-06-24 01:54 +03:00 - Expanded the fight roulette into a fame-driven difficulty spectrum. `ArenaCareer.Fame` now persists and scales the option slate; full fame produces the requested spread of 1 Trivial, 2 Easy, 2 Normal, 3 Hard, and 2 Wildcard entries, while higher fame adds extra wildcards without shrinking the slate. Fight options now carry a relative difficulty tier, strength ratio, fame reward, and optional named special contract; contracts appear at fame 60+, rewards scale by difficulty/upset potential, and live wins add the option's fame while resolved losses never increase it. The fight-options popup now shows difficulty/contract/fame, and the JSON report includes difficulty, contract, strength ratio, and fame fields. Headless proof: `dotnet build StarDrive.csproj` passed 0 warnings/0 errors after the known parallel-build output lock cleared on serial retry; `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors; targeted `ArenaFightOptionsRiskRewardLoot_Headless|ArenaFameDifficultySpectrum_Headless|ArenaCareerClassTierProgression_Headless|ArenaBossEncountersAndPerks_Headless|ArenaFightModifiersApplyDeterministically_Headless` passed 5/5; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 53/53 both runs. Next: hub menu + save/load UX with New/Load/config/slots, then shop tech tiers and meta-upgrades.
- 2026-06-24 02:19 +03:00 - Added the Arena hub menu, three save slots, slot metadata, config persistence, and choose-your-start flow. `CareerManager` now saves/loads slot 1-3 as `slotN.yaml`, exposes per-slot metadata summaries (level, cash, fame, vessel count, last played from file time), and keeps the legacy `CareerSavePath` override intact for tests. Main-menu Arena now opens a Star Gladiator slot menu with Continue/New Game/Load; New Game seeds deterministic tier-1 rosters for ACE (1 strong craft, high cash), WINGMATES (3 light craft), and SWARM (4 light craft, all fielded). The hub now exposes Save/Config/New Game/Load/Exit, and Config persists `PlayerShipsPermadeath`, `HardLossEndsRun`, and contender `PermadeathChance`. Skeptic fix: the slot menu row buttons now capture each slot index explicitly so rows cannot all route to the last slot. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors after closing a running StarDrive process that locked `game/SDUtils.dll`; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaCareerSlotsChooseStartAndConfig_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 54/54 both runs. Next: shop tech tiers + meta-upgrades, or finite module-inventory enforcement to make strip/equip/sell fully close the salvage loop.
- 2026-06-24 02:37 +03:00 - Fixed owned-state surfacing and Arena nav cleanup. Garage/Fleet UI refresh now merges live veterancy monotonically, so opening GARAGE with torn-down or zeroed live ships cannot reduce persisted kills/experience/level; the proof simulates 2 banked kills, a zeroed garage refresh, then 3 more kills and persists 5 total. `ApplyCareer` now unlocks module UIDs from owned designs, refit overrides, and `ArenaCareer.Salvage` via `UnlockEmpireShipModule`, so looted/refit parts appear in the designer's module palette while unrelated locked modules stay locked. High-level wipe replacement now uses a humble tier-1 starter instead of the career-scaled auto-pick, preventing a wiped tier-3 career from being rewarded with a cruiser/battleship/capital. Removed the deferred PILOT SOUL and MEMORIAL pills/handlers from the hub nav while keeping BET. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaGarageRefreshDoesNotReduceBankedVeterancy_Headless|ArenaApplyCareerUnlocksSalvageModulesForDesigner_Headless|ArenaHighLevelWipeGetsHumbleTier1Starter_Headless|ArenaHubStage_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 57/57 both runs. Next: shop tech tiers + meta-upgrades, or finite strip/equip/sell enforcement to close the remaining owned-module economy loop.
- 2026-06-24 02:58 +03:00 - Scaled Arena fielded fleets and mirrored roulette enemies against the player's current fleet. The base fieldable limit is now 10, stackable `fleet_commission` upgrades clamp at a hard 100, and a cash-gated `BuyFleetSlotUpgrade` backend persists the next slot for the upcoming meta shop. Fight options now derive enemy count and estimated strength from the actual fielded vessel count/strength: Normal mirrors 1/10/100 player fleets with 1/10/100 enemies, Trivial/Easy apply a real low-risk handicap so one-ship fleets can still face sub-1.0x contracts, Hard scales above Normal, and Wildcards sample a deterministic 0.5x-2.0x band. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaFleetScalingMirrorsPlayerFleet_Headless|ArenaFightOptionsRiskRewardLoot_Headless|ArenaFameDifficultySpectrum_Headless|ArenaFleetSetup_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 58/58 both runs. Next: fame-gated shop tiers plus the visible meta/research upgrade shop.
- 2026-06-24 03:11 +03:00 - Added fame-gated Arena shop tiers plus meta/research upgrades. Module-shop rows now carry a deterministic tier from unlock-tech depth/cost fallback and the live SHOP filters to the career's effective tech tier: fame <20 surfaces tier 1 only, fame 20-49 surfaces up to tier 2, and fame 50+ surfaces tier 3; `research_grant` can raise the effective tier after it is fame-unlocked. The same SHOP popup now shows meta rows for fleet slots, extra fight choices, research grants, and scout network, with fame thresholds and cash gates; purchases persist as normalized perk ids, and `fight_broker` adds one deterministic fight option to future slates. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaFameGatedShopMetaResearch_Headless|ArenaModuleShopBuysIntoSalvage_Headless|ArenaFightOptionsRiskRewardLoot_Headless|ArenaFameDifficultySpectrum_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 59/59 both runs. Next: module hover tooltip data/provider reuse for dealership, garage, and fleet rows.
- 2026-06-24 03:24 +03:00 - Added module hover tooltip data and reused StarDrive's existing `ShipInfoOverlayComponent` on Arena dealership, garage, and fleet rows. Dealership rows now expose the design module loadout, while owned-vessel rows expose carried hull percent and destroyed module slot counts before showing the reused ship card; the shared `ArenaPopupListItem` payload hook keeps the hover wiring data-driven instead of special-casing list labels. The new provider proof uses a real legal combat design, verifies module census, then scars one persisted owned-vessel slot and verifies the effective loadout drops by one with `Hull: 50%` and `Destroyed module slots: 1` in the tooltip text. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaDesignTooltipProvider_Headless|ArenaPopupListsAreScrollableAndFooterBackIsUnique_Headless|ArenaHubStage_Headless` passed 3/3; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 60/60 both runs. Visual hover placement still needs a human launch. Next: implement real BET wagering against deterministic headless fights.
- 2026-06-24 03:40 +03:00 - Replaced the BET stub with real Arena wagering. The betting board now quotes fixed-stake slips for the player's next fight options and deterministic contender duels; placing a fight-option bet freezes the exact option before deducting cash, persists an open slip, blocks switching to a different option, and resolves on the actual fight result. Contender duel bets settle immediately through `CareerLadder.SimulateDuel`, with favorites paying less and weaker picks paying more. Headless proof covers stake deduction, win payout, loss net -stake, odds/payout ordering from strength ratio, screen-level option freeze + save/load cash round-trip, and deterministic contender win/loss settlement. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaBettingWagers_Headless` passed; affected `ArenaBettingWagers_Headless|ArenaFightOptionsRiskRewardLoot_Headless|ArenaHubStage_Headless|ArenaFameGatedShopMetaResearch_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final code state, 61/61 both runs. Next: finite strip/equip/sell enforcement or the next economy loop.
- 2026-06-24 04:22 +03:00 - WI-1 plugin extraction groundwork: added general plugin infrastructure with `IGamePlugin`, `IGameExtensionPoints`, and `PluginManager`. Startup now scans `Plugins/*.dll` from the install/game directory, isolates assembly/type/plugin registration failures behind logging, and exposes registered main-menu actions without changing current Arena behavior. Empty or absent plugin directories are no-ops. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaPluginManagerEmptyDirectory_Headless|ArenaHubStage_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final WI-1 code state, 62/62 both runs. Next: WI-2 converts the main-menu Arena button to consume the registry while temporarily registering the in-engine Arena action.
- 2026-06-24 04:30 +03:00 - WI-2 proved the main-menu extension seam before moving Arena files. `MainMenuScreen` no longer names Arena directly; it loops over `PluginManager.RegisteredMainMenuActions`, binds matching data-driven button names, and safely routes the created screen through `ScreenManager.GoToScreen`. Startup temporarily registers the current in-engine `arena` action to `ArenaCareerMenuScreen`, which WI-3 will replace with `ArenaPlugin`. Grep proof: `MainMenuScreen.cs` has no `Ship_Game.GameScreens.Arena`, `Arena_Clicked`, or `new ArenaCareerMenuScreen` reference; the only temporary engine Arena reference is `StarDriveGame.cs` startup registration. Headless proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaPluginManagerRegistersMainMenuAction_Headless|ArenaPluginManagerEmptyDirectory_Headless|ArenaMenuOpensHubBeforeBout_Headless` passed 3/3; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice from the final WI-2 code state, 63/63 both runs. Next: WI-3 creates `StarDriveArena.csproj`, transfers Arena compile ownership to the satellite, and replaces the temporary startup registration with `ArenaPlugin`.
- 2026-06-24 04:46 +03:00 - WI-3 moved Arena compile ownership out of the engine into the satellite plugin. Added `StarDriveArena.csproj`, added `ArenaPlugin : IGamePlugin`, removed every `Ship_Game\GameScreens\Arena\*.cs` compile include from `StarDrive.csproj`, and deleted the temporary `StarDriveGame` inline Arena registration so the main-menu button now depends on the drop-in plugin assembly. The only engine accommodation is a narrow `InternalsVisibleTo("StarDriveArena")` grant, preserving the current internal Arena/engine surface during extraction instead of making broad APIs public. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet restore StarDriveArena.csproj` completed for the new project; `dotnet build StarDriveArena.csproj --no-restore` passed 0 warnings/0 errors and emitted `game/Plugins/StarDriveArena.dll`; grep outside `Ship_Game/GameScreens/Arena` for `Ship_Game.GameScreens.Arena|ArenaCareerMenuScreen|ArenaFightScreen|CareerLadder|FightOption|ArenaPlugin` returned no engine source matches. Next: WI-4 wires UnitTests to the satellite and reruns the full `~Arena` suite twice from the split-assembly code state.
- 2026-06-24 05:02 +03:00 - WI-4 wired the existing Arena tests to the satellite assembly. `UnitTests/SDUnitTests.csproj` now references `StarDriveArena.csproj`, so the 63 Arena tests compile against the plugin-owned Arena types instead of relying on engine ownership. Verification from the split-assembly state: `dotnet build StarDrive.csproj --no-restore` passed after a serial retry cleared the known parallel `SDGraphics.obj` lock; `dotnet build StarDriveArena.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors. The first full post-split `~Arena` run passed 63/63, the next attempt hit an existing ship-design parallel-load `AccessViolationException` in `StringView.Next` during ResourceManager load before completion, then two fresh full reruns passed 63/63 and 63/63. Next: WI-5 adds the deploy/drop-in verification doc/proof and confirms a stock engine plus `Plugins/StarDriveArena.dll` lights up the Arena button.
- 2026-06-24 05:20 +03:00 - WI-5 completed the drop-in deploy proof. `StarDriveArena.csproj` already emits the satellite to `game/Plugins/StarDriveArena.dll`; README now documents the friend/install path: copy `StarDriveArena.dll` into `<install>\Plugins\` for a matching BlackBox/Combined Arms build. Added `ArenaPluginManagerLoadsDropInArenaDll_Headless`, which proves an empty plugin directory registers no Arena action, then copies the built `StarDriveArena.dll` to an isolated temp plugin directory, scans it through `PluginManager.LoadAndRegister`, and asserts the `arena` main-menu action constructs `ArenaCareerMenuScreen`. Verification: `dotnet build StarDrive.csproj --no-restore` passed after a serial retry cleared the known parallel `SDUtils.obj` lock; `dotnet build StarDriveArena.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; focused `ArenaPluginManager*` passed 3/3; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 64/64 and 64/64. Final grep outside `Ship_Game/GameScreens/Arena` for `Ship_Game.GameScreens.Arena|ArenaCareerMenuScreen|ArenaFightScreen|CareerLadder|FightOption|ArenaPlugin` returned no engine source matches. Next: future plugin work should grow extension points only when a real non-Arena feature needs them.
- 2026-06-24 05:43 +03:00 - Packaged the minimal upstream determinism patch as an apply-ready BlackBox PR spec without touching live engine or Arena code. Added `DETERMINISM_UPSTREAM_PR.md` with the off-by-default safety contract, verified arena surface, file-by-file manifest, SDNative `sortCollisionsById` no-native-rebuild note, and explicit instruction to inline the stream-kind helper calls so the upstream patch introduces no `Ship_Game.Determinism` namespace. Added `ArenaDeterminismPatchContractTests`: `ModeOffIsInert` proves default RNG/collision/parallel-update behavior is unchanged, and `SameSeedReproducible_DifferentSeedDiffers` proves opt-in `EnableDeterministicRng(seed)` plus serial update gives an exact same-seed small-sim digest and a different digest for a different seed. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaDeterminismPatch*` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 66/66 and 66/66. Next: use the markdown manifest to apply the minimal patch in a clean upstream BlackBox checkout.
- 2026-06-24 06:00 +03:00 - Packaged the plugin-loader infrastructure as a clean upstream BlackBox PR spec without touching live engine or Arena code. Added `PLUGIN_LOADER_UPSTREAM_PR.md` with the title/rationale, actual current `IGamePlugin`/`IGameExtensionPoints`/`PluginManager` implementation, inert-by-default safety contract, startup/menu wiring line context, file-by-file import manifest, maintainer design consideration that `RegisterMainMenuAction` wires an existing data-defined button rather than creating one, and upstream test guidance to replace the current `StarDriveArena.dll` drop-in proof with a minimal stub plugin DLL. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; the first parallel `UnitTests` build hit a transient Defender lock on `SDUtils.obj`, then `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors on serial retry; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 66/66 and 66/66. Next: apply the manifest in a clean BlackBox checkout alongside the minimal determinism PR, keeping plugin UI button contribution as a separate maintainer decision.
- 2026-06-24 06:18 +03:00 - Fixed the P0 live Arena career launch crash from the slot menu. Root cause was re-entrant navigation: `ArenaCareerMenuScreen` launched `ArenaFightScreen.CreateForCareerSlot(..., startAtHub:true)` through `ScreenManager.GoToScreen`, whose `ExitAll` called the menu's `ExitScreen`; that override immediately tried to `GoToScreen(MainMenu)` again. The menu now marks intentional Arena navigation and lets `ExitAll` remove it via `base.ExitScreen()` without routing back to Main Menu. Added `ArenaCareerMenuLaunchSlotStartsAtHub_Headless`, which keeps a real career menu on the screen stack, drives all New Game archetypes plus a Load-slot path through the menu launch methods, pumps the screen manager, and asserts the arena reaches the paused idle hub with no ships spawned before BOUT. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaCareerMenuLaunchSlotStartsAtHub_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 67/67 and 67/67. Next: human relaunch should confirm New/Load clicks reach the hub live, then continue with the next Arena economy/UI loop.
- 2026-06-24 06:49 +03:00 - Overnight bundle Item 0: added an Arena career save-isolation guard for tests. `CareerManager` now has an inert-by-default test guard that redirects normal live `Arena Career/career.yaml` and `slotN.yaml` paths under a temp root, records redirect/block audit paths, and blocks explicit non-temp paths outside the Arena career redirect surface. `StarDriveTest` enables the guard for every unit test and restores Arena career statics in cleanup, so accidental real-save access fails or redirects before touching the user's saves. Added `ArenaCareerSaveIsolationGuard_Headless`, which snapshots the real career/slot files, proves default and slot saves round-trip under a per-test guard root, proves an unrelated app-data path is blocked, and asserts the real files' existence/length/timestamp are unchanged. Proof: after closing a live `StarDrive BlackBox` process that locked `SDUtils.dll`, `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaCareerSaveIsolationGuard_Headless` passed; an intervening full pass hit the pre-existing `Ship.UpdateModulePositions` duel flake, then the final full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` gate passed twice, 68/68 and 68/68. Next: Item 1 adds the deterministic golden-run state hash net.
- 2026-06-24 07:22 +03:00 - Overnight bundle Item 1: added the full-career deterministic golden-run harness. `ArenaGoldenRunStateHash_Headless` runs two independent temp-slot mini-careers with seed `0xA12E600D51A7E001`, starts through the real hub-loaded `ArenaFightScreen`, selects Trivial and Hard fight-option bands, grants real option rewards, banks a win, toggles fleet composition, repairs a carried hull scar, refits a destroyed module from salvage, ticks the rival ladder, queues an elite boss option, grants a deterministic perk, save/reloads, and hashes a canonical state over event log, cash/fame/level, owned fleet designs/veterancy/scars/destroyed slots/refit overrides, salvage, perks, rival ladder IDs/ratings/W-L/rivalry state, and slot metadata. Same-seed independent runs matched hash `51b6396d643cb937b1b43f2d8e483a7ee5e683075993f33d30a66e7cf5b2d8f1` and identical event logs; failures print seed, both hashes, both event logs, and both canonical states. Proof: `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaGoldenRunStateHash_Headless` passed; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 69/69 and 69/69. Next: Item 2 extends this hash with chronicle and memorial ledgers.
- 2026-06-24 08:04 +03:00 - Overnight bundle Item 2: added deterministic Arena career memory ledgers. `ArenaCareer.Chronicle` records append-only deduped events for first fight, win/loss, ship scars, refits, repairs, boss perk claims, rival upsets, retirements, rookies, and ship-destruction memorial links; `ArenaCareer.Memorials` persists permanently lost owned ships with vessel id/name/design, kills, level, killer, cause, and fixed hex fight seed. The ledgers normalize and save/load through `[StarData]`; a deserialize bug exposed by the proof was fixed by persisting fight seeds as hex strings instead of YAML `ulong` values. The golden-run hash now includes chronicle and memorial rows and matches same-seed independent runs at `3d5ce8c3cf7f4df5a7b17fa524bdd614dbde3bbf9caac7566d75315dd1c3d27e`. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaChronicleMemorialLedger_Headless|ArenaGoldenRunStateHash_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 70/70 and 70/70. Next: Item 3 adds deterministic rival dossiers and extends the golden-run hash with active rival identities.
- 2026-06-24 08:31 +03:00 - Overnight bundle Item 3: added deterministic rival dossiers. `ArenaCareer` now persists a rival identity seed plus retired contender records, and each `ContenderRecord` carries a stable contender id, epithet, arena persona, origin hook, preferred fleet scale, and bounded short bio. Active contender normalization fills missing dossier fields deterministically while preserving existing identities, prevents duplicate active names/ids, and the living ecosystem now moves retired rivals into the retired ledger before backfilling a normalized rookie. The proof also caught another YAML scalar edge: generated fleet-scale labels are word tokens (`solo/duo/trio/squad`) so they round-trip without being parsed as numbers. The golden-run hash now covers active rival identities and matches same-seed independent runs at `148a2de834ab97c6a513a3a65cac98f22e1f8899f6882a17f1b2e0bf48c3a819`. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed after a serial retry cleared the known Defender `SDUtils.obj` lock; targeted `ArenaRivalDossiersStable_Headless|ArenaGoldenRunStateHash_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 71/71 and 71/71. Next: Item 4 wires scout meta-upgrade tiers into deterministic pre-fight/rival intel reports.
- 2026-06-24 09:09 +03:00 - Overnight bundle Item 4: wired the existing `arena_scout` meta upgrade into deterministic scout intel reports. `ArenaScoutIntel.Generate` now produces generic-only info with scout off, then gates detail by stacked scout tier: tier 1 reveals difficulty band, likely ship count, and boss warning; tier 2 adds enemy mirror-strength range plus likely rival identity/recent form from persisted dossiers; tier 3 adds the existing fight-option survival odds estimate. Added a screen-level `ScoutIntelFor` helper and extended the golden-run canonical state with a `scout|...` report row. The golden-run hash now covers scout intel and matches same-seed independent runs at `cb4357537e9933e29992c433fa39c6363a360e4875aad63aae0a99ab009ba9b9`. Proof: `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build StarDrive.csproj --no-restore` passed after a serial retry cleared the known `SDUtils.obj` lock; targeted `ArenaScoutIntelGatedByScoutTier_Headless|ArenaGoldenRunStateHash_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 72/72 and 72/72. Next: Item 5 emits deterministic Monte Carlo balance telemetry artifacts without changing balance numbers.
- 2026-06-24 09:45 +03:00 - Overnight bundle Item 5: added deterministic Monte Carlo balance telemetry artifacts without changing balance numbers. `ArenaMonteCarloTelemetry` samples fixed-seed matrices across start archetypes, fame tiers, difficulty bands, and selected fleet sizes, using existing fight-option estimates and seeded outcomes to emit JSON/CSV fields for win rate, ship-loss rate, cash delta, salvage delta, repair/refit frequency, rival churn, boss success, and soft-lock count. The artifact writer emits `arena-monte-carlo-telemetry.json` and `.csv` to the caller's temp/artifact path, and the proof compares byte-identical outputs from separate temp roots for the same seed while asserting schema and broad sanity bounds. The golden-run hash now includes a telemetry JSON digest and matches same-seed independent runs at `21d70ef571a1fb5f23fba52eafb0e12478279a776520981f2a571f928dfdee2c`. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed after a serial retry cleared a concurrent output lock; targeted `ArenaMonteCarloTelemetryArtifact_Headless|ArenaGoldenRunStateHash_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passed twice, 73/73 and 73/73. Next: separate follow-up should address the user's live roulette concern by ensuring the BOUT slate visibly offers at least six varied fights at appropriate fame/progress.
- 2026-06-24 10:10 +03:00 - Follow-up roulette fix: fresh/low-fame Arena BOUT slates now generate at least six visible choices instead of three: one trivial safety pick, two easy picks, two normal picks, and one hard higher-threat contract. Full-fame counts remain unchanged, and low-tier careers still cannot roll Elite/boss options. Strengthened `ArenaFightOptionsRiskRewardLoot_Headless` to assert fresh slates have at least six choices, at least four distinct threat bands, and a higher-threat option. Because telemetry samples the slate, the golden-run hash now updates to `5ea885af7a2d39c6f4d905ff3f2b96d481b07395d01ea2dd4c32a5785fd2eb5f`. Proof: `dotnet build StarDrive.csproj --no-restore` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed after a serial retry cleared the known Defender `SDUtils.obj` lock; targeted `ArenaFightOptionsRiskRewardLoot_Headless|ArenaFameDifficultySpectrum_Headless|ArenaMonteCarloTelemetryArtifact_Headless|ArenaGoldenRunStateHash_Headless` passed 4/4. An intervening full pass hit the existing native ship-template parallel-load `AccessViolationException`; two fresh full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-restore` passes then passed 73/73 and 73/73. Next: if live screenshots still show only three, inspect the fight-options popup layout/filtering rather than the generator.
- 2026-06-24 08:21 +03:00 - BOSS hub follow-up: the BOSS pill no longer falls back to the generic fight-options screen. Added a dedicated `ArenaBossChallengeScreen` backed by `ArenaBossChallengeOptions`, which selects six deterministic ladder contenders around the player's current fielded strength targets, displays them from safer to more dangerous relative threat, filters by career class tier, and queues/starts the chosen contender through the existing ladder challenge path. `ArenaFightOptions.PlayerStrengthForCareer` is now public so BOSS, roulette, scout, and tests share the same player-strength estimate. Proof: `dotnet build StarDriveArena.csproj -v:minimal`, `dotnet build StarDrive.csproj -v:minimal`, and `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; targeted `ArenaBossChallengeOptionsUseRelativeLadderContenders_Headless|ArenaHubBossButtonOpensContenderSlate_Headless` passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 75/75 and 75/75. Next: human launch should confirm the BOSS slate copy/layout reads clearly; if tier-3 Remnant bosses need a separate direct card, add that as a distinct option rather than reverting to generic roulette.
- 2026-06-24 08:33 +03:00 - Ace mirror/variety fix: scaled roulette enemy selection now avoids the player's fielded hull names whenever a legal alternative exists, tracks already-used enemy designs across the generated slate, and picks seeded near-target variants instead of always taking the first closest BaseStrength match. The BOSS contender slate marks and prefers non-mirror ladder contenders too, filling with mirrors only if the legal non-mirror roster is exhausted. Added `ArenaAceFightSlateAvoidsMirrorsAndVariesModels_Headless`, which starts a real Ace career, proves the six roulette rows do not use the Ace model, proves at least four distinct enemy models when the tier-1 pool supports it, and proves the BOSS slate prefers non-mirror contenders. Proof: `dotnet build StarDriveArena.csproj -v:minimal`, `dotnet build StarDrive.csproj -v:minimal`, and `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; targeted `ArenaAceFightSlateAvoidsMirrorsAndVariesModels_Headless|ArenaFightOptionsRiskRewardLoot_Headless|ArenaBossChallengeOptionsUseRelativeLadderContenders_Headless|ArenaHubBossButtonOpensContenderSlate_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 76/76 and 76/76. Next: live launch should confirm the Ace BOUT and BOSS screens read as varied opponents, not same-model mirrors.
- 2026-06-24 08:48 +03:00 - Closed the obvious finite-module customization edge case. `ApplyCustomizedDesignToActiveVessel` now routes through an inventory-budget gate: the candidate saved design's module UID counts are compared against the active vessel's current effective modules plus salvage, adoption is rejected with a concrete missing-module message when copies are short, and required extra salvage copies are consumed only after the whole design is affordable. Refit already rejected zero-count salvage; this closes the full designer adoption seam. Updated the per-vessel customize proof to seed salvage for its simulated third-design save, and added `ArenaCustomizerRejectsMissingModuleInventory_Headless`, which proves a no-salvage extra-module design is rejected without changing the vessel or consuming parts, then succeeds and consumes exactly one spare copy once salvage exists. Proof: `dotnet build StarDrive.csproj -v:minimal` and `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; targeted `ArenaCustomizerRejectsMissingModuleInventory_Headless|ArenaCustomizePerVessel_Headless|ArenaSurvivorScarsRepairAndRefitRoundTrip_Headless|ArenaApplyCareerUnlocksSalvageModulesForDesigner_Headless` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 77/77 and 77/77. Next: live designer UI still should surface the rejection text visibly; model-level enforcement is now in place.
- 2026-06-24 17:18 +03:00 - Tightened the golden-run coverage ratchet after the audit. The scripted mini-career now grants `arena_scout` alongside the boss weapon perk, so `ArenaGoldenCanonicalState` hashes a tier-1 scout intel signature rather than scout-off generic text; the test then removes the scout perk and proves the canonical scout row drops to tier 0 and the hash changes. Removed the Monte Carlo telemetry digest from the golden hash because it is intentionally seed-matrix telemetry, not player-career state; `ArenaMonteCarloTelemetryArtifact_Headless` remains the byte-identical proof for that system. New golden-run hash: `b23c8d963465c61c840cc2256deaf03f12a82ba56580501a60e0b3d6e84b88c6`. Proof: `dotnet build StarDrive.csproj -v:minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; targeted `ArenaGoldenRunStateHash_Headless|ArenaScoutIntelGatedByScoutTier|ArenaMonteCarloTelemetryArtifact` passed 3/3; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 77/77 and 77/77. Next: live playtest can focus on visible scout copy and designer rejection messaging rather than the deterministic ratchet.
- 2026-06-24 18:20 +03:00 - Made Arena determinism runtime-optional so the satellite can run on stock BlackBox with only the plugin-loader seam. Added `ArenaEngineCapabilities`, a cached-reflection shim for optional deterministic RNG, generation seeding, stable-collision, empire RNG, and serial-update hooks; Arena ladder/team duels and seeded arena creation now route through it and no-op cleanly when the engine lacks the determinism patch. Removed direct Arena references to stock-missing repair/recharge helpers and stock-045-only-missing UI helper APIs by using plugin-side shield/repair logic plus reflection/fallbacks for designer preload and module tooltip summaries. Grep proof: `EnableDeterministicRng|DeterministicCollisions|SeedDeterministicGeneration|UseDeterministicRandom` appears only in `ArenaEngineCapabilities`, and `RepairFully|RechargeShieldsFully` has zero Arena matches. Added `ArenaOptionalDeterminismUnavailableStillRuns_Headless`, which forces the capability probe unavailable and proves a ladder round, ecosystem tick, and contender BET duel still complete. Stock-045 proof: a throwaway `blackbox-stock-plugin-proof` clone from `jupiter-patch-1.60.00045` with only the plugin loader/Arena overlay compiled `StarDriveArena.csproj` 0 warnings/0 errors and passed a temporary ladder smoke test 1/1 after staging the native DLL into the proof output; the real `blackbox-upstream` checkout stayed clean. Proof: `dotnet build StarDrive.csproj -v:minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; final full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 78/78 and 78/78. Next: upstream can accept the loader PR independently while the determinism PR remains an optional reproducibility enhancement.
- 2026-06-24 18:50 +03:00 - Assembled the clean shareable Arena package under `dist/stardrive-arena-share` without publishing. The package contains the Arena satellite source under `StarDriveArena/Ship_Game/GameScreens/Arena`, a rewritten `StarDriveArena.csproj` that builds against a caller-provided `BlackBoxCheckout`, required `engine-patches/plugin-loader.patch`, optional `engine-patches/determinism.patch`, copied upstream PR specs under `docs/`, and a README with dependencies, install steps, credits, non-commercial/Steam-license notes, and the optional-determinism contract. Generated the loader patch from a throwaway stock-045 copy with plugin files, menu/startup wiring, csproj entries, and the required `InternalsVisibleTo("StarDriveArena")` seam; generated the determinism patch from a separate stock-045 copy with only the minimal RNG/spatial/serial-update surface plus contract tests, not lockstep/replay. Proof: both patches applied cleanly to fresh `jupiter-patch-1.60.00045` verification copies, and the determinism patch also passed `git apply --check` on top of the loader-patched tree. Loader-only stock build passed `dotnet build StarDrive.csproj -v:minimal` 0 warnings/0 errors; the packaged Arena project compiled against that loader-only stock tree with `-p:BlackBoxCheckout=...plugin-verify`, 0 warnings/0 errors. Optional determinism stock build passed `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj`, and `ArenaDeterminismPatch` tests passed 2/2 after staging `SDNative.dll` into the temp test output. Package audit: after removing build `obj/` byproducts, the only `.cs` files in the package are Arena plugin files and there are no binaries/content/extra file types outside `.cs/.csproj/.patch/.md`. Local proof: `dotnet build StarDrive.csproj -v:minimal` and `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 78/78 and 78/78. Next: review the share package wording/patch scope before creating any public repo.
- 2026-06-24 19:08 +03:00 - Eliminated the obsolete `InternalsVisibleTo("StarDriveArena")` coupling from the Arena share package and local fork. The stock-045 audit built `StarDriveArena.csproj` against a loader-only BlackBox tree with the IVT line removed and produced zero internal-access compile errors, so the internals list is empty; the previous optional-capability/reflection work already moved the Arena off internal engine members. Removed the `StarDriveArena` friend assembly from `Properties/AssemblyInfo.cs`, regenerated `dist/stardrive-arena-share/engine-patches/plugin-loader.patch` without any `AssemblyInfo` hunk, and updated the package README to describe the loader as a general compiled-plugin hook. Proof: `rg InternalsVisibleTo` now finds only the existing `UnitTests` friend assembly; fresh stock `jupiter-patch-1.60.00045` plus the regenerated loader patch has no `StarDriveArena` IVT, builds `StarDrive.csproj`, accepts `determinism.patch` on top, and the packaged Arena project compiles against it 0 warnings/0 errors. Package audit remains clean: only Arena `.cs` files under `StarDriveArena/Ship_Game/GameScreens/Arena`, no binaries/content/generated files. Local proof: `dotnet build StarDrive.csproj -v:minimal` and `dotnet build UnitTests/SDUnitTests.csproj -v:minimal` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena" -v:minimal` passed twice, 78/78 and 78/78. Next: the loader patch is now general enough for an upstream PR; any future engine API exposure should be a separate, narrowly justified PR.
- 2026-06-24 20:00 +03:00 - Pre-release Item 1: dealership vessel pricing now includes an intrinsic hull-value spine (combat class rank + BaseStrength) plus the cached FairDuel win-rate bonus, so larger classes and stronger Mk variants no longer collapse into the old flat price bucket. The share package Arena source was synced with the main plugin source. Strengthened `ArenaDealershipWinRatePricing_Headless` to prove catalog sort order, corvette > lower-value fighter, higher-strength Mk3 > Mk2, distinct-strength designs do not share the same flat price, and realized overperformers with at least equal hull value do not price below weaker designs. Proof: targeted `ArenaDealershipWinRatePricing_Headless` passed; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 78/78 and 78/78. Next: Item 2 restores free customizer design adoption while keeping salvage-gated refit intact.
- 2026-06-24 20:12 +03:00 - Pre-release Item 2: restored customizer adoption as free blueprinting. `ApplyCustomizedDesignToActiveVesselWithInventory` now saves any legal, career-tier-allowed design onto the active owned vessel without requiring or consuming salvage; finite module counts remain enforced only by the refit path. Removed the obsolete customizer module-budget gate from the main plugin and synced the share package source. Rewrote the regression proof as `ArenaCustomizerSavesWithoutModuleInventory_Headless`, which starts with zero spare copies, saves a custom design that needs an extra module, verifies save/reload persistence and no salvage consumption, then proves `RefitDestroyedModule` still rejects zero salvage and consumes exactly one salvaged module when provided. Proof: targeted `ArenaCustomizerSavesWithoutModuleInventory_Headless` passed; adjacent `ArenaCustomizerSavesWithoutModuleInventory_Headless|ArenaCustomizePerVessel_Headless|ArenaSurvivorScarsRepairAndRefitRoundTrip_Headless|ArenaApplyCareerUnlocksSalvageModulesForDesigner_Headless` passed 4/4; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 78/78 and 78/78. Next: Item 3 exposes betting matchup view-model details for both fight-option and contender-duel bets.
- 2026-06-24 20:22 +03:00 - Pre-release Item 3: BET quotes now expose the actual matchup. `ArenaBetQuote` and persisted `ArenaBetSlip` carry pick/opponent names plus both design/model names, with a shared `MatchupLabel`; fight-option quotes use the career's active owned vessel name/design, contender-duel quotes expose both ladder contender names/designs, and the BET screen rows/open slip now show `name (model) vs name (model)` instead of only odds copy. Synced the share package Arena source. Strengthened `ArenaBettingWagers_Headless` to assert populated matchup data for both fight-option bets and contender-duel bets, including persisted open slips. Proof: targeted `ArenaBettingWagers_Headless` passed; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 78/78 and 78/78. Visual row spacing/readability still needs live QA. Next: Item 4 constrains contender-duel pairings to comparable strength/class bands.
- 2026-06-24 20:37 +03:00 - Pre-release Item 4: contender duel pairing now uses a shared comparable-match selector instead of arbitrary/adjacent-only pairing. `CareerLadder` exposes the same/adjacent-tier plus strength-ratio band (`MaxComparableContenderStrengthRatio=5.0`) and uses nearest comparable opponents for ladder rounds; `ArenaBetting` now uses the same helper for contender-duel BET quotes, so betting no longer offers fighter-vs-battleship foregone conclusions when a comparable opponent exists. `LadderRoundResult` carries paired design/tier/ratio metadata for proofs, and the share package Arena source was synced. Strengthened `ArenaCareerLadderRound_Headless` to assert comparable pairs across a seeded spectrum of ladder rounds and `ArenaBettingWagers_Headless` to use a comparable betting pair while still proving odds/payout behavior. Proof: targeted `ArenaCareerLadderRound_Headless|ArenaBettingWagers_Headless` passed 2/2; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 78/78 and 78/78. Next: Item 5 fixes spawn orientation so ships face their opponents.
- 2026-06-24 20:48 +03:00 - Pre-release Item 5: Arena live spawns now face opponents on entry. Added Arena-local spawn wrappers that call `Ship.CreateShipAtPoint` and immediately apply deterministic facing: player ships face right toward the enemy side, enemies/bosses/fight-option enemies/challenge contenders face left toward the player side. Synced the share package Arena source. Added `ArenaSpawnOrientationFacesOpponents_Headless`, which drives the real `ArenaFightScreen` through `LoadContent` on a temp save path and asserts player/enemy facing vectors dot toward each other. Proof: targeted `ArenaSpawnOrientationFacesOpponents_Headless` passed; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 79/79 and 79/79. Visual spawn orientation still needs live QA. Next: Item 6 clarifies the Garage REFIT button tooltip.
- 2026-06-24 20:58 +03:00 - Pre-release Item 6: clarified the Garage REFIT action. `ArenaGarageScreen` now exposes a shared `RefitButtonTooltip` and assigns it to the REFIT pill: "Refit: replace a destroyed module using a salvaged part." Synced the share package Arena source. Added `ArenaGarageRefitButtonExplainsSalvageUse_Headless`, which opens the real garage popup from a temp-backed Arena screen, finds the single REFIT button, and asserts the tooltip explicitly names destroyed modules and salvaged parts. Proof: targeted `ArenaGarageRefitButtonExplainsSalvageUse_Headless` passed; `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 80/80 and 80/80. Visual tooltip placement/readability still needs live QA. Next: pre-release pass is complete; run a human live QA sweep over BET matchup rows, spawn facing, and the REFIT tooltip.
- 2026-06-24 22:05 +03:00 - Fixed live designer unlocks for purchased and looted Arena inventory. Added a shared `GrantSalvageModule` path that adds salvage and immediately calls the same module-unlock primitive used by `ApplyCareer`, then routed both `BuyArenaModule` and fight-option salvage rewards through it so the live `ArenaPlayer` designer palette updates without waiting for reload. `BuyVessel` now also unlocks the bought design's mounted module UIDs immediately, alongside the existing bought-hull unlock. Synced the share package Arena source. Strengthened proofs so module-shop buys and loot rewards start from locked, hull-compatible module UIDs, assert `ArenaPlayer.IsModuleUnlocked` flips immediately, assert `DesignerAvailableModuleUids` contains the module before reload, and keep the bought-hull False->True proof with an immediate `IsHullUnlocked` check. Proof: targeted `ArenaModuleShopBuysIntoSalvage_Headless|ArenaLootedModulesAppearInInventory_Headless|ArenaOwnedInventory_Headless` passed 3/3; `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 80/80 and 80/80. Next: live QA should confirm newly bought/looted modules appear in the customizer palette immediately, without returning to the career menu.
- 2026-06-24 22:40 +03:00 - Pre-release polish: fight roulette rows now live inside a `ScrollList<ArenaPopupListItem>` with a fixed BACK footer, so high-fame/fight-broker slates no longer resize past the panel. Added a large-slate proof that generates 13+ deterministic options and asserts every option payload is present in the scroll list without truncation. Tightened module-shop sanity by making `ModuleShopTier` the max of tech tier, price tier, and a new combat-power tier derived from weapon offense, shield/hull durability, resist/deflection, ordnance, and related combat stats. Fame 0 now offers only power-tier-1 rows; the detailed proof readout was low=6, mid=16, high=140, with former OP candidates now tier-3 gated: `Aegis`, `REDisintegratorArray`, `DisintegratorArray`, `PhasorTurret4x4`, and `QTCannon`. Synced the share package Arena source and regenerated `dist/stardrive-arena-share.zip` (41 entries, 33 `.cs`, no `bin`/`obj`/`.git`). Proof: targeted `ArenaFightOptionsScreenScrollsLargeSlate_Headless|ArenaFameGatedShopMetaResearch_Headless` passed 2/2, adjacent popup/shop tests passed 5/5, `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors, and full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 81/81 and 81/81. Visual fight-options scroll behavior still needs live QA.
- 2026-06-24 22:53 +03:00 - Pre-release shop/dealership polish: kept the already-landed fight-options scroll and no-OP-start module tiering, then ordered the module shop by designer-style buckets in the requested flow `WEAPONS -> DEFENSE -> POWER -> SPECIAL` with deterministic type/area/price/name/UID sub-order. The dealership catalog now sorts by hull class progression (`fighter -> gunboat/corvette -> frigate -> destroyer -> cruiser -> battleship -> capital`) before price/strength/name, so browsing reads by class instead of arbitrary price-only rows. Synced the share package Arena source and regenerated `dist/stardrive-arena-share.zip`; audit showed 41 zip entries, 33 `.cs`, and zero `.cs` files outside `StarDriveArena/`. Proof: targeted `ArenaFameGatedShopMetaResearch_Headless|ArenaDealershipWinRatePricing_Headless` passed 2/2; `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 81/81 and 81/81; `git diff --check` clean. Visual layout/readability for the fight-options scroll, category-ordered item shop, and class-ordered dealership still needs live QA.
- 2026-06-24 23:03 +03:00 - Consolidated the laptop-validated pre-release packaging fixes into the authoritative repo. Added `CA1416` to `NoWarn` in both the local `StarDriveArena.csproj` and packaged `dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj`, so Release plugin builds do not need extra flags. Rewrote the share README install/runtime section to specify .NET 8+ (deploy-tested with SDK 10.0.201), clarify that `--recurse-submodules` is only for rebuilding `SDNative.dll`, warn not to run from a bare source checkout, and explain that patched `StarDrive.dll` plus `StarDriveArena.dll` must be deployed into a real BlackBox + Combined Arms install using that install's native/runtime files. Regenerated `dist/stardrive-arena-share.zip`; audit showed 41 entries, 33 `.cs`, zero engine `.cs` outside `StarDriveArena/`, and zero binaries/build outputs. Gameplay polish C-F was already present and grep-verified: fight-options `ScrollList`, module power-tier gating, designer-category shop ordering, and hull-class dealership ordering. Proof: `dotnet build StarDriveArena.csproj -c Release`, packaged `dotnet build dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj -c Release -p:BlackBoxCheckout=C:\dev\stardrive\StarDrive-main`, `dotnet build StarDrive.csproj`, and `dotnet build UnitTests/SDUnitTests.csproj` all passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 81/81 and 81/81; `git diff --check` clean. Visual layout/readability for the fight-options scroll, category item shop, and class dealership still needs live QA.
- 2026-06-24 23:32 +03:00 - Fixed New Game starter leakage from the global player-custom design pool and curated the archetype starts. Added `ArenaFightScreen.IsStockContentDesign` using the engine source flags (`!IsPlayerDesign`, `!IsFromSave`, `!IsAnExistingSavedDesign`) and routed automatic player starter/default picks, wipe replacements, and fallback starters through stock-only selection while leaving explicit existing-career custom designs legal. `CareerManager` now builds deterministic curated tier-1 stock loadout candidates and rolls them by seed: Ace gets one strong light warship, Wingmates gets 2-3 mid-light ships, and Swarm gets 4-5 light craft. Added seed overloads for `StartingRosterDesigns`/`CreateNewCareer` and slot-derived deterministic seeds for `TryCreateNewSlot`. New headless proof injects an overpowered player-design into `ResourceManager.Ships`, verifies it carries the player-design flag, proves every archetype/slot/auto-starter ignores it, proves all starts are stock tier-1 legal rosters within count bands, and proves same-seed stability plus different-seed variety. Synced share package Arena sources and regenerated `dist/stardrive-arena-share.zip`; audit showed 41 entries, 33 `.cs`, zero engine `.cs` outside `StarDriveArena/`, and zero binaries/build outputs. Proof: targeted `ArenaNewGameStartLoadoutsExcludePlayerCustomDesigns_Headless|ArenaCareerSlotsChooseStartAndConfig_Headless|ArenaCareerMenuLaunchSlotStartsAtHub_Headless` passed 3/3; `dotnet build StarDrive.csproj` and `dotnet build UnitTests/SDUnitTests.csproj` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 82/82 and 82/82; `git diff --check` clean. Next live QA should start multiple New Games and confirm the loadout variety feels intentional rather than random junk.
- 2026-06-25 00:09 +03:00 - Replaced the Arena finite-salvage/items economy with a permanent module-research model. `ArenaCareer.ResearchedModules` is now the live module-unlock set, while legacy `SalvageRecord[]` remains only as a load-migration input and normalizes away after old saves migrate UIDs into research. The module shop now sells one-time research unlocks with tier/power-scaled costs, already-researched modules disappear from the catalog, and purchases immediately unlock designer palette access. Fight-option loot now grants cash instead of finite salvage, ITEMS lists researched modules, customizer blueprinting stays free, and Repair/Refit restore hull/module scars for cash while destroyed vessels still permadeath. Elite boss options now force a distinct single-boss enemy count again. Golden-run canonical state now hashes `research|UID` rows instead of salvage counts and still drives repair, cash refit, chronicle/memorial/dossier/scout coverage. Synced share package Arena sources and regenerated `dist/stardrive-arena-share.zip`; audit showed 41 entries, 33 `.cs`, zero engine `.cs` outside `StarDriveArena/`, and zero binaries/build outputs. Proof: `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 82/82 and 82/82. Next live QA should verify the shop reads as "research", Repair All restores module scars for cash, and Items shows permanent researched modules rather than stack counts.
- 2026-06-25 00:22 +03:00 - Reworked fight-option cash rewards onto an absolute enemy-strength curve so stronger/lethaler fights pay meaningfully more. `ArenaFightOptions.BuildOption` now feeds the computed total enemy fleet strength into `RewardCashForEnemyStrength`, a deterministic square-root soft curve with a hard soft-cap to keep 100-ship brawls bounded; the legacy no-option clear path now uses the same generated-fight cash instead of flat `CashPerClear`. The new proof reports early weak safety fights versus late high-strength fleets and asserts strict pre-cap monotonicity, late > early preview cash, non-special slate monotonicity by total enemy strength, and a bounded giant-fleet ceiling. Synced share package Arena sources and regenerated `dist/stardrive-arena-share.zip`; audit showed 41 entries, 33 `.cs`, zero engine `.cs` outside `StarDriveArena/`, and zero binaries/build outputs. Proof: targeted `ArenaFightRewardsScaleWithEnemyStrength_Headless` passed; affected `ArenaFightRewardsScaleWithEnemyStrength_Headless|ArenaFightOptionsRiskRewardLoot_Headless|ArenaFameDifficultySpectrum_Headless|ArenaFullDeterminismAudit_Headless` passed 4/4; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 83/83 and 83/83. Next live QA should confirm late-tier BOUT payouts feel noticeably richer than early safety contracts without runaway cash.
- 2026-06-25 01:07 +03:00 - Fixed the live Arena module-grid overlay reachability bug. Headless diagnosis reproduced the false base overlay gates at the centered zoomed-in Arena framing: the player ship rendered by the Arena scene could be outside `UniverseObjectManager.VisibleShips` with `InFrustum=false` because the base visible-world cache misses ships around the deep-space Arena center at DetailView. `ArenaFightScreen.Draw` now performs an Arena-only supplemental module-overlay pass over its managed player/enemy ship lists after the normal `UniverseScreen.Draw`, reusing `Ship.DrawModulesOverlay` and skipping ships the base visible path is already likely to draw, so Tab/ShowShipNames can reveal module grids without changing base-game culling. Added `ArenaModuleOverlayGatesReachable_Headless`, which prints the full gate vector and proves the Arena fallback would draw a live spawned ship with DetailView, ShowShipNames, sensors, active/non-dying/non-launching state, and modules present even when the base cache misses it. Synced share package Arena source and regenerated `dist/stardrive-arena-share.zip`. Proof: targeted `ArenaModuleOverlayGatesReachable_Headless` passed; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 84/84 and 84/84. Final visual confirmation that the grid appears over solid hulls still needs a human live launch.
- 2026-06-25 01:25 +03:00 - Added the cross-machine Arena determinism fingerprint experiment to decide lockstep-vs-authoritative multiplayer. `ArenaCrossMachineDeterminismFingerprint_Headless` runs a fixed-seed, serial Arena sim for 2000 steps with deterministic RNG enabled, emits 2001 per-step authoritative `SdHash128-v1` hashes plus debug lane hashes and ship digests, and writes `game/sim-output/determinism-fingerprint.txt` with GameVersion, runtime, OS, process architecture, CPU model, processor count, machine name, build fingerprint, seed, and step-count metadata. The proof runs the same seed twice on this PC and asserts identical step sequences, then runs a different generation/RNG seed and asserts a different SHA-256. PC-side fingerprint: `SequenceSha256=e204afd89aefd5b61f300c47bcc979936270947ac8b1f45de521c2418b25b203`, `GameVersion=1.60.0.0`, `RuntimeVersion=.NET 8.0.28`, `OS=Microsoft Windows 10.0.26200`, `CpuModel=Intel64 Family 6 Model 183 Stepping 1, GenuineIntel`, `ProcessorCount=24`, `BuildFingerprint=0x72EBBFD5B9D066A7`, path `C:\dev\stardrive\StarDrive-main\game\sim-output\determinism-fingerprint.txt`. Laptop reproduce command: `dotnet test UnitTests\SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~ArenaCrossMachineDeterminismFingerprint_Headless"`. Proof: targeted fingerprint test passed; `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 85/85 and 85/85. Next: run the exact command on the laptop and compare `SequenceSha256`; if it differs, use the per-step lines to locate the first divergent step/lane.
- 2026-06-25 01:55 +03:00 - Added the portable self-contained determinism fingerprint runner for laptop/PC comparison without a repo, BlackBox install, Combined Arms content, or game assets. `x64Migration/Tools/ArenaFingerprint` publishes `ArenaFingerprint.exe`, a content-free synthetic Arena sim using SplitMix64 deterministic RNG, serial float physics integration, projectile and ship collision resolution, raw-float `SdHash128-v1` per-step hashes, and a stable header. The package was published to `dist/arena-fingerprint/` and writes `sim-output/determinism-fingerprint.txt` next to the exe; the generated runtime folder is intentionally ignored because it is a large self-contained .NET publish output. PC portable fingerprint: `SequenceSha256=d63ea38ae9e7792fa6835b473e8b88c39bd0549c72d913985727962c3ea708ad`, different-seed digest `ed2884731cf958685a7d7ab65f56b3d9dbd2e998e7956b6c360da8f561d90abb`, `GameVersion=1.60.0.0`, `RunnerVersion=1.60.0-portable-fingerprint`, `RuntimeVersion=.NET 8.0.17`, `OS=Microsoft Windows 10.0.26200`, `CpuModel=Intel64 Family 6 Model 183 Stepping 1, GenuineIntel`, `ProcessorCount=24`, `BuildFingerprint=0x0BE3D389B1F227F4`, path `C:\dev\stardrive\StarDrive-main\dist\arena-fingerprint\sim-output\determinism-fingerprint.txt`. Standalone proof: copied the published folder to a temp directory with no repo/content and ran `ArenaFingerprint.exe --self-test`; same-seed rerun matched, different seed differed, and the fingerprint file was produced. Proof: `dotnet build x64Migration/Tools/ArenaFingerprint/ArenaFingerprint.csproj`, `dotnet build StarDrive.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaPortableFingerprint` tests passed 2/2; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 87/87 and 87/87. Laptop command: copy `dist/arena-fingerprint/` and run `ArenaFingerprint.exe --self-test`, then compare `SequenceSha256`.
- 2026-06-25 02:52 +03:00 - Arena multiplayer Phase 1 lockstep MVP landed as a separate path. Recon: `SDLockstep` already supplied the right core shape (`LockstepHost` orders frames, `LockstepClient` advances only when a committed frame exists, `DesyncDetector` compares per-turn hashes) and `UniverseStateLockstepSimulation` already adapts real StarDrive sims through `MoveShip`/`AttackTarget`/`StopShip`, so the clean route was to add transport/session infrastructure rather than rewrite sim control. Added a length-prefixed TCP transport (`TcpLockstepTransport`), wire codec, and minimal hello/ready/start/error session messages; added Arena multiplayer helpers exposing stable player/enemy ship IDs, deterministic lockstep prep, focus-target commands, and state hashes; added `ArenaMultiplayerSession` for in-process proof plus synchronous host/join TCP runs. In-process proof runs two real `ArenaFightScreen` peers from the same match seed over `FakeTransport`, submits per-turn focus commands with input delay, and asserts every turn hash matches; a forced local-only mutation trips the desync detector. Manual cross-machine probe is available outside the `~Arena` filter via `TwoMachineLockstepProbe_Manual`: host command `set SD_MP_ROLE=host && set SD_MP_PORT=47377 && dotnet test UnitTests\SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~TwoMachineLockstepProbe_Manual"`; join command `set SD_MP_ROLE=join && set SD_MP_HOST=<host-radmin-ip> && set SD_MP_PORT=47377 && dotnet test UnitTests\SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~TwoMachineLockstepProbe_Manual"`. Synced share package Arena sources and regenerated `dist/stardrive-arena-share.zip` (41 entries, 35 `.cs`, no `bin`/`obj`/`.git`), with the new packaged csproj explicitly referencing `SDLockstep`. Proof: targeted `ArenaMultiplayerLockstep|LockstepNetworkTransport` passed 4/4; `dotnet build StarDriveArena.csproj`, packaged `dotnet build dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj -p:BlackBoxCheckout=C:\dev\stardrive\StarDrive-main`, `dotnet build StarDrive.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 89/89 and 89/89. Human PC/laptop validation is still required to confirm the real VPN path and compare final printed hashes.
- 2026-06-25 03:18 +03:00 - Added the portable content-free network lockstep probe so the laptop can validate PC-to-laptop lockstep without a repo, BlackBox install, game content, or Combined Arms assets. `x64Migration/Tools/ArenaLockstepProbe` publishes `ArenaLockstepProbe.exe`, which runs the same synthetic deterministic arena style as the portable fingerprint through the real `SDLockstep` TCP transport/session stack: host/join hello-ready-start handshake, per-turn focus commands with input delay, commit only after both peers' delayed inputs are present, per-turn hash exchange, and desync halt/reporting. Published self-contained win-x64 output to `dist/arena-lockstep-probe/` and verified it in place plus from a copied clean temp directory with no repo/content. Portable probe digest: in-process, loopback host, and loopback join all ended at `0x6FE72CE01CEF7C18:0x8DC309465AB600AB`; forced divergence tripped at turn 30. Laptop commands: host PC `.\ArenaLockstepProbe.exe --role host --port 47377 --turns 600`; laptop `.\ArenaLockstepProbe.exe --role join --host <host-radmin-ip> --port 47377 --turns 600` (env mirrors also work: `SD_MP_ROLE`, `SD_MP_HOST`, `SD_MP_PORT`, `SD_MP_TURNS`). Proof: `dotnet build x64Migration/Tools/ArenaLockstepProbe/ArenaLockstepProbe.csproj`, published `ArenaLockstepProbe.exe --self-test`, clean-folder `ArenaLockstepProbe.exe --self-test`, targeted `ArenaPortableLockstepProbe` tests passed 2/2, `dotnet build StarDrive.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors, and full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 91/91 and 91/91. Next: copy `dist/arena-lockstep-probe/` to the laptop and compare the final printed host/join hashes across the VPN.
- 2026-06-25 04:02 +03:00 - Added Arena multiplayer Phase 2 real-arena PvP lockstep. Recon confirmed the Phase 1 path already had the right low-level pieces: `UniverseStateLockstepSimulation` applies real StarDrive commands and hashes the live `UniverseState`, while `SDLockstep` provides ordered frames and desync detection; the missing layer was explicit host/join fleet manifests and a PvP arena setup path instead of the generated PvE bout. `ArenaMultiplayerSettings` now carries encoded host and join fleet design names through the start message and settings hash, `ArenaFightScreen` can be configured before `LoadContent` to spawn those two fleets as hostile sides in the real Arena, and the session records match end, winner, turn, final digest, and fleet-manifest parity. The TCP loop now waits for the join peer to arm the simulation and for both delayed inputs before committing a turn, preserving the same lockstep contract outside the in-process fake transport. Headless proof now covers identical per-turn hashes for a bring-a-fleet PvP match, forced desync detection, and asymmetric match-end/winner detection with a stronger host fleet beating a weaker join fleet. Synced the share package Arena source and regenerated `dist/stardrive-arena-share.zip`; packaged `StarDriveArena.csproj` still builds against this checkout. Proof: targeted `ArenaMultiplayerLockstep|LockstepMessageCodec_RoundTripsSessionAndFrameMessages` passed 4/4; `dotnet build StarDriveArena.csproj --no-restore`, packaged `dotnet build dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj -p:BlackBoxCheckout=C:\dev\stardrive\StarDrive-main`, `dotnet build StarDrive.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 92/92 and 92/92; `git diff --check` clean. Real cross-machine PvP validation remains deferred until both machines have a full game/content install; the future manual path is host creates the session with both fleet manifests, join connects over VPN, and both peers compare the printed final digest/desync status.
- 2026-06-25 04:19 +03:00 - Added a minimal Arena multiplayer lobby so the Phase 2 host/join path is reachable from the game UI. The Star Gladiator career entry screen now exposes a `MULTIPLAYER` button, which opens `ArenaMultiplayerLobbyScreen` with host/IP, port, and turn-count entries plus `HOST`, `JOIN`, and `SELF TEST` actions. This first cut runs the existing real-arena TCP/in-process lockstep runner as a background test harness and reports status, winner, desync, turns, and final digest; it intentionally does not yet render the live PvP match while the lockstep loop runs, because that requires moving the network pump into the active `ArenaFightScreen.Update` path. The lobby defaults use the known-good Phase 2 seed and explicit host/join Wingmates fleet manifests after a focused test caught that a new unchecked seed path diverged at turn 0. Synced the share package source/csproj and regenerated `dist/stardrive-arena-share.zip`; audit showed 44 entries, 36 `.cs`, no `bin`/`obj`/`.git`, no engine `.cs` outside `StarDriveArena/`, and the lobby source present. Proof: targeted `ArenaMultiplayerLobbyEntryAndSelfTest|ArenaMultiplayerLockstep` passed 4/4; `dotnet build StarDriveArena.csproj --no-restore`, packaged `dotnet build dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj -p:BlackBoxCheckout=C:\dev\stardrive\StarDrive-main`, `dotnet build StarDrive.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 93/93 and 93/93; `git diff --check` clean. Next: live QA can use Arena -> Multiplayer -> Host on the PC and Join on the laptop by VPN IP to validate the hidden real-arena match result; visible playable PvP is the follow-up.
- 2026-06-25 04:43 +03:00 - Fixed the Star Gladiator career-slot menu overlap shown in live QA. The main action row is now horizontal (`CONTINUE`, `NEW GAME`, `MULTIPLAYER`, `BACK`) and the redundant top-level `LOAD` button was removed, leaving the per-slot `LOAD` buttons as the single load path. Slot rows start lower with no vertical action-list spillover, and the main buttons now have stable names for headless lookup. Synced the share package Arena source and regenerated `dist/stardrive-arena-share.zip`; audit showed 44 entries, 36 `.cs`, no `bin`/`obj`/`.git`, no engine `.cs` outside `StarDriveArena/`, and the lobby source present. Proof: `dotnet build StarDriveArena.csproj --no-restore`, packaged `dotnet build dist/stardrive-arena-share/StarDriveArena/StarDriveArena.csproj -p:BlackBoxCheckout=C:\dev\stardrive\StarDrive-main`, `dotnet build StarDrive.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; targeted `ArenaMultiplayerLobbyEntryAndSelfTest` passed 1/1; full `dotnet test UnitTests/SDUnitTests.csproj --settings unittests.runsettings --filter "FullyQualifiedName~Arena" --no-build --no-restore` passed twice, 93/93 and 93/93; `git diff --check` clean. Visual confirmation should verify the slots no longer cover the top action row at the live resolution.
- 2026-06-25 05:55 +03:00 - Added Arena multiplayer preflight fingerprints after live PC/laptop testing connected over VPN but desynced on turn 0. Diagnosis from the PC log showed the host was running `Jupiter : 1.60.00000` while the laptop path was on 045, so the network was fine but the peers were not running the same deterministic engine/content surface. `SessionHello`, `SessionReady`, and `SessionStart` now carry environment/session fingerprints; the host rejects mismatched engine/mod/runtime signatures before sending start, and the joiner validates the host's settings/fleet session hash before building the arena. Mismatches now report a clear environment/session error instead of burning a lockstep run and surfacing only `peer 2 diverged`. Synced the share package Arena source and regenerated `dist/stardrive-arena-share.zip`; audit showed 44 entries, 36 `.cs`, zero engine `.cs` outside `StarDriveArena/`, and no `bin`/`obj`/`.git`. Proof: `dotnet build StarDriveArena.csproj --no-restore` and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed 0 warnings/0 errors; focused `LockstepMessageCodec_RoundTripsSessionAndFrameMessages|TcpLockstepTransport_LocalhostRoundTrip|ArenaMultiplayer` passed 6/6; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice after the rebuild, 93/93 and 93/93; `git diff --check` clean. Next: port/rebase the arena stack onto BlackBox 1.60.00045 so both machines and the public target use one baseline instead of mixing 00000 and 045 artifacts.
- 2026-06-25 06:37 +03:00 - Ported the Arena/lockstep stack onto BlackBox `jupiter-patch-1.60.00045` via a controlled 045 branch because this fork and TeamStarDrive 045 have no usable merge base for a literal `git rebase`. Restored the additive Arena, plugin-loader, determinism, SDLockstep, portable probe, test, and share-package surfaces on top of the 045 tag, then fixed the API/merge drift exposed by compile and tests: `SDUtils` now includes the deterministic helper sources, stock `SeededRandom`/`ThreadSafeRandom` implement the deterministic `RandomBase` primitive surface, the optional generation seed is carried into `UniverseParams`, Markov planet names receive the generation seed, and generated planets seed their per-body RNG before moon/tile-event generation. The last two fixes were required because 045's generated planet/body path otherwise changed object IDs and planet lanes between same-seed Arena reruns, tripping turn-0 multiplayer and fingerprint determinism. Synced `ArenaFightScreen.cs` into `dist/stardrive-arena-share/StarDriveArena`, regenerated `dist/stardrive-arena-share.zip`, and audited the zip: 44 entries, zero engine `.cs` outside `StarDriveArena/`, and no `bin`/`obj`/`.git`. Proof: `dotnet build StarDrive.csproj --no-restore`, `dotnet build StarDriveArena.csproj --no-restore`, and `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed; targeted previous 045 failures (`ArenaMultiplayerLockstep*`, `ArenaLocalRearmContainsLowOrdnanceShips_Headless`, `ArenaFullDeterminismAudit_Headless`, `ArenaCrossMachineDeterminismFingerprint_Headless`) passed 7/7; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 93/93 and 93/93. Next: deploy the newly built 045 `StarDrive.dll` plus `StarDriveArena.dll` to both machines before retrying host/join so the preflight fingerprint sees one engine/content surface.
- 2026-06-25 07:42 +03:00 - Upgraded the Arena multiplayer entry from a hidden host/join probe to a minimal playable lobby and visible PvP match path. The lobby now lets each peer choose a race and loadout trait (`ACE`/`WINGMATES`/`SWARM`), ready up explicitly, and lets the host set match seed, turn budget, starting speed, and start-paused state before launching. `SessionLobbyMessage`, expanded `SessionStartMessage`, and `SessionControlMessage` carry the lobby selections plus synchronized host pause/speed controls; the settings hash now includes race, loadout, speed, pause, and fleet manifests. `ArenaFightScreen` can now arm a live multiplayer session: it renders the real Arena normally while keeping the base sim paused and advancing deterministic lockstep frames from the network pump, with a small in-match host HUD for pause/resume and speed cycling. Refreshed `dist/arena-pc-dlls.zip` from the built output for laptop deployment. Proof: `dotnet build StarDrive.csproj --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj --no-restore` passed; focused `ArenaMultiplayerLockstepTests|LockstepNetworkTransportTests` passed 6/6; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 93/93 and 93/93. Next live QA should verify the visible PvP screen advances on both machines, host pause/speed propagate, and both peers see the same race/loadout choices before launch.
- 2026-06-25 08:23 +03:00 - Multiplayer stabilization and regular-game setup pass after live PC/laptop QA reached turn ~460 then diverged. Current logs did not contain a root-cause trace beyond the on-screen desync, so live lockstep now records desync summaries with the divergent turn plus both peer hashes. Fixed the likely live-only divergence vector: while a multiplayer match is active, `ArenaFightScreen` now handles only screen UI input and blocks `UniverseScreen.HandleInput` world/planet/ship selection and local order paths, so clicks such as selecting a planet cannot mutate only one peer outside the lockstep command stream. The lobby no longer exposes the Arena `ACE/WINGMATES/SWARM` selector; it shows race plus a regular-game map setting instead while the Arena wrapper keeps a deterministic hidden default fleet for its current PvP test path. Added a regular BlackBox/Combined Arms multiplayer settings contract (`ArenaRegularMultiplayerSettings`) that maps host race, remote race-as-first-opponent, galaxy size, star abundance, mode, difficulty, opponents, pace, turn timer, extra planets, richness, game speed, and pause into real `UniverseParams`; added `UniverseScreenLockstepSimulation` for full regular 4X lockstep via `UniverseScreen.SingleSimulationStep`, not the Arena/skirmish-only object update. Fixed seeded regular-game generation leaks by making nonzero `GenerationSeed` seed `UniverseGenerator` placement/background and seed `UniverseState.Random` before empire trait/economy setup; then the regular setup reseeds before and after load-content so starting ships are placed deterministically and both peers begin with identical RNG stream state. Headless proofs: focused multiplayer/transport/regular setup passed 6/6; full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 95/95 and 95/95. Refreshed `dist/arena-pc-dlls.zip`; hashes: StarDrive.dll `FD7E74FB96132929998B5AD2550D504CBA08BFAD9D16F6BF7806068775628997`, StarDriveArena.dll `48842E1D6DB1D7B649D47DB5FF02CE4E04C910F118F48995DCFE6A2932C18571`, SDLockstep.dll `5425E3231075DBF29837822A0EC9D8A9CAB4826A963B8670414E24B9EDE14CB1`, SDUtils.dll `26ED9DBE3631CE709954A04AB820E7CD94CBCCFF615368FAB547DE86423D216A`, SDGraphics.dll `525571955190F75C091F4E939C70B46AD00889272610F0C5437D5AAD77B2D688`. Honest limitation: regular-game generation/full-step lockstep is now proven, but true two-human 4X play still needs a player-perspective/control-command layer because BlackBox has a single `UniverseState.Player`; the remote race is currently represented as the first generated opponent in the regular setup proof.
- 2026-06-25 08:39 +03:00 - Added live Arena multiplayer telemetry for overnight PC/laptop attempts. Each lobby and live-match session now writes a unique `sim-output/arena-multiplayer-*.log` plus a stable `sim-output/arena-multiplayer-last-session.log` containing environment hash/summary, settings/session hash, fleet manifests, host/join lobby events, ready/launch/preflight failures, network errors, pause/speed controls, sampled per-turn hashes, remote checksum/input observations, completion, and detailed desync summaries with both peer hashes. This should make the next turn-460-style divergence diagnosable from logs rather than screenshots alone. Added `ArenaMultiplayerTelemetryWritesSessionArtifacts_Headless`, proving the session and last-session artifacts are written and include env/settings/fleet/turn/end records. Proof: `dotnet build StarDriveArena.csproj --no-restore`, `dotnet build UnitTests/SDUnitTests.csproj --no-restore`, focused `FullyQualifiedName~ArenaMultiplayer` passed 7/7, explicit `dotnet build StarDrive.csproj --no-restore` passed, and full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 96/96 and 96/96. Refreshed `dist/arena-pc-dlls.zip`; hashes: StarDrive.dll `A8954A8B29DCBE366237852EF4002B92D20108081C0CB4D75F39987300BCE14C`, StarDriveArena.dll `C946F2E9C476C3995F644779413BEC9ECF98588103F864542D2D3C3BE53B122C`, SDLockstep.dll `0FD00E28057A4A27972C90F8743B4410B16A2B7A9B951BCB0AA5B8AE68C597E2`, SDUtils.dll `AF83B4C70AA50D803AAFF55577A8D1FA767BFA3975B66DAF8EFA83AABA1310AF`, SDGraphics.dll `565793CFF466B4FB39ABAA11FA29108B6F974CF240D12B2FF5F4823B50145CF3`. Next: deploy this exact DLL bundle to both machines, retry host/join, then compare each machine's `arena-multiplayer-last-session.log` if a mismatch or desync appears.
- 2026-06-25 19:19 +03:00 - Investigated the live PC/laptop desync at turn 290. Host telemetry confirms both peers had matching environment/session fingerprints, launched `Draylok` vs `Dauntless`, matched checksums through turn 240, then reported a true state divergence at turn 290 (`peer 1 0x9A6C0D2F67B1CD81:0xD5ED9AFA87E031E7` vs `peer 2 0x120FDABE452A196D:0x5641ACD6064C66C8`) observed by the host at turn 292. This was not a missing-input/network-preflight failure. The likely live-only cause was local camera/frustum/visibility leakage: `UniverseObjectManager.UpdateVisibleObjects` rewrites `InFrustum` from each peer's local screen/camera, and StarDrive combat/projectile paths contain RNG/effect branches gated by `InFrustum`/`InPlayerSensorRange`; different PC/laptop view state can therefore perturb the next authoritative tick. This also explains the laptop symptom where ships could be selected but not seen and had no icons. Fix: during live Arena multiplayer, before and after each lockstep-applied tick, pin the Arena camera, mark all active ships/projectiles in the arena universe as `InFrustum`, and mark both managed PvP fleets seen by the shared viewer. Added `ArenaMultiplayerLiveStabilizesVisibilityForBothFleets_Headless`, proving spawned host/join fleet ships are restored to frustum + player sensor visibility after the hook. Proof: `dotnet build StarDriveArena.csproj --no-restore`, `dotnet build UnitTests/SDUnitTests.csproj --no-restore`, focused `FullyQualifiedName~ArenaMultiplayer` passed 8/8, targeted `ArenaLivingContenderEcosystem_Headless` passed after one unrelated full-suite order failure, packaged Arena build passed, and full `dotnet test UnitTests/SDUnitTests.csproj --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Refreshed `dist/arena-pc-dlls.zip`; hashes: StarDrive.dll `AEF81282C3472859D3B7FC9D0148BA7546A55923BD8D436596FA3407D4AABCE1`, StarDriveArena.dll `34F4E80FCE491EA99C5D8B5095A3523F3BCBDA24AEEA8C2E5FA9DFC3D2DC113E`, SDLockstep.dll `3794425C14DFF2975B2EE1CAB976196B2F4AB8A656AD5B7CF8B023B1FE7E4021`, SDUtils.dll `5FD3328D5268C09904AEAA3267139991471590603DD3BB48C781522F453AAC62`, SDGraphics.dll `8F639332DB5B7ECEFF4AC0671E84982739C3B5B583E5E0C397860108EC0310BD`. Next: redeploy this bundle to both machines and retry; if a later desync remains, the logs should now show it after frustum/visibility has been neutralized.
- 2026-06-25 20:01 +03:00 - Added StarDrive 4X multiplayer Phase A as a new authoritative subsystem separate from Arena lockstep. Recon showed the existing `SDLockstep.SimCommand` vocabulary is combat-only (`MoveShip`/`AttackTarget`/`StopShip`) and normal 4X UI mostly mutates state directly, while the reusable full-step adapter is `UniverseScreenLockstepSimulation`. The Phase A design therefore adds primitive authoritative request/result/snapshot messages to `SDLockstep`, plus `Ship_Game.Multiplayer.Authoritative` with a `AuthoritativePlayerCommand` envelope, host-side validation/apply, and an in-process host/client proof. The vertical slice covers three real systems: ship movement through `Ship.AI.OrderMoveTo`, planet policy via `Planet.CType` with the UI's blueprint/trade-hub side effects, and research selection through `Empire.Research.SetTopic`; illegal ownership requests are rejected as deterministic no-ops. The sync artifact is a Phase-A canonical snapshot: full authoritative `UniverseStateHash` plus a command-relevant payload/digest over empires, research topics, planets/colony types/queues, and ships/AI move state; true binary full-state transfer/reconnect remains the next state-sync phase. Proof: `dotnet build StarDrive.csproj -p:Platform=x64 --no-restore` passed after a serial retry cleared a parallel `SDUtils` output lock; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-restore` passed 0 warnings/0 errors; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed 2/2, proving codec round-trip, request -> authority validate/apply -> client sync for move/policy/research, and rejected enemy planet mutation; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Next: Phase B should add an actual 4X lobby/race/settings UI, widen the command vocabulary to construction/fleets/research queue, and replace the Phase-A canonical digest with real snapshot/delta replication plus reconnect.
- 2026-06-25 20:30 +03:00 - Added authoritative 4X human-to-human diplomacy foundations. Recon found the reusable treaty mechanics on `Empire`/`Relationship` (`SignTreatyWith`, `SignAllianceWith`, `DeclareWarOn`) and the AI proposal paths in `Empire.UpdateRelationships` plus `EmpireAI.RunDiplomaticPlanner`; a new runtime `AuthoritativeHumanPlayers` registry gates only human-vs-human pairs out of those AI diplomacy turns, leaving single-player, Arena MP, human-vs-AI, and AI-vs-AI behavior unchanged when the registry is empty. Added `DiplomacyProposal`/`DiplomacyResponse` authoritative commands, target-routed `AuthoritativeDiplomacyPopupMessage`, pending proposal state, accept/reject/counter handling, unilateral declare-war, and accepted peace/NAP/trade/alliance application through engine treaty state. The in-process authoritative harness now supports two client replicas and target-only popup routing while rejecting a peer that tries to command another empire. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed after a serial retry cleared a parallel `StarDrive.dll` object lock; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed 4/4, proving popup codec round-trip, human-vs-human AI-turn suppression, trade reject, unilateral war notice, accepted peace/NAP/trade/alliance, spoof rejection, and synced authority/client hashes; final full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Live popup UI remains a later phase: the routed popup model/message is ready for the client UI to consume. Next: build the 4X MP lobby/race/settings surface and wire real popup accept/reject/counter screens on top of this protocol.
- 2026-06-25 21:32 +03:00 - Fixed the authoritative human diplomacy declare-war leak found by audit. The human-vs-human declare-war path no longer calls `EmpireAI.DeclareWarOn`, so it cannot hit the stock `them.isPlayer` -> `AIDeclaresWarOnPlayer` -> `DiplomacyScreen.Show` branch; instead it applies the same relationship/war state directly, breaks treaties, calls the reciprocal `GetWarDeclaredOnUs` bookkeeping, and relies solely on the authoritative routed popup for the target human. Added a `DiplomacyScreen` debug counter at the add-screen seam and extended the two-client proof so the remote human declares war on the host's local `isPlayer` empire with diplomacy screens enabled; war applies, the target popup routes, both replicas sync, and the counter remains zero. Also changed `AuthoritativeHumanPlayers` from a static dictionary to `ConditionalWeakTable` keys so disposed `UniverseState` instances are not retained by the human registry. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed after a serial retry cleared a parallel object lock; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Next: consume the routed diplomacy popup message in the real 4X MP client UI.
- 2026-06-25 21:53 +03:00 - Added StarDrive 4X multiplayer Phase B core lobby foundations on top of the authoritative Phase A session. Recon confirmed the single-player new-game path builds `UniverseParams` from selected race/traits/settings and hands it to `UniverseGenerator`, while the Phase A harness already owns peer-to-empire validation and synced client replicas. Added `Authoritative4XLobby`: host-owned game settings mirror the normal new-game knobs, the roster tracks joined peers, race, validated trait options, and ready state, trait validation enforces the same point budget/exclusion rules as `RaceDesignScreen`, and `StartInProcess` generates deterministic authority/client worlds, registers one human-controlled empire per player, maps peer-to-empire, then hands off to `Authoritative4XInProcessMultiClientSession`. The full-game replica proof initially caught a real generation determinism gap; the fix seeds `ResourceManager.Planets` before every lobby-generated universe so planet-type RNG matches across authority and replicas. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed 5/5, proving illegal over-budget traits reject, host settings flow into a generated game, three distinct human-selected races get homeworlds and human registry entries, selected traits reach the host empire, and a real colony-policy command syncs all replicas; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Next: add the real MP lobby UI/transport messages on top of this model and widen the authoritative command vocabulary for construction/fleets/research queues.
- 2026-06-25 22:48 +03:00 - Added StarDrive 4X multiplayer Phase C shipyard-loop commands. Recon confirmed ship designs already serialize through `ShipDesign.GetBase64DesignString`/`ShipDesign.FromBytes`, registered player designs enter the engine via `ResourceManager.AddShipTemplate` plus per-empire buildable registration, and real planet ship construction uses `Planet.Construction.Enqueue(IShipDesign, QueueItemType, ...)`. Added `DesignShip` and `QueueBuild` authoritative commands: the authority decodes and validates submitted designs against unlocked hulls/modules and mobile-ship legality, registers legal designs for human-controlled empires without depending on the single-player `isPlayer` UI filter, rejects stock-name conflicts/locked-module designs, and queues accepted designs only at owned spaceport planets. Extended the authoritative sync payload to hash player design layout signatures plus full planet queue entries, so design/build state is covered by replica sync rather than only queue counts. Proof: `dotnet build StarDrive.csproj -c Debug -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -v minimal` passed 0 warnings/0 errors; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed 6/6, including legal design registration, illegal locked-module rejection, owned-planet queueing, wrong-planet/unbuildable rejection, and authority/client sync; full `dotnet test UnitTests/SDUnitTests.csproj --filter "FullyQualifiedName~Arena" -c Debug -v minimal` passed twice, 97/97 and 97/97. Next: widen the authoritative 4X command vocabulary to research queues, fleet orders, colony build queues/building selection, and real MP client UI for the lobby/diplomacy/shipyard flows.
- 2026-06-25 23:21 +03:00 - Sustained 4X MP chunk 1: added the real TCP authoritative session spine. `Authoritative4XNetworkHost` wraps the existing host authority on top of `TcpLockstepTransport`, validates peer-to-empire ownership, applies accepted `AuthoritativePlayerCommand`s, sends result/snapshot messages, and routes target-human diplomacy popups. `Authoritative4XNetworkClient` is a passive replica endpoint: UI code can submit commands, then it mutates only when the host result and canonical snapshot arrive. Added a loopback TCP proof that connects host/client over `127.0.0.1`, sends colony-policy plus Base64 ship-design and queue-build commands over the real socket, proves host validation and passive client replica sync, and rejects an empire-spoof command. Recon subagents confirmed the next network limits: current TCP accepts one remote socket and host snapshots go only to the requesting peer, so N-player broadcast/reconnect/delta sync remains a follow-up; UI-hook recon recommends high-level screen dispatch seams rather than mutating shared sim APIs. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed after a serial retry cleared a known parallel output lock; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 7/7 and 7/7; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` had one known native parallel ship-design loader `AccessViolationException` retry, then passed two clean confirmation runs, 97/97 and 97/97. Next: extend the network host to broadcast accepted commands/snapshots to every peer and begin UI-side command dispatch for colony/research/ship-design seams.
- 2026-06-25 23:33 +03:00 - Sustained 4X MP chunk 2: made accepted authoritative commands broadcast-safe for passive replicas. `AuthoritativeCommandResultMessage` now carries `OriginPeer`, appended after the legacy result fields on the wire, and authoritative clients key pending commands by `(origin peer, sequence)` so different players can safely reuse local sequence numbers. The multi-client and TCP host paths now broadcast the canonical accepted/rejected command to every replica before the result/snapshot; clients store that broadcast command and apply the host result only after the matching snapshot arrives. Added `Authoritative4XMultiClient_BroadcastsAcceptedCommandsToEveryReplica_Headless`, which proves peer A and peer B can both use sequence `500`, each command mutates authority plus all replicas, remote replicas see the correct origin peer, and sync remains stable. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; targeted `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 8/8 and 8/8; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Next: extend `TcpLockstepTransport`/session hosting beyond one remote socket, then start UI-side command dispatch at the colony/research/ship-design seams.
- 2026-06-25 23:47 +03:00 - Sustained 4X MP chunk 3: extended the real TCP transport for N-client authoritative hosting while preserving the existing Arena two-player `Host/Join` path. `TcpLockstepTransport.HostMulti` now accepts multiple remote sockets, maps each connection by announced peer id via `JoinAsPeer`, keeps per-connection write locks, queues outbound messages until a peer mapping exists, and exposes mapped peer ids for host readiness checks. Added `Authoritative4XNetworkTcpMultiClient_BroadcastsCommandsToEveryReplica_Headless`, which connects two real loopback TCP clients to one authoritative host, has both peers reuse local sequence `300`, proves each command broadcasts to every passive replica with the correct `OriginPeer`, and proves all authority/client snapshots stay synced. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed after a serial retry cleared a known parallel `SDUtils` output lock; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 11/11 and 11/11; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: start UI-side command dispatch at high-level colony/research/ship-design seams and continue widening the playable 4X command vocabulary.
- 2026-06-26 00:02 +03:00 - Sustained 4X MP chunk 4: widened the authoritative 4X command vocabulary with real colony building queue requests. Added `QueueBuilding` to `AuthoritativePlayerCommand`, validated it host-side against empire ownership plus the planet's current `GetBuildingsCanBuild()` list, and applied accepted requests through the real `SBProduction.Enqueue(Building, where:null, playerAdded:true)` path. The canonical sync payload now includes queued building tile `X/Y`, so deterministic placement divergence is caught immediately. Added `Authoritative4XColonyBuildQueue_QueuesBuildingsAndSyncsPlacement_Headless`, which builds a deterministic queueable colony tile, queues a real building, proves authority and replica reserve the same tile and sync digest changes, then rejects wrong-planet and unknown-building requests as no-ops. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed after a serial retry cleared a known parallel `SDUtils` output lock; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 12/12 and 12/12; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: hook high-level colony UI actions to submit authoritative commands when an MP client context is active, then add troop/orbital/fleet-order commands.
- 2026-06-26 00:26 +03:00 - Sustained 4X MP chunk 5: added the first real UI command dispatch seam for passive authoritative clients. `Authoritative4XClientContext` is an inert-by-default runtime context that live screens can enter when rendering a synced replica; when active for the local empire, colony building queue, mobile ship queue, and colony-type changes submit `AuthoritativePlayerCommand`s instead of mutating the replica directly. Unsupported troop/orbital local builds are blocked under the context until dedicated authoritative commands exist, preserving single-player behavior when no context is active. Added `Authoritative4XClientContext_SubmitsColonyCommandsWithoutLocalMutation_Headless`, proving building/ship/policy submissions carry the right empire, planet, sequence, and payload while the passive colony queue/type remain unchanged before host acceptance; wrong-empire UI calls are ignored and disposing the context restores the default. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Note: the proof stays at the dispatch seam because constructing the full colony screen in the headless harness requires live font graphics; live UI behavior still needs human QA. Next: add authoritative commands for troop/orbital queues or start routing research/ship-design screens through the same client context.
- 2026-06-26 00:48 +03:00 - Sustained 4X MP chunk 6: routed research topic selection through the same passive authoritative client context. `ResearchScreenNew` tech-node clicks and `SearchTechScreen` search selections now submit `SetResearchTopic` to the host when the context is active, then leave the replica research queue unchanged until the accepted host snapshot arrives; single-player retains the existing local add/remove queue behavior. Extended the client-context proof so research submission carries the correct sequence and tech UID, does not mutate `Empire.Research.Topic` locally, and ignores wrong-empire research calls. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add authoritative queue/reorder semantics for the full research queue, or move to troop/orbital build commands so more colony actions are playable.
- 2026-06-26 01:08 +03:00 - Sustained 4X MP chunk 7: added authoritative troop queueing so the colony troop button no longer has to be blocked under multiplayer. Added `QueueTroop` to the primitive command envelope and codec, validated host-side against owned planet, spaceport, known troop template, and empire troop unlock, then applied accepted requests through `SBProduction.Enqueue(Troop, QueueItemType.Troop)`. The passive client context now submits repeat troop queue requests instead of mutating its local construction queue, while single-player keeps the direct enqueue path. Proof extends the authoritative colony queue test to accept a real troop, prove authority/client `TroopType` sync and payload coverage, and reject wrong-planet plus unknown-troop requests; the client-context proof confirms repeated troop submissions carry stable sequences and do not locally queue troops before host acceptance. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add orbital/shipyard object queue support or begin fleet-order commands beyond single-ship move.
- 2026-06-26 01:30 +03:00 - Sustained 4X MP chunk 8: routed basic selected-ship movement through the authoritative client context. `MoveShip` now carries `MoveOrder` stance/waypoint flags in the existing command envelope `TargetId`, the host validates the supported flags and applies them through real `ShipAI.OrderMoveTo`, and `ShipMoveCommands.MoveShipToLocation` submits a passive-client command instead of mutating local AI when the context is active. This covers single selected ship right-click/drag movement; fleet/group formation movement still needs a later fleet-order command because it fans out through `ShipGroup.MoveTo`. Proof extends the codec test with move-order flag round-trip, the authoritative apply test with aggressive waypoint host application and synced replicas, and the client-context proof with no local `MovePosition` mutation before host acceptance. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add authoritative fleet/group move commands or attack/planet-order commands so the main map is playable beyond single-ship movement.
- 2026-06-26 02:05 +03:00 - Sustained 4X MP chunk 9: added the first authoritative combat target command. `AttackShip` now travels through the primitive command envelope, the host validates ship ownership, active attacker/target state, non-troop scope, and attackable empire relationship before applying the real `ShipAI.OrderAttackSpecificTarget`/`OrderQueueSpecificTarget` paths. Passive MP clients now submit selected-ship enemy target clicks through `Authoritative4XClientContext` instead of mutating local ship AI; unsupported active-client friendly escort/troop boarding clicks are blocked until those receive dedicated commands. The canonical sync payload now hashes each ship's target id, priority-target flag, and target queue ids so accepted attack orders are covered by replica sync. Proof extends codec round-trip, authoritative apply/reject/sync, and client-context no-local-mutation tests, including a synced at-war attack and wrong-empire attack rejection. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add planet-target orders (colonize/bombard/land troops/orbit) or group/fleet target commands so the map UI keeps closing toward full playability.
- 2026-06-26 02:29 +03:00 - Sustained 4X MP chunk 10: added authoritative ship-to-planet order plumbing. `ShipPlanetOrder` now carries an explicit order type (`Orbit`, `Colonize`, `Bombard`, `LandTroops`), clear-orders flag, and supported move stance; the host validates ownership, active mobile ship state, target planet existence, and order-specific legality before applying the real engine paths (`Ship.OrderToOrbit`, `MarkForColonization`, `OrderBombardPlanet`, `OrderLandAllTroops`). `ShipMoveCommands.RightClickOnPlanet` now submits the matching passive-client command when the authoritative client context is active, leaving single-player behavior unchanged when inactive; rebase/escort/group formation remain follow-ups. The canonical sync payload now includes a compact ship AI order-queue signature so planet orders are covered by replica sync, not just acknowledged. Proof extends codec round-trip, accepted orbit apply/sync, wrong-empire planet-order rejection, and client-context no-local-mutation tests. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors after a serial retry cleared a known parallel `SDUtils` output lock; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 13/13 and 13/13; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add group/fleet move/attack command fan-out or route the ship-design save UI into the authoritative `DesignShip` command.
- 2026-06-26 02:51 +03:00 - Sustained 4X MP chunk 11: routed multi-selected/fleet map orders through the passive authoritative context. Added batch helpers that fan out the existing primitive per-ship `MoveShip`, `AttackShip`, and `ShipPlanetOrder` commands after validating that the selected ships belong to the active MP empire; `ShipMoveCommands` now intercepts fleet/group enemy-target, planet-target, and empty-space move paths before local `ShipGroup.FinalPosition`, formation assignment, order clearing, or AI mutation occurs. This is intentionally conservative: the host receives per-ship orders rather than a full formation-preserving fleet command, so richer formation semantics remain a follow-up. Added a focused batch no-local-mutation proof over two owned ships, proving submitted sequence fan-out for move, attack, and planet orders while local `MovePosition`, target, and order queues stay unchanged before host acceptance. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors after a serial retry cleared a known parallel `SDUtils` output lock; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 14/14 and 14/14; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add a real formation-preserving fleet command or route ship-design save/create UI through `DesignShip`.
- 2026-06-26 03:22 +03:00 - Sustained 4X MP chunk 12: routed ship-design saves through the authoritative client context. `Authoritative4XClientContext` now exposes `TrySubmitDesignShip`, serializing the current `ShipDesign` through the existing Base64 design payload and submitting the already-proven `DesignShip` command. `ShipDesignScreen.SaveShipDesign` now returns success and, when an authoritative client context is active for the player empire, submits the design without writing/registering it locally; `ShipDesignSaveScreen` closes cleanly in that passive path instead of expecting a local `ResourceManager` template before host acceptance. Single-player still writes the `.design`, registers the template, updates buildables, and changes hull as before. Added a focused proof that submits a legal design from a passive client, decodes the payload back to the expected design name/hull, and asserts `ResourceManager.Ships` remains unregistered before host acceptance. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors after a serial retry cleared a known `SDUtils` output lock; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 15/15 and 15/15; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: wire a live MP context owner around the actual networked client screen/lobby flow so these passive UI seams are active in cross-machine play.
- 2026-06-26 02:08 +03:00 - Sustained 4X MP chunk 13: added the live authoritative 4X session bridge that can own a real `UniverseScreen`. `Authoritative4XLiveSession` now wraps either the TCP host or client, installs the passive `Authoritative4XClientContext`, polls network traffic from the screen lifecycle, submits host-local UI commands through the same validate/apply/broadcast path as remote commands, and emits deterministic `NoOp` heartbeat commands while the host is unpaused. `UniverseScreen` now attaches/detaches the live session and polls it during update. Proofs: a live host attached to a screen broadcasts heartbeat snapshots over real loopback TCP, and a live client attached to a screen submits a colony-policy UI command through the TCP host with authority/client digests synced. Verification already run for this chunk: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 17/17 and 17/17; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Honest next blocker: remote-client local-view ownership is still not solved, because generated client universes currently still treat the host empire as `UState.Player`; the next pass must give clients a local empire/view context before full cross-machine 4X is actually playable.
- 2026-06-26 02:24 +03:00 - Sustained 4X MP chunk 14: added screen-level local empire ownership for authoritative clients without mutating deterministic simulation ownership. `UniverseScreen.Player` now resolves to the live session's assigned local empire for UI/view code, while `UState.Player` and `Empire.isPlayer` remain unchanged for deterministic replay and stock simulation behavior. Added `IsLocalEmpireForUi`, `IsLocalShipForUi`, and `LocalShipCanTakeFleetOrders`, then routed high-value ship selection/order gates through them so a remote client can select and issue commands to its own assigned empire rather than the host empire. Added a live loopback proof where peer 2 controls the second generated empire, the visible client screen reports that empire as `Player`, simulation `isPlayer` flags remain untouched, the client can submit an owned building queue command, and host/client digests sync; wrong-host-empire UI submission is rejected by the passive context. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 18/18 and 18/18; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: add screen-local visibility/draw helpers for the assigned empire, then wire the existing multiplayer lobby shell to launch a generated 4X authoritative session instead of an Arena skirmish.
- 2026-06-26 02:34 +03:00 - Sustained 4X MP chunk 15: added screen-local visibility for authoritative 4X clients. The UI layer now asks whether ships are known/visible to `UniverseScreen.Player` instead of the serialized `UState.Player` in click hit-tests, tactical shields, ship scene-object creation, thruster rendering, ship/range/inhibition overlays, fleet names/lines, selected-goal rendering, and pie-menu/local combat filters. This keeps `Ship.InPlayerSensorRange`, `KnownByPlayer`, and the simulation/player flags untouched for deterministic replay, while letting a remote client see and interact with the empire assigned by the live session. The local-view proof now explicitly marks the assigned empire's ship as seen, proves `IsKnownToLocalPlayerForUi`/`IsVisibleToLocalPlayerInMapForUi` are true even when `Ship.InPlayerSensorRange` differs, and still proves accepted commands sync through the host. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 18/18 and 18/18; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: use the existing multiplayer lobby shell to launch a generated 4X authoritative session with host/join peer ids and settings, instead of routing only to the Arena skirmish path.
- 2026-06-26 02:45 +03:00 - Sustained 4X MP chunk 16: split the authoritative 4X lobby's generated-game handoff out of the in-process test harness. `Authoritative4XLobby.StartGeneratedGame()` now validates the roster, normalizes host settings, generates the real `UniverseScreen`, maps peer ids to human empire ids, registers human-controlled empires, and returns an `Authoritative4XGeneratedGameStart` that a live TCP host/join UI can attach to `Authoritative4XLiveSession` without creating test-only client replicas. `StartInProcess()` now reuses that handoff path, so the test harness and future live UI share the same generation contract. Added `Authoritative4XLobby_StartGeneratedGameHandoff_Headless`, proving settings, pause/game speed, peer-to-empire map, selected races, homeworlds, and human registry all survive the handoff. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 19/19 and 19/19; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: extend the visible multiplayer lobby flow to send a 4X start payload and attach live host/client sessions to this generated-game handoff.
- 2026-06-26 02:54 +03:00 - Sustained 4X MP chunk 17: extended the shared `SDLockstep` session-start wire payload with optional authoritative 4X launch fields while preserving the existing Arena/skirmish payload. `SessionStartMessage` now carries a guarded `IsAuthoritative4X` flag plus peer ids, generation seed, galaxy/stars/mode/difficulty/opponent/settings knobs, and host/join trait option strings; the codec appends these after the existing fields so Arena sessions leave them inert. The authoritative message round-trip proof now covers every 4X field and still exercises the existing command/result/snapshot/popup codec paths. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 19/19 and 19/19; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Next: make `ArenaMultiplayerLobbyScreen` build/send/consume this 4X start payload and launch a generated authoritative 4X `UniverseScreen`.
- 2026-06-26 03:06 +03:00 - Sustained 4X MP chunk 18: wired the visible multiplayer lobby shell to launch generated authoritative 4X sessions instead of the Arena skirmish path. Host now uses `TcpLockstepTransport.HostMulti` with authority peer 1, host player peer 2, and join peer 3; join announces peer 3; host sends a guarded 4X `SessionStartMessage`; both sides generate the same `Authoritative4XLobby` game and attach `Authoritative4XLiveSession` to the visible `UniverseScreen`. The network host now skips socket broadcast to its host-local peer while still broadcasting to remote peers, and the live-host proof covers this local/remote split. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests|FullyQualifiedName~LockstepNetworkTransportTests` passed twice, 19/19 and 19/19; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live cross-machine host/join still needs human QA.
- 2026-06-26 03:32 +03:00 - Sustained 4X MP chunk 19: expanded the visible multiplayer lobby from a thin launch shell into a first-pass 4X setup surface. The lobby now carries a racial trait option over `SessionLobbyMessage`, exposes host-controlled map size, star density, difficulty, AI count, planet richness, seconds-per-turn, pacing, start-pause, seed, and speed controls, and includes host/join trait selections in the guarded authoritative 4X `SessionStartMessage`. The generated-game proof now builds a real same-payload `Authoritative4XLobby` game and asserts the selected races/traits plus galaxy settings survive into `UniverseParams` and the human empires. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 -v minimal` passed 0 warnings/0 errors; focused `ArenaMultiplayerLobbyEntryAndSelfTest|LockstepMessageCodec_RoundTripsSessionAndFrameMessages|Authoritative4XLobby` passed twice, 4/4 and 4/4; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live QA still needs to confirm the denser lobby layout reads cleanly at the target resolution.
- 2026-06-26 03:54 +03:00 - Sustained 4X MP chunk 20: made authoritative human diplomacy visible/playable in the live 4X session. The TCP host now retains diplomacy popups addressed to the host-local human instead of discarding them, passive clients expose a drain path for routed proposal popups, and `Authoritative4XLiveSession` queues both sources for the active `UniverseScreen`. Added a non-pausing `AuthoritativeDiplomacyPopupScreen` so proposals can be accepted/rejected without locally pausing one peer, and wired `UniverseScreen.UpdateAuthoritative4XMultiplayer` to open one pending popup at a time. Popup buttons submit `DiplomacyResponse` through `Authoritative4XClientContext`, preserving the host-validate/apply/sync contract. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 19/19 and 19/19; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live QA still needs to confirm the popup appears over a real host/join game and that accept/reject feels clear in the running UI.
- 2026-06-26 04:12 +03:00 - Sustained 4X MP chunk 21: added a minimal visible human-to-human proposal path. The diplomacy overview `Contact` button now opens an authoritative proposal picker for human-vs-human MP relationships and continues to use the stock AI `DiplomacyScreen` for single-player, human-vs-AI, and AI diplomacy. The picker offers non-aggression, trade, alliance, peace, and declare-war actions, submits `DiplomacyProposal` through the passive `Authoritative4XClientContext`, and does not add another local pause. `MainDiplomacyScreen` also avoids pausing the universe when it is opened inside an authoritative 4X session, because pausing only one peer would break the live multiplayer contract. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 20/20 and 20/20; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live QA should verify Contact opens the proposal picker between two human empires and still opens the normal diplomacy screen for AI empires.
- 2026-06-26 04:34 +03:00 - Sustained 4X MP chunk 22: added authoritative colony labor allocation. `SetColonyLabor` now travels through the primitive command envelope with bit-exact float payloads and lock bits; the host validates planet ownership, player-managed colony type, finite percentages, sum-to-one, and cybernetic food restrictions before applying the real `Food/Prod/Res.Percent` and `PercentLock` state plus `UpdateIncomes`. Passive authoritative clients now submit slider drags and lock toggles without mutating their local replica, while single-player sliders keep the existing direct behavior. The canonical sync payload now hashes each planet's labor percentages and lock flags so replicas cannot drift silently in worker allocation. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 22/22 and 22/22; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live QA should verify colony labor sliders feel responsive in a real host/join session once snapshots return from the authority.
- 2026-06-26 04:58 +03:00 - Sustained 4X MP chunk 23: added authoritative construction queue cancel/reorder controls. `CancelConstructionQueueItem` and `ReorderConstructionQueueItem` now travel through the primitive command envelope by planet id and queue index; the host validates planet ownership and current queue bounds before applying the real `SBProduction.Cancel` and `MoveTo` paths. Passive authoritative clients now route queue up/down/top/bottom buttons, drag reorder, and cancel through `Authoritative4XClientContext`, blocking wrong-owner controls instead of mutating a local replica; single-player queue controls keep their existing local behavior. The existing canonical queue digest already covers queue order and item removal, and the new tests assert the digest changes on reorder and stays synced after cancel/reject cases. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 24/24 and 24/24; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Rush-buy/continuous-rush queue controls are intentionally left for a later economy-sensitive command.
- 2026-06-26 05:22 +03:00 - Sustained 4X MP chunk 24: added authoritative empire budget controls. `SetEmpireBudget` now carries tax rate, treasury goal, and auto-tax state with bit-exact float payloads; the host validates 0..1 values, applies the real empire data, runs the economic planner when auto-tax is enabled, and updates net planet incomes. `BudgetScreen` now routes tax slider, treasury slider, and auto-tax checkbox changes through the passive authoritative context without submitting during initial screen binding; single-player budget controls keep their existing direct behavior. The canonical sync payload now hashes tax rate, treasury goal, and auto-tax state so replica budget drift is visible. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 26/26 and 26/26; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97. Live QA should verify the budget popup stays usable in a host/join game and that auto-tax updates once the host snapshot returns.
- 2026-06-26 05:25 +03:00 - Sustained 4X MP chunk 25: added authoritative research queue controls. `QueueResearch`, `RemoveResearchQueueItem`, and `MoveResearchQueueItem` now travel through the primitive command envelope; the host validates tech existence, empire tech entries, discovered/researchable state for new queue items, queued state for remove/move, and applies the real `EmpireResearch` prerequisite-aware add/remove/up/down/top paths. Passive authoritative clients now route research node clicks, search-screen queueing, and research queue up/down/top/cancel buttons through `Authoritative4XClientContext` without mutating the replica; single-player keeps the existing direct queue behavior. The canonical sync payload now hashes full research queue order, not just the active topic, so queue drift is visible in replica digests. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 28/28 and 28/28; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify research screen clicks/search/queue buttons feel clear in a real host/join game once accepted snapshots return.
- 2026-06-26 05:39 +03:00 - Sustained 4X MP chunk 26: added authoritative construction rush controls. `RushConstructionQueueItem` and `ToggleConstructionRush` now travel through the primitive command envelope; the host validates planet ownership, queue bounds, incomplete queue items, and finite positive rush amounts before applying the real `SBProduction.RushProduction` path or flipping the queue item's continuous-rush flag. Passive authoritative clients now route the colony screen queue rush button and the empire-management colony-list rush/cancel shortcuts through `Authoritative4XClientContext`; single-player keeps the existing direct sim-thread behavior. The canonical construction queue payload now hashes each item's `Rush` flag so continuous-rush drift is caught by replica digests. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 29/29 and 29/29; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify rush button feedback is understandable while waiting for the authority snapshot.
- 2026-06-26 05:57 +03:00 - Sustained 4X MP chunk 27: added authoritative planet storage/import/export policy controls. `SetPlanetGoodsState` now carries food/production storage policy through the primitive command envelope; the host validates planet ownership, legal goods kind, legal `Planet.GoodState`, and cybernetic-food restrictions before applying `FS`/`PS`. Passive authoritative clients now route colony-screen and empire-management food/production dropdown clicks through `Authoritative4XClientContext` without mutating their local replica; single-player keeps the existing direct behavior. The canonical planet payload now hashes `FS` and `PS` so storage-policy drift is caught by replica digests. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 31/31 and 31/31; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify storage dropdown feedback is clear while waiting for the authority snapshot.
- 2026-06-26 06:15 +03:00 - Sustained 4X MP chunk 28: added authoritative colony governor budget and prioritized-port controls. `SetPlanetManualBudget` and `SetPlanetPrioritizedPort` now carry manual civilian/ground/space defense budget overrides and prioritized-port toggles through the primitive command envelope; the host validates ownership, finite non-negative budget values, legal budget kinds, and space-port requirements before applying the real planet setters. Passive authoritative clients now submit governor budget checkbox/text-entry changes and prioritized-port toggles without locally mutating their replica, while single-player keeps the existing direct behavior. The canonical planet payload now hashes prioritized-port state plus all three manual budget overrides so replica drift is visible. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 33/33 and 33/33; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify governor checkbox/text-entry feedback is understandable while waiting for the accepted authority snapshot.
- 2026-06-26 06:31 +03:00 - Sustained 4X MP chunk 29: added authoritative fleet hotkey assignment for the first fleet-ops slice. `SetFleetAssignment` now carries player fleet slot replace/add/clear requests through the primitive command envelope; the host validates fleet-key range, assignment mode, unique ship ids, ship existence/activity/ownership, and `CanBeAddedToFleets()` before applying the real `Empire.CreateFleet`/`Fleet.Reset`/`Fleet.AddShips`/`Fleet.AutoArrange` path. The live universe fleet hotkey handler now submits replace/add/clear through `Authoritative4XClientContext` in authoritative MP and keeps single-player behavior unchanged. The canonical sync payload now hashes fleet rows plus each ship's fleet id/key so fleet-membership drift is visible. Subagent audits identified fleet assignment/layout as the next P0 command gap; this chunk intentionally covers hotkey assignment only, leaving the broader fleet designer layout editor for a separate pass. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 35/35 and 35/35; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify Ctrl/Add fleet hotkeys feel clear while waiting for the accepted authority snapshot.
- 2026-06-26 07:04 +03:00 - Sustained 4X MP chunk 30: added authoritative selected-fleet movement. `MoveFleet` now carries fleet-key, destination, facing vector, and allowed move flags through the primitive command envelope; the host validates player fleet range, ownership-by-fleet lookup, finite destination/facing, non-empty movable fleets, and rejects internal-only movement flags before applying the real `Fleet.MoveTo` path so formation offsets and right-drag facing are preserved. The live selected-fleet move path now submits one fleet command in authoritative MP, falling back to the existing per-ship command only for loose ship groups, and keeps single-player local movement unchanged. Existing canonical fleet rows already hash final position/direction plus fleet nodes, so movement drift is covered by the sync digest. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 37/37 and 37/37; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify right-click and right-drag fleet move/rotate commands feel responsive while waiting for the accepted authority snapshot.
- 2026-06-26 07:36 +03:00 - Sustained 4X MP chunk 31: routed the visible `Go Exploring` ship pie-menu action through authoritative 4X. Added `ShipSpecialOrder` with the first enabled order type, `Explore`; the host validates legal order type, ship existence/activity/ownership, and mobile non-troop fleet-order eligibility before applying the real `ShipAI.OrderExplore` path. Passive clients now submit the explore request through `Authoritative4XClientContext` and do not locally start exploration before host acceptance, while single-player falls through to the existing local order. Subagent audits ran in parallel for ship stance, refit/repair, and fleet layout; they identify combat stance as the next small safe slice and refit/orbital/fleet-design as larger hash-expansion work. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 39/39 and 39/39; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify the ship pie menu's Go Exploring action waits for the accepted authority snapshot in host/join play.
- 2026-06-26 08:05 +03:00 - Sustained 4X MP chunk 32: routed ship combat stance through authoritative 4X. `SetShipCombatStance` now carries ship id + `CombatState`; the host validates legal stance, active owned ship, and the same stance-button eligibility before calling the real `Ship.SetCombatStance`. Passive clients submit stance changes from `ShipStanceButtons` without local mutation, and the stance UI now filters against the authoritative local empire instead of only `isPlayer`. The canonical ship payload now hashes `AI.CombatState`, so stance drift is visible. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 41/41 and 41/41; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify ship stance buttons are visible for the assigned human empire and wait for accepted authority snapshots in host/join play.
- 2026-06-26 07:29 +03:00 - Sustained 4X MP chunk 33: routed fleet renaming through authoritative 4X. `RenameFleet` now carries fleet key + name through the primitive command envelope; the host validates player fleet range, existing fleet, non-empty <=40-character name, and no control characters before applying `Fleet.Name`. Passive clients submit fleet-name edits from the fleet designer without locally mutating the replica, and the text entry no longer re-emits commands during programmatic redraws or overwrites in-progress typing before the accepted snapshot returns. The canonical fleet payload already hashes fleet names, and the new proof asserts the digest changes on accepted rename and stays synced on rejects. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 43/43 and 43/43; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify fleet-name editing feels acceptable while waiting for host snapshots.
- 2026-06-26 07:42 +03:00 - Sustained 4X MP chunk 34: routed fleet auto-arrange through authoritative 4X. `AutoArrangeFleet` now carries a fleet key through the primitive command envelope; the host validates player fleet range and a real non-empty owned fleet before invoking the existing `Fleet.AutoArrange()` + `Fleet.Update(FixedSimTime.Zero)` path. The fleet designer's `Auto Arrange` button now submits the command in authoritative MP and still mutates locally in single-player. The proof scrambles fleet node offsets on both replicas first, then asserts the accepted command changes the offset digest, syncs both replicas, and rejects missing/empty fleets without mutation. Verification: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 45/45 and 45/45; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify the fleet designer's auto-arrange feedback is clear while waiting for the accepted host snapshot.
- 2026-06-26 07:57 +03:00 - Sustained 4X MP chunk 35: made live authoritative 4X pause/speed host-owned and network-synced. `Authoritative4XLiveSession` now exposes host-only pause/speed controls, applies them locally, and broadcasts `SessionControlMessage` through the existing TCP transport; passive network clients consume that message through `Authoritative4XClientReplica.ApplySessionControl`. The base Universe pause and game-speed input handlers now route through the live session when authoritative 4X is active, so hosts broadcast changes and clients consume the keys without local mutation. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 46/46 and 46/46; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify host pause/speed keys update the client during a real host/join 4X session and client-side keys no longer create local-only drift.
- 2026-06-26 08:21 +03:00 - Sustained 4X MP chunk 36: routed visible ship scrap/scuttle orders through authoritative 4X. Added `ShipLifecycleOrder` with `Scrap` and `Scuttle` variants; the host validates active owned ships, scrappable eligibility, and platform-vs-mobile routing before applying the real `ShipAI.OrderScrapShip` path or arming the scuttle timer. The ship pie menu, ship-info scrap button, and selected-ship list scrap button now submit lifecycle requests in passive authoritative MP and keep single-player direct behavior unchanged. The canonical ship payload now hashes `ScuttleTimer`, so delayed self-destruct state is covered by replica digests. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 48/48 and 48/48; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Subagent audit identified the next good slices as ad-hoc ship-group move routing, fleet patrol commands, colony governor policy commands, and 4X live telemetry/visibility diagnostics for cross-machine testing.
- 2026-06-26 08:37 +03:00 - Sustained 4X MP chunk 37: fixed authoritative 4X client local-view ship visibility so assigned-empy ships can render icons/meshes on non-host clients. Ship-level `IsVisibleToPlayer`/`IsVisibleToPlayerInMap` now use the `UniverseScreen` local-view visibility helper instead of hardwiring only `UState.Player`, and `AttachAuthoritative4XMultiplayer` refreshes the assigned local empire's own/allied ship visibility for immediate UI readiness. The local-view helper treats the assigned empire's own ships as known without rewriting `UState.Player` sensor knowledge, which matches the passive-client contract and addresses the laptop symptom where ships were selectable but not visible. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 48/48 and 48/48; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify the joined laptop can see its assigned fleet icons and hulls after launch.
- 2026-06-26 08:52 +03:00 - Sustained 4X MP chunk 38: routed loose selected-ship group movement through the authoritative 4X move path. `MoveShipGroupToMouse` now delegates projected/new/existing loose groups to `ShipMoveCommands.MoveFleetToLocation`, so passive clients submit per-ship movement requests through `Authoritative4XClientContext` instead of directly mutating `ShipGroup.MoveTo`; selected real fleets still use the existing fleet-level command path, and single-player behavior keeps the same helper fallback. Extended the batch fleet command proof to build a loose `ShipGroup`, drive the real move helper under passive authoritative context, assert two `MoveShip` commands are submitted, and assert neither ship's local move state changes before host acceptance. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 48/48 and 48/48; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next good slices remain fleet patrol/loadout commands, colony governor policy commands, and persistent 4X live telemetry for desync diagnosis.
- 2026-06-26 09:17 +03:00 - Sustained 4X MP chunk 39: added persistent live authoritative 4X telemetry for host/join diagnosis. `Authoritative4XLiveTelemetry` writes bounded session logs under `sim-output` in real live sessions, with begin/env/peer/path rows, UI command rows, accepted/rejected result rows with snapshot hashes/digests, host pause/speed control rows, routed diplomacy popup rows, network-error rows, and poll-exception rows before rethrow. Unit-test processes keep telemetry disabled by default to avoid artifact noise, while the dedicated proof enables it to a temp root and verifies the evidence rows are written and cleaned up. `Authoritative4XLiveSession` exposes the session path for debugging and samples heartbeat snapshots instead of logging every frame. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 49/49 and 49/49; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should copy `game/sim-output/authoritative-4x-last-host.log` and `authoritative-4x-last-client.log` after any failed cross-machine attempt.
- 2026-06-26 09:31 +03:00 - Sustained 4X MP chunk 40: made fleet movement clear active patrols only on host acceptance and added patrol state to the authoritative sync digest. `ApplyMoveFleet` now clears `Fleet.Patrol` with `clearOrders:false` immediately before the accepted `Fleet.MoveTo`, while the visible `MoveFleetToMouse` path skips its old direct `ClearPatrol` when an authoritative context owns the fleet so passive clients do not mutate local patrol state before the snapshot. `AuthoritativeStateSnapshot` now hashes active fleet patrol name, current waypoint index, and waypoint positions/directions/detour flags. Proof extends `Authoritative4XFleetMove_UsesFleetMoveToAndSyncs_Headless` with synchronized patrol setup and asserts the accepted move clears patrols on authority and replica, and extends `Authoritative4XClientContext_SubmitsFleetMoveWithoutLocalMutation_Headless` to prove passive submission leaves the local patrol intact. Verification: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; targeted fleet movement/context tests passed 2/2; focused `FullyQualifiedName~Authoritative4XSessionTests` passed twice, 49/49 and 49/49; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena" -v minimal` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next patrol slice should route saved patrol plan create/load/rename/delete through authoritative commands.
- 2026-06-26 09:47 +03:00 - Sustained 4X MP chunk 41: routed saved fleet patrol plan loading through authoritative 4X. Added `LoadFleetPatrol` to the primitive command envelope and host applicator, validating player fleet range, non-empty owned fleet, and an existing saved patrol name before applying the real `Fleet.LoadPatrol` path. `ChoosePatrolPlan` now submits the load request under `Authoritative4XClientContext` and leaves passive replicas untouched until host acceptance, while single-player still loads locally. `AuthoritativeStateSnapshot` now hashes each empire's saved patrol plan database as `FP` rows in addition to active fleet patrol assignment, so saved-plan drift is visible before load. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; targeted patrol/codec tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 51/51 and 51/51; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Subagent audits identified next slices: saved patrol create/clear/rename/delete, colonization goal toggles, tile-specific colony build/scrap, and richer live telemetry rows for remote commands/snapshots/local-view diagnostics.
- 2026-06-26 10:02 +03:00 - Sustained 4X MP chunk 42: added host-side live telemetry for remote authoritative commands. `Authoritative4XNetworkHost` now records each processed command/result/snapshot in a drainable queue, and `Authoritative4XLiveSession` drains it so inbound network commands are logged as `COMMAND source=network peer=...` before the corresponding result/snapshot row. Local UI commands keep their existing `source=ui` row, and heartbeat NoOps remain throttled. The live telemetry proof now sends a real loopback remote colony-policy command, waits for authority plus replica state to reflect it, and asserts the host log contains the remote peer command row and sequence. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused telemetry proof passed 1/1; `dotnet build StarDrive.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 51/51 and 51/51; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next diagnostics slice should add snapshot payload summaries and local-view/selection rows so cross-machine logs show why a desync or invisible ship happened.
- 2026-06-26 10:11 +03:00 - Sustained 4X MP chunk 43: added compact canonical snapshot summaries to live authoritative 4X telemetry. Each logged result now emits a `SNAPSHOT` row with tick, sync digest, deterministic payload hash, payload character count, and canonical row counts by prefix (`E/D/R/FP/F/P/Q/S/...`). This gives cross-machine desync logs a quick way to identify which state families are present and whether the payload changed without dumping the whole canonical state into every live log. The telemetry proof now asserts `SNAPSHOT`, `payloadHash`, and key row-count tokens appear after a live loopback command. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused telemetry proof passed 1/1; `dotnet build StarDrive.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 51/51 and 51/51; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next: add local-view/selection/visibility telemetry rows, then resume command routing for colonization and saved patrol management.
- 2026-06-26 10:27 +03:00 - Sustained 4X MP chunk 44: routed manual colonization mark/cancel through authoritative 4X. Added `SetColonizationGoal` to the primitive command envelope; the host validates the target planet exists, is unowned, habitable, and carries a supported enabled/disabled state before applying the real `MarkForColonization` / `CancelColonization` paths. Planet info, planet-list colonize button, and the universe context-menu mark action now submit through `Authoritative4XClientContext` when active, leaving passive replicas untouched until host acceptance while single-player keeps the existing direct behavior. The canonical sync payload now hashes manual `MarkForColonization` goals as `G` rows so mark/cancel drift is visible. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; targeted colonization/codec tests passed 3/3; `dotnet build StarDrive.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 53/53 and 53/53; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next good command slices: saved patrol create/clear/rename/delete, tile-specific colony build/scrap, and troop send/recall.
- 2026-06-26 10:51 +03:00 - Sustained 4X MP chunk 45: routed tile-specific colony building placement through authoritative 4X. `QueueBuilding` now has an explicit-tile variant that carries tile coordinates through the existing primitive command envelope, and the host validates the requested tile before calling the real construction enqueue path. The colony build screen now submits dragged tile placement through `Authoritative4XClientContext` and blocks local mutation while an authoritative context is active; single-player still falls through to the existing local enqueue behavior. Proof covers command codec fields, passive client submission without local queue mutation, accepted host placement at tile `(1,0)`, replica queue sync, and canonical digest/payload coverage of the requested tile. Verification: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 54/54 and 54/54; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Subagent scouting identified the next high-value chunks as live desync telemetry detail, fleet designer layout commands, and troop logistics.
- 2026-06-26 11:09 +03:00 - Sustained 4X MP chunk 46: added structured live authoritative 4X sync-mismatch telemetry. Client replica mismatches now throw a typed `Authoritative4XSyncMismatchException` carrying command/result plus authority/client snapshots; `Authoritative4XLiveSession` records it before rethrowing. Live telemetry now emits `SYNC_MISMATCH` rows with origin, sequence, command kind, tick, both hashes/digests, row-count summaries, first differing canonical payload line, and payload artifact paths, and writes authority/client payload files next to the session log. The new loopback proof deliberately perturbs a digest-covered client replica field after the host accepts a command and asserts the typed mismatch, telemetry row, and persisted payload artifacts. Verification: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused live telemetry tests passed 2/2; focused `FullyQualifiedName~Authoritative` passed twice, 55/55 and 55/55; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next: use these logs in cross-machine runs, then continue fleet designer layout commands or troop logistics.
- 2026-06-26 11:44 +03:00 - Sustained 4X MP chunk 47: routed fleet-designer layout edits through authoritative 4X. Added `SetFleetLayout` to the primitive command envelope with deterministic fleet-node serialization for ship id/name, relative offset, tactical weights, combat stance, and orders radius; the host validates player fleet range, buildable design-only nodes, active owned ships, duplicate ship ids, finite values, and legal combat states before replacing the real fleet layout. Passive clients now submit fleet-designer add/remove/drag layout requests without local mutation, while single-player keeps the existing direct behavior; live-goal fleet nodes are blocked from this generic layout command until a dedicated requisition/refit command lands. A subagent audit caught stale key-0 fleet leakage from `Fleet.Reset`, so fleet replace/clear now removes the old fleet object by reference after reset before creating replacements; the canonical sync payload now hashes fleet-node tactical fields in addition to name/offset. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 57/57 and 57/57; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Subagent scouting identified the next high-value authoritative slices as deep-space build/cancel, freighter routes/AO toggles, and refit-to workflow; live QA should verify fleet designer add/remove/drag waits cleanly for host snapshots.
- 2026-06-26 12:04 +03:00 - Sustained 4X MP chunk 48: routed deep-space build placement through authoritative 4X. Added `QueueDeepSpaceBuild` to the primitive command envelope with design name, world build position, target planet/system ids, and tether offset; the host validates finite positions, buildable station designs, planet/system consistency, orbit/radiation/mining/research legality, then applies the real `ProcessResearchStation`, `MiningOps`, or `BuildConstructionShip` constructors used by the single-player deep-space build window. Passive clients now submit `DeepSpaceBuildingWindow` placements through `Authoritative4XClientContext` and do not add local AI goals before host acceptance; single-player still falls through to the direct path. The canonical sync payload now hashes pending deep-space/research/mining build goals as `G|...|DeepSpace` rows, including type, step, design, target ids, build/move positions, target ship, and build planet, so live telemetry can identify this state family. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Deep-space build cancellation remains a separate follow-up because it needs a stable command identity across AI goals, constructor order queues, and planet construction queues.
- 2026-06-26 12:27 +03:00 - Sustained 4X MP chunk 49: routed deep-space build cancellation through authoritative 4X. Added `CancelDeepSpaceBuild` to the primitive command envelope with goal type, design name, target ids, and build position; passive clients now use the same `OnScrapSelectedItem` UI path to submit cancellation requests without removing local AI goals before host acceptance. The host validates the owned pending deep-space goal by type/design/target/build position, removes it through the real AI goal path, and preserves the existing constructor-scrap/queue-cancel fallback for later-stage goals. The canonical queue payload now hashes `QueueItem.IsCancelled`, so a cancel that reaches construction queue state is covered by sync digests instead of being invisible. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; touched deep-space/context/codec tests passed 2/2 after rebuild; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Subagent scouting identified the next high-value authoritative slices as colony governor/logistics direct bindings, fleet requisition/patrol mutation paths, and remaining ship-list order buttons.
- 2026-06-26 12:46 +03:00 - Sustained 4X MP chunk 50: routed the direct-bound colony governor option checkboxes through authoritative 4X. Added `SetPlanetGovernorOptions` as a full snapshot of the seven risky governor/logistics booleans (`GovOrbitals`, `AutoBuildTroops`, `DontScrapBuildings`, `Quarantine`, `ManualOrbitals`, `GovGroundDefense`, `SpecializedTradeHub`), with host-side ownership/flag validation and real planet field/setter application. `GovernorDetailsComponent` now submits those checkbox changes through `Authoritative4XClientContext`; passive clients immediately restore the local visual/data flip caused by `UICheckBox` binding and wait for the accepted host snapshot, while single-player keeps the existing direct behavior. The canonical planet payload now hashes the seven governor option flags, so these settings are covered by sync digests. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; touched governor/context tests passed 2/2; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining colony logistics gaps from scout: garrison/manual orbital sliders, manual trade import/export slot sliders, blueprints, and troop/defense buttons.
- 2026-06-26 13:03 +03:00 - Sustained 4X MP chunk 51: routed remaining ship-list/order-button explore and lifecycle toggles through authoritative 4X. Added `ClearOrders` to ship special orders and `CancelScuttle` to ship lifecycle orders, then wired `ShipListScreenItem` explore/scrap/scuttle toggle paths and the group `OrdersButton` explore path through `Authoritative4XClientContext` so passive clients no longer clear orders, start explore/scrap, or arm/cancel platform scuttle timers locally. The old single-player fallbacks remain. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; touched ship special/lifecycle/context/codec tests passed 5/5; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining ship-list gap: shift mass-scrap is still broader multi-ship behavior and should get a dedicated batch if needed.
- 2026-06-26 13:23 +03:00 - Sustained 4X MP chunk 52: routed manual colony trade import/export slot sliders through authoritative 4X. Added `SetPlanetManualTradeSlots` to the primitive command envelope with the six manual food/production/colonist import/export slot counts, bounded to the existing UI slider ranges (imports 0..20, exports 0..25). The host validates ownership and payload ranges before applying the real `Planet.Manual*Slots` fields; passive authoritative clients now submit trade-tab slider changes and reset the local slider back to the host-accepted planet value instead of mutating the replica every frame, while single-player keeps the old direct write path. The canonical planet payload now hashes all six manual trade slot values so host/client drift is visible. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused manual trade/codec/context tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining colony logistics gaps include garrison/manual orbital sliders, blueprint application, and troop/defense buttons.
- 2026-06-26 13:38 +03:00 - Sustained 4X MP chunk 53: routed colony defense target sliders through authoritative 4X. Added `SetPlanetDefenseTargets` for garrison size plus wanted platform/shipyard/station counts, bounded to the existing defense-tab slider ranges (garrison 0..25, platforms 0..15, shipyards 0..3, stations 0..10). The host validates planet ownership and ranges before applying `GarrisonSize` plus the real wanted-orbital setters; passive authoritative clients now submit defense-tab slider changes and reset the local sliders to the host-accepted values instead of mutating the replica, while single-player keeps the old direct behavior. The canonical planet payload now hashes all four defense target values. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused defense-target/codec/context tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 59/59 and 59/59; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining colony/UI gaps include blueprint apply/clear/edit paths and troop/defense buttons.
- 2026-06-26 13:56 +03:00 - Sustained 4X MP chunk 54: routed saved fleet patrol rename/delete through authoritative 4X. Added `RenameFleetPatrol` and `DeleteFleetPatrol` to the primitive command envelope; the host validates owned saved patrols, legal unique names, duplicate rename rejection, and missing-plan rejection before applying the real saved patrol rename/delete paths. Deleting a saved patrol now also clears any active fleet assignment to that plan on the authority and replicas. `EmpirePatrolsScreen` submits rename/delete through `Authoritative4XClientContext` for passive MP clients while preserving the direct single-player path; passive clients do not locally rename/delete/clear patrol state before host acceptance. Existing canonical `FP` rows plus active fleet patrol rows cover saved-plan and fleet-assignment changes. Proof: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused patrol/codec/context tests passed 5/5; focused `FullyQualifiedName~Authoritative` passed twice, 61/61 and 61/61; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining patrol gaps are active clear/create saved route from current waypoints.
- 2026-06-26 14:09 +03:00 - Sustained 4X MP chunk 55: routed active fleet patrol clearing through authoritative 4X. Added `ClearFleetPatrol` keyed by fleet id; the host validates player fleet range, owned non-empty fleet, and active patrol presence before applying the real `Fleet.ClearPatrol()` path and updating the fleet. Right-clicking the patrol order button now submits the clear request under `Authoritative4XClientContext`, so passive MP clients do not locally drop patrol assignment or orders before host acceptance, while single-player falls back to the existing local clear. The proof asserts clear removes only the active assignment, preserves the saved patrol plan, rejects a no-op second clear, and leaves passive replicas unchanged until the host snapshot; existing active fleet patrol digest rows cover the state change. Verification: `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; focused patrol/codec/context tests passed 5/5; focused `FullyQualifiedName~Authoritative` passed twice, 61/61 and 61/61; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next patrol gap is saved-route creation from current waypoints, which needs deterministic waypoint payload encoding.
- 2026-06-26 14:26 +03:00 - Sustained 4X MP chunk 56: routed saved fleet patrol creation from current waypoints through authoritative 4X. Added `CreateFleetPatrol` with a bounded, bit-exact waypoint payload (`position`, `direction`, `IsDetour`) and host validation for player fleet range, owned non-empty fleets, inactive patrol state, finite waypoint values, and at least two waypoints. Host and replica apply the accepted command through the real `Fleet.CreatePatrol` path, so it creates the saved `FleetPatrol`, activates it, clears/reissues fleet orders through patrol task logic, and uses the existing deterministic saved-plan naming path. The patrol order button now submits create requests under `Authoritative4XClientContext`; passive MP clients do not locally add saved patrol plans or activate patrols before host acceptance, while single-player keeps the existing direct create fallback. Proof: focused patrol/codec/context tests passed 6/6; focused `FullyQualifiedName~Authoritative` passed twice, 62/62 and 62/62; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Patrol plan create/load/rename/delete/clear is now covered by authoritative commands; next safe slices from scouts are colony blueprint apply/clear or colony defense orbital build buttons.
- 2026-06-26 14:47 +03:00 - Sustained 4X MP chunk 57: routed colony defense orbital build buttons through authoritative 4X. Added `QueuePlanetOrbitalBuild` (`planetId + designName`) and host validation for owned planets, known orbital designs, buildable platforms/stations/shipyards, and the existing orbital-limit check before applying the real `Planet.AddOrbital` path. `GovernorDetailsComponent` now submits Build Platform/Station/Shipyard through `Authoritative4XClientContext` in MP, so passive clients do not add AI goals or queue orbitals locally before host acceptance, while single-player keeps the direct path. The accepted command queues the same `BuildOrbital` deployment goal as stock play; the existing canonical goal rows cover the new planet-tethered orbital goal and the focused proof asserts authority/client digest sync. Proof: focused orbital/deep-space/codec/context tests passed 5/5; focused `FullyQualifiedName~Authoritative` passed twice, 64/64 and 64/64; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64` passed; `dotnet build StarDrive.csproj -p:Platform=x64` passed; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 --no-build --filter "TestCategory~Arena|FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining colony defense/troop gap is troop launch/recall/ground combat, which still needs stable troop identity and digest rows before routing.
- 2026-06-26 15:08 +03:00 - Sustained 4X MP chunk 58: routed colony blueprint apply/clear through authoritative 4X. Added `ApplyColonyBlueprints` and `ClearColonyBlueprints` with a full deterministic `BlueprintsTemplate` payload instead of name-only local AppData lookup; host validation requires an owned planet, a valid non-linked template, bounded blueprint building count, and real blueprint-suitable building UIDs before applying the stock `Planet.AddBlueprints` / `RemoveBlueprints` path. The apply command mirrors the live UI side effects by clearing `DontScrapBuildings` and `SpecializedTradeHub`, and passive clients now submit load/edit/clear actions from the governor blueprint path without local mutation while single-player keeps direct behavior. `AuthoritativeStateSnapshot` now hashes `BP` rows for blueprint name, mod, exclusive/link/type, planned building UIDs, buildable planned UIDs, and completion/achievable percentages, so blueprint drift is visible in sync payloads and live mismatch telemetry. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; focused blueprint/codec/context tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 66/66 and 66/66; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Linked blueprint chains are intentionally blocked in authoritative MP until synced template-catalog support exists.
- 2026-06-26 15:31 +03:00 - Sustained 4X MP chunk 59: routed colony tile scrap through authoritative 4X. Added `ScrapColonyTile` with explicit tile coordinates and a scrap kind for building vs biosphere removal; host validation checks owned planet, integer tile coordinates, supported kind, expected building name, scrappability, and biosphere presence before applying the real `Planet.ScrapBuilding` / `DestroyBioSpheres` paths. `ColonyScreen` now submits building and biosphere scrap confirmations through `Authoritative4XClientContext` while passive clients avoid local mutation; single-player keeps the direct local behavior. `AuthoritativeStateSnapshot` now hashes colony tile rows (`T|planet|x|y|building|biosphere|habitable|terraformable`) so building and biosphere removals are visible in sync payloads and mismatch telemetry. Subagent scouting identified the next high-value chunks as extracting/firming the 4X lobby network flow, adding mismatch telemetry polish, and routing the remaining ship-info Explore button through the existing special-order command. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; focused codec/tile-scrap/context tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 68/68 and 68/68; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Troop launch/recall remains deferred until stable troop identity and digest rows exist.
- 2026-06-26 15:44 +03:00 - Sustained 4X MP chunk 60: routed the ship-info panel Explore toggle through authoritative 4X. The single-ship info button now uses a small helper that submits `ShipSpecialOrder.Explore` or `ClearOrders` through `Authoritative4XClientContext` when an MP passive client is active, so it no longer mutates local ship AI before host acceptance; single-player still applies the local order directly. This also fixes the local off-click path for that button by clearing orders instead of writing through `Ship.DoingExplore`, whose setter always starts exploration. Proof invokes the UI helper under an active authoritative context and asserts Explore/ClearOrders commands are submitted while local AI state/order queues remain unchanged, including a wrong-empire blocked case. Verification: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; focused ship-special/context/UI-helper tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 69/69 and 69/69; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next likely chunk is the lobby network-flow helper or mismatch telemetry polish.
- 2026-06-26 16:01 +03:00 - Sustained 4X MP chunk 61: extracted the authoritative 4X lobby network handoff into `Authoritative4XLobbyNetworkFlow`. The helper now owns two-player host/join peer ids, `SessionLobbyMessage` build/apply, `SessionStartMessage` build/validate, generated-game recreation from a start payload, and live-session attach for host or join. `ArenaMultiplayerLobbyScreen` now delegates start validation, generated-game creation, and live-session attach through that helper instead of duplicating the handoff logic inside the themed UI. The new loopback proof sends the join lobby selection over `TcpLockstepTransport`, applies it to the host lobby, sends the start payload back, validates settings/build/protocol, creates matching generated universes on both sides, attaches live host/client sessions, and proves pause/speed propagation. Verification: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused lobby-flow/lobby/Arena-lobby tests passed 4/4; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 70/70 and 70/70; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. The visible lobby is still two-player-shaped; broader roster/settings UI can now build on this non-Arena helper.
- 2026-06-26 16:21 +03:00 - Sustained 4X MP chunk 62: hardened authoritative ship-to-planet colonize handling for the playable early-game path. `Authoritative4XClientContext` now mirrors the host's order-specific legality checks for passive-client planet orders, including colony ship vs habitable/unowned target, bombard legality, troop landing legality, and the supported move-order flag set; invalid passive-client colonize attempts are blocked locally instead of sending junk commands or falling through to local mutation. Added a colony-ship fixture to the deterministic `BuildWorld` test harness and proved a real `ShipPlanetOrder(Colonize)` creates the stock `MarkForColonization(ship, planet, empire)` goal on the authority, replicates the assigned colony ship/manual flag/AI state to the client, changes the sync digest, and rejects non-colony or owned-target colonize requests as no-ops. The passive-client proof asserts a colony right-click submits exactly one colonize command without locally changing AI state/order queues/goals, while non-colony, owned-target, and unsupported waypoint colonize requests are blocked. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused colonize/codec tests passed 3/3; focused `FullyQualifiedName~Authoritative` passed twice, 72/72 and 72/72; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97 after rerunning sequentially to avoid the shared `blackbox.log` test-harness lock; `git diff --check` clean apart from line-ending normalization warnings. Scout results point next at decoded 4X MP command telemetry and remaining direct local UI mutation slices in empire automation plus selected-ship utility orders.
- 2026-06-26 16:36 +03:00 - Sustained 4X MP chunk 63: improved authoritative 4X live telemetry so future desync attempts carry decoded command evidence. `Authoritative4XLiveTelemetry.Command` now records a deterministic `textHash`, `textChars`, bounded raw text preview, and a compact decoded `summary` for command families including movement, colony type, research queue operations, diplomacy, ship design/build queues, attack/planet orders, labor/budget/logistics sliders, fleet/patrol/layout commands, deep-space build/cancel, governor flags, blueprints, and tile scrap. Large payloads such as serialized ship designs no longer dump unbounded raw text into the live log; they get a stable hash, char count, and bounded preview. Added a direct telemetry proof that writes empire-budget, ship-planet-order, manual-trade-slot, blueprint, and long design payload commands and asserts decoded summaries plus hash/char evidence, and extended the live host/client telemetry proof to assert decoded `SetColonyType` summaries appear on real session logs. Verification: focused telemetry tests passed 3/3; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative` passed twice, 73/73 and 73/73; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Next: use the richer logs on the next live mismatch, and continue routing direct local UI mutations in empire automation or selected-ship utility orders.
- 2026-06-26 17:00 +03:00 - Sustained 4X MP chunk 64: routed empire automation settings through authoritative 4X. Added `SetEmpireAutomation` with a bounded encoded payload for automation flags plus the selected freighter/colony/scout/constructor/research-station/mining-station designs; the host validates legal flag bits, design-name payloads, buildable role-appropriate designs, and then applies the real empire automation flags and default-design fields, including `SwitchRushAllConstruction`. `AutomationWindow` now binds empire toggles to `Screen.Player` instead of `UState.Player`, so joined clients edit their assigned human empire, and in authoritative MP it snapshots/restores local automation/global preference state before submitting the host request; single-player keeps the existing direct behavior. The canonical sync payload now hashes automation flags and selected automation designs, and live telemetry decodes the new command payload for future desync diagnosis. Verification: `dotnet build StarDrive.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; focused automation/codec/telemetry tests passed 4/4; focused `FullyQualifiedName~Authoritative4X` passed twice, 75/75 and 75/75; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Live QA should verify the automation window submits and refreshes clearly in a real host/join 4X session; scouts identify selected-ship utility orders and a first-class authoritative 4X lobby entry as the next high-value slices.
- 2026-06-26 17:20 +03:00 - Sustained 4X MP chunk 65: added a first-class authoritative 4X multiplayer main-menu entry without hard-coupling the base engine to the Arena plugin. The plugin loader now supports optional titled menu actions; existing data-defined buttons are reused, and missing titled plugin actions contribute a normal menu button, so active mod layouts such as Combined Arms do not need ignored YAML edits just to expose the entry. `ArenaPlugin` registers both `arena` (`Star Gladiator`) and `auth_4x_multiplayer` (`4X Multiplayer`); the new action opens the shared lobby in `Authoritative4X` mode, while the old Star Gladiator surface remains the default. Stock Jupiter/Mars/Venus layouts also include the 4X button for explicit placement. Proofs assert plugin title metadata, legacy titleless action compatibility, drop-in Arena DLL registration of both actions, missing-button contribution, both lobby surface modes, and tracked menu-layout button presence. Verification: `dotnet build StarDrive.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; focused plugin/lobby tests passed 4/4; focused `FullyQualifiedName~Authoritative4X` passed twice, 75/75 and 75/75; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Live QA should verify the contributed CA-layout button appears in the expected main-menu position and that host/join/ready/launch remain clear.
- 2026-06-26 17:49 +03:00 - Sustained 4X MP chunk 66: routed the selected-ship resupply utility order through authoritative 4X. `ShipSpecialOrder` now has a `Resupply` variant; the host validates active owned ship state and applies the real `ship.Supply.ResupplyFromButton()` path, while passive clients submit the command from both the selected-ship order button and the single-ship info-panel resupply toggle without locally enqueueing resupply before host acceptance. Existing authoritative ship digest rows already cover the resulting `AI.State` and order queue, and the proof now asserts the accepted resupply order changes the sync digest and replays identically on the client replica. Verification: `dotnet build StarDrive.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; focused `FullyQualifiedName~ShipSpecialOrder` passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed twice, 75/75 and 75/75; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Next utility-order gaps are trade toggles, carrier launch/recall toggles, and explicit refit routing.
- 2026-06-26 18:12 +03:00 - Sustained 4X MP chunk 67: routed selected-freighter trade-policy toggles through authoritative 4X. Added `SetShipTradePolicy` with `Food`, `Production`, `Colonists`, and `InterEmpire` variants; the host validates active owned freighters and applies the real ship trade flags, while passive clients submit requests from both multi-select order buttons and the single-ship info panel instead of mutating local replica state. The authoritative ship digest now includes freighter/trade flags, and live telemetry decodes the trade-policy payload for future mismatch logs. Verification: `dotnet build StarDrive.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-restore` passed; focused trade-policy/codec/telemetry tests passed 4/4; focused `FullyQualifiedName~Authoritative4X` passed twice, 77/77 and 77/77; exact rerun of transient `ArenaCareerLadderRound_Headless` passed after one isolated full-filter NRE; full `dotnet test UnitTests/SDUnitTests.csproj -p:Platform=x64 -p:Configuration=Debug --no-build --filter "FullyQualifiedName~Arena"` then passed twice, 97/97 and 97/97. Remaining selected-ship utility gaps are carrier launch/recall/troop toggles, AO/trade-route definition, and explicit refit routing.
- 2026-06-26 18:45 +03:00 - Sustained 4X MP chunk 68: routed selected carrier/troop utility toggles through authoritative 4X. Added `SetShipCarrierPolicy` with fighter launch/recover, assault-shuttle launch/recover, recall-before-FTL, send-troops-to-ship, and allow-board-ship variants; the host validates active owned ships plus the relevant fighter/troop bay capability before applying the real carrier setters. Passive clients now submit these requests from both multi-select order buttons and the single-ship info panel instead of mutating local carrier state, while single-player keeps the existing direct fallback. The authoritative ship digest now hashes fighter/troop bay presence, launch toggles, recall policy, send/board toggles, and manual hangar override; live telemetry decodes the new carrier-policy payload for mismatch logs. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused carrier-policy/codec/telemetry/UI-helper tests passed 5/5; focused `FullyQualifiedName~Authoritative4X` passed twice, 80/80 and 80/80; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Remaining selected-ship utility gaps are AO/trade-route definition and explicit refit routing.
- 2026-06-26 19:10 +03:00 - Sustained 4X MP chunk 69: routed freighter trade-route and area-of-operation definition through authoritative 4X. Added `SetShipTradeRoute` and `SetShipAreaOfOperation`; the host validates active owned freighters, real planet ids, the same trade-route eligibility as `Ship.AddTradeRoute`, and the existing 5000x5000 AO minimum before applying the stock route/AO mutations. Passive clients now submit route add/remove and AO add/remove-by-point requests from `UniverseScreen` input instead of mutating local freighter state, while single-player keeps the existing fallback; the input path also now guards stale define-mode clicks with no selected ship. The authoritative ship digest hashes sorted trade-route ids and sorted AO rectangles, and live telemetry decodes route/AO payloads for future mismatch logs. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed after a serial retry cleared a parallel SourceLink file lock; focused route/AO/codec/telemetry tests passed 4/4; focused `FullyQualifiedName~Authoritative4X` passed twice, 82/82 and 82/82; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining selected-ship utility gap is explicit refit routing, with ship rename/mass-scrap as lower-priority direct-mutation cleanup.
- 2026-06-26 19:58 +03:00 - Sustained 4X MP chunk 70: routed explicit ship refit through authoritative 4X. Added `RefitShip` with `One`, `All`, and `Fleet` modes; the host validates active owned refittable ships, legal buildable same-hull targets, scuttle/scrap conflicts, fleet membership, and an available refit yard before applying the stock `RefitShip` / `RefitOrbital` goal path. Passive clients now submit the refit-window buttons instead of locally adding AI goals, while single-player keeps the original direct behavior. The authoritative snapshot now emits `G|Refit` rows for mobile refit goals before any queue item exists, and live telemetry decodes refit payloads. The host apply path queues the selected source ship first, then de-duplicates matching active empire ships and owned-cache ships so accepted bulk refit cannot miss the clicked ship. Parallel scouts also mapped the next command gaps: fleet design/requisition, ship-design research routing, capital rebuild, and a 4X lobby self-test/diagnostic pass. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed after serial rerun cleared a parallel runtimeconfig file lock; focused refit/codec/telemetry tests passed 5/5; focused `FullyQualifiedName~Authoritative4X` passed twice, 85/85 and 85/85; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should verify refit-window button feedback under host/join play because UI still waits for the accepted host snapshot.
- 2026-06-26 20:14 +03:00 - Sustained 4X MP chunk 71: routed the ship-design "Research This Ship" missing-tech flow through authoritative 4X. `ShipDesignLoadScreen` now filters already-queued techs, submits each remaining missing tech through the existing `QueueResearch` command when authoritative MP is active, and avoids local research-queue mutation on passive clients; single-player keeps the direct `EmpireResearch.AddTechToQueue` behavior. The extracted `QueueShipDesignMissingResearch` helper makes the UI path headlessly testable, and the new proof asserts duplicate queued techs are skipped, the missing tech emits one command, wrong-empire submission is blocked, and the local queue stays unchanged until host acceptance. Verification: focused ship-design research/research-queue tests passed 2/2; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 86/86 and 86/86; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining scout-prioritized gaps are capital rebuild, fleet-design load/requisition, and replacing the 4X lobby self-test with an authoritative loopback proof.
- 2026-06-26 20:43 +03:00 - Sustained 4X MP chunk 72: replaced the visible 4X lobby self-test with a real authoritative 4X loopback proof. In `Authoritative4X` mode, `SELF TEST` now builds a deterministic two-player lobby, exchanges lobby/start payloads over TCP loopback, validates the start payload, creates matching generated games, attaches live host/client authoritative sessions, submits one client UI colony command, and asserts accepted synced snapshots. The visible 4X start path now builds its start message through `Authoritative4XLobbyNetworkFlow` instead of hand-assembling the payload, and failed 4X starts report seed/settings/race/turn diagnostics to both lobby status and the remote peer. Verification: focused lobby/self-test tests passed 5/5; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 87/87 and 87/87; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Live QA should open the 4X multiplayer lobby and press `SELF TEST`, expecting `4X LOOPBACK OK`, then run a real host/join start and issue one visible colony UI command from the joined client.
- 2026-06-26 21:01 +03:00 - Sustained 4X MP chunk 73: routed colony capital/outpost rebuild through authoritative 4X. Added `BuildCapitalHere`; the host validates the target planet exists and is owned by the issuing empire before invoking the real `Planet.BuildCapitalHere()` path, while passive clients submit the governor button request without locally marking the homeworld or enqueueing the capital rebuild. Live telemetry now decodes the command, and the transport codec proof covers the new command id. Verification: focused build-capital/codec/client-context tests passed 3/3; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 89/89 and 89/89; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean. Remaining queued worker patches cover fleet requisition, ship-to-ship target orders, and saved fleet-design load.
- 2026-06-26 21:28 +03:00 - Sustained 4X MP chunk 74: routed fleet requisition through authoritative 4X. `RequisitionScreen` now submits `Assign Now` through the existing authoritative fleet-layout command and routes `Build Now` / `Rush Now` through new `QueueFleetRequisition`; passive clients do not locally mutate fleet membership or create requisition goals before host acceptance. The host validates owned fleet keys, unique node payloads, empty target nodes, existing mobile buildable ship designs, and the rush flag before creating the real `FleetRequisition` goals. The canonical snapshot now emits `G|FleetRequisition` rows and live telemetry decodes the requisition payload. Verification: focused fleet requisition/fleet/codec tests passed 12/12; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 92/92 and 92/92; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean apart from line-ending normalization warnings. Remaining queued worker patches cover ship-to-ship target orders and saved fleet-design load.
- 2026-06-26 21:52 +03:00 - Sustained 4X MP chunk 75: routed ship-to-ship target orders through authoritative 4X. Added `ShipTargetOrder` for attack, escort, friendly troop transfer, and boarding; the host validates active owned ships, active targets, target loyalty, attackability, troop presence, target troop capacity, and queueing only for attack before applying the real `ShipMoveCommands` engine calls (`AddEscortGoal`, `OrderTroopToShip`, `OrderTroopToBoardShip`, `OrderQueueSpecificTarget`, `OrderAttackSpecificTarget`). Passive clients now submit right-click ship target orders instead of locally mutating AI state, while single-player keeps direct behavior. The canonical ship snapshot now includes escort target id, and live telemetry decodes the new target-order payload. Verification: focused ship-target/codec/telemetry tests passed 4/4; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 94/94 and 94/94; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97; `git diff --check` clean. Remaining queued worker patch covers saved fleet-design load; carrier assault-bay boarding beyond the existing single-troop right-click path remains out of scope.
- 2026-06-26 22:07 +03:00 - Sustained 4X MP chunk 76: routed saved fleet-design load through authoritative 4X. `FleetDesignScreen.LoadFleetDesign` now submits the loaded layout, fleet rename, and fleet icon change through authoritative client context when MP is active; passive clients no longer clear/rebuild fleet nodes, rename fleets, or change icons locally before host acceptance. Added `SetFleetIcon` as command id 56, host-side icon validation, and telemetry decoding; the saved-load helper composes existing `SetFleetLayout` and `RenameFleet` with the new icon command so the command stream stays explicit. Verification: focused saved fleet-design load tests passed 2/2; neighboring fleet layout/codec tests passed 3/3; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 96/96 and 96/96; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Remaining audit-prioritized gaps include ground troop launch/land/combat, ship/planet rename commands, and richer live desync telemetry artifacts.
- 2026-06-26 22:31 +03:00 - Sustained 4X MP chunk 77: closed more passive-client local mutation paths. Added authoritative `RenameShip` and `RenamePlanet` commands (ids 57/58) with host validation, client-context helpers, live telemetry summaries, and UI routing from selected-ship info, ship-list rows, and colony planet-name entry; cosmetic names now replay through the host without entering the canonical simulation digest, avoiding generated-lobby false desyncs. Routed planet-info/list troop-send and combat-screen bombard toggles through existing authoritative `ShipPlanetOrder`, and aligned `LandTroops` validation with the real engine path so single-troop shuttles and friendly reinforcement landings are legal. Verification: focused rename/troop/bombard tests passed 4/4; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 99/99 and 99/99; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Remaining high-risk gap is full ground-combat action authority (troop launch/tile move/attack), plus richer live desync telemetry artifacts.
- 2026-06-26 22:53 +03:00 - Sustained 4X MP chunk 78: added live authoritative 4X multiplayer observability for cross-machine debugging. Lobby/start and live logs now share deterministic `sessionId`/`startFingerprint` values derived from the start payload, and the live `SESSION` line records start settings, build/protocol summary, local peer/empire, peer-to-empire map, and human empire ids. Sync mismatches now include session fingerprint fields and write a `*-sync-mismatch-recent-events.log` artifact with the retained recent command/result/network events leading up to the mismatch, alongside the existing authority/client payload artifacts. Verification: focused telemetry/lobby tests passed 4/4 from the main checkout; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 99/99 and 99/99; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --settings unittests.runsettings --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Note: the telemetry worker worktree could not run MSTest due missing native DLL in that worktree, but the same patch passed in the main checkout. Ground-troop authority remains unintegrated in `C:\dev\stardrive\StarDrive-arena-045-groundtroops` because it is unbuilt/untested and needs focused proofs before landing.
- 2026-06-26 23:22 +03:00 - Sustained 4X MP chunk 79: routed ground troop launch, recall, tile movement, and ground-combat attack clicks through authoritative 4X. Added `GroundTroopOrder` handling for launch-one/all, recall-all, move, troop-vs-troop attack, troop-vs-building attack, and building-vs-troop attack; the host validates planet/tile/troop ownership, range, action availability, enemy/war state, and current target legality before applying the real `Troop.Launch`, `TroopManager.MoveTowardsTarget`, and `CombatScreen.StartCombat` paths. Colony, governor, planet-list recall, and combat-screen UI now submit these orders on passive clients without local mutation, while single-player keeps the old direct behavior. The canonical authoritative snapshot now hashes grounded troops (`GT`), ship-carried troops (`ST`), and active ground combats (`GC`) so replica sync covers troop scars/actions and started combats. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused ground-troop tests passed 3/3; focused `FullyQualifiedName~Authoritative4X` passed twice, 102/102 and 102/102; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 97/97 and 97/97. Live QA should verify ground troop launch/recall/move/attack clicks wait for accepted host snapshots and that cross-machine logs include `GT/ST/GC` row counts after ground-combat activity.
- 2026-06-26 23:49 +03:00 - Sustained 4X MP chunk 80: fixed the live multiplayer lobby crash when `JOIN` targets an unreachable host. The lobby no longer calls `TcpClient.Connect` synchronously from the main update thread; join attempts now run in a background task, marshal success/failure back to the screen, and convert socket failures into visible status text instead of escaping through `UpdateGame`. Host setup also catches listener/port failures and reports them in-lobby. Added a headless refused-port proof so failed joins return an error without throwing. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused join-failure plus `FullyQualifiedName~Authoritative4X` passed twice, 103/103 and 103/103; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 98/98 and 98/98. Live QA should retry joining a stopped/unreachable host and expect a non-crashing `FAILED:` status, then retry against the actual host.
- 2026-06-27 00:09 +03:00 - Sustained 4X MP chunk 81: made the visible multiplayer lobby trait picker budgeted like single-player instead of single-choice. The lobby now keeps `TraitOptions` as a selected set, uses one control to cycle the highlighted racial trait and a second `ADD/DROP/FULL` control to mutate the set, displays selected traits with spent/budget in the peer cards, and preserves the multi-trait string through the authoritative start payload. The selector accepts compatible traits up to the normal racial trait point budget, respects exclusion pairs, and rejects illegal adds without changing the current set. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused lobby trait/entry tests passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed twice, 103/103 and 103/103; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 99/99 and 99/99. Live QA should verify the lobby text remains legible with longer trait names and that host/join both see the chosen multi-trait set before launch.
- 2026-06-27 00:30 +03:00 - Sustained 4X MP chunk 82: fixed the live authoritative 4X launch crash caused by raw-hash-only drift. The reported failure had matching canonical authoritative digests (`0xA85300E80751AC06`) but differing raw `UniverseStateHash` values, so the client was treating transient/local raw hash variance as a fatal desync. The client replica now uses the canonical payload digest as the authoritative sync contract, still throws on real canonical digest divergence, and records raw-hash-only variance as `RAW_HASH_DRIFT` telemetry with both raw hashes and the shared digest. Added a network-client regression that injects a poisoned raw hash with an unchanged canonical payload and proves the client continues while exposing the drift diagnostic; the existing mismatch telemetry proof still passes for real canonical divergence. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed after a serial rerun cleared a parallel generated-resource file lock; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; targeted raw-drift and sync-mismatch telemetry tests passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed twice, 104/104 and 104/104; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 99/99 and 99/99. Live QA should retry host/join launch and, if raw hashes differ again with matching digests, expect the client log to continue with a `RAW_HASH_DRIFT` row instead of crashing.
- 2026-06-27 01:00 +03:00 - Sustained 4X MP chunk 83: hardened canonical sync-mismatch diagnostics after the live client reported a real digest divergence at heartbeat tick 31. Added the first differing canonical payload row directly to `Authoritative4XSyncMismatchException`, so the next live crash paste identifies the exact divergent state instead of only raw/digest hashes. Added an exact generated-start unpaused heartbeat proof using the same seed/race shape as the live report (`24237`, Cordrazine vs Dauntless when present), which stays canonically synced for 40 host heartbeats under the authoritative session pump; this narrows the live-only divergence to screen/client environment side effects or cross-machine state, not the shared generated lobby/session heartbeat code. Existing deliberate mismatch telemetry now asserts the crash text includes `firstDiff`. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed after a serial rerun cleared an obj-file lock from parallel builds; targeted heartbeat + mismatch telemetry proofs passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed twice, 105/105 and 105/105; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 99/99 and 99/99. Live QA should retry with this bundle and paste the new `firstDiff line=...` text or the client `SYNC_MISMATCH` artifact; that row is the next fix target.
- 2026-06-27 01:27 +03:00 - Sustained 4X MP chunk 84: fixed the live turn-31 canonical desync identified by the new first-diff row. The pasted row showed only `S|1488|...` ship `Position.X` differed (`1234658415` vs `1234658390`, decoded as a 3.125 world-unit movement drift) while ownership, orders, target queues, combat state, and carrier/trade flags matched. The authoritative canonical digest now quantizes volatile ship movement fields (`Position` and AI `MovePosition`) into 32-world-unit buckets, keeping tiny cross-machine FP integration drift from crashing the passive replica while preserving exact checks for gameplay state and still detecting material position divergence. Added `Authoritative4XSnapshot_ToleratesSubQuantumShipMovementDrift_Headless`, which replays the generated lobby start at seed `24237`, advances 31 fixed ticks, applies the exact live drift bits, proves canonical digests match, then moves the ship 128 units and proves the digest changes. Verification: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; targeted movement-drift + generated-heartbeat proofs passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed twice, 106/106 and 106/106; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Live QA should retry host/join with the refreshed DLL bundle; if another canonical mismatch appears, the next `firstDiff` row will identify the next volatile or real state field.
- 2026-06-27 01:43 +03:00 - Sustained 4X MP chunk 85: corrected the chunk-84 movement tolerance after live QA hit the expected bucket-boundary failure at tick 32 (`S|1488|...` physical position bucket `38757` vs `38756`). A wider quantization bucket would still have boundary cases, so the canonical digest now treats live-integrated ship `Position.X/Y` as volatile raw-hash telemetry and excludes those physical coordinates from the gameplay sync contract. AI `MovePosition`, orders, targets, combat state, ownership, queues, carrier/trade flags, and other command/state fields remain canonical and exact. Updated the regression to `Authoritative4XSnapshot_IgnoresVolatileShipPositionButTracksMoveTarget_Headless`: generated lobby seed `24237`, 31 fixed ticks, exact pasted drift bits plus a 128-unit physical divergence do not change the canonical digest, but a 128-unit AI move-target change still changes it. Verification: targeted movement-contract and generated-heartbeat proofs passed 2/2; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused `FullyQualifiedName~Authoritative4X` passed twice, 106/106 and 106/106; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. The build was briefly blocked by a running `StarDrive BlackBox` process holding `game/SDUtils.dll`; that stale process was stopped before verification. Live QA should retry with the refreshed DLL bundle; any further canonical mismatch now points to a non-position field in `firstDiff`.
- 2026-06-27 02:16 +03:00 - Sustained 4X MP chunk 86: fixed the live turn-1359 authoritative desync and joined-human AI-control leak. The pasted firstDiff showed ship 1159 diverging only in transient movement-solver order-queue stages (MoveToWithin1000 present on the client while the authority had already advanced to MakeFinalApproach) with the same move target and durable follow-up orders, so canonical ship order signatures now exclude volatile movement micro-plans while retaining durable orders such as hold, orbit, colonize, targets, and AI move targets. Generated authoritative lobby starts now configure every human-controlled empire on both authority and replica universes: sidekicks and all automation flags are off, stale auto-design selections are cleared, rush-all construction is cleared, and pre-picked AI research queues are reset. Remote human empires are also guarded in EmpireAI so stock strategic, research, military, and war planners do not run for authoritative-human empires even though only the host empire is isPlayer. Proof: targeted volatile order/signature + generated-human handoff tests passed 2/2; dotnet build StarDrive.csproj -p:Configuration=Debug -p:Platform=x64 --no-restore passed; dotnet build UnitTests/SDUnitTests.csproj -p:Configuration=Debug -p:Platform=x64 --no-restore passed; focused FullyQualifiedName~Authoritative4X passed twice, 107/107 and 107/107; full FullyQualifiedName~Arena passed twice, 99/99 and 99/99; refreshed dist/arena-pc-dlls.zip with manifest hashes for the five deploy DLLs. Live QA should redeploy the refreshed DLL bundle to both machines, retry host/join, and verify the joined player can select/control their own empire without AI automation or turn-1359-style order-queue desyncs.
- 2026-06-27 03:13 +03:00 - Sustained 4X MP chunk 87: added a deployable authoritative 4X probe and fixed a cross-machine generated-start mismatch caused by machine-local saved player ship designs. The new `x64Migration/Tools/Authoritative4XProbe` console runner exercises the real authoritative lobby/start/session over TCP, emits PC/laptop artifacts, and can run on the laptop via SSH-launched interactive scheduled task. The first PC-host/laptop-join run exposed a tick-1 canonical payload diff: the PC authority had a local saved `Combatl Freighter` player design in human empire buildables while the laptop did not. Generated MP human empire setup now strips `IShipDesign.IsPlayerDesign` buildables so local AppData saved designs cannot enter authoritative starts; MP designs must enter through the explicit `DesignShip` command path. Proof: new local-design scrub tests passed 2/2; local probe self-test passed 40 turns; local two-process TCP probe passed 300 turns; PC host + laptop join passed 1600 turns with matching final digest `0x0522056EE80B3832:0x3CD2B7F00DEB3FE6/0x81AD8E40954BB4E5`; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed 108/108; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Laptop bundle refreshed at `D:\authoritative4x-probe`; local package refreshed at `dist/authoritative4x-probe.zip`. Live QA can now use the probe for repeatable PC/laptop sync checks while the real UI path continues to receive command-routing fixes.
- 2026-06-27 03:35 +03:00 - Sustained 4X MP chunk 88: strengthened the deployable authoritative 4X probe from heartbeat-only proof into a two-player command-injection proof. `Authoritative4XNetworkClient` now exposes a processed-command drain so test/probe code can assert every accepted command by origin peer and sequence even when TCP batches host and client results in one poll. The probe now injects real commands from both players: host and join each set a planet manual civilian budget and empire tax/treasury budget, then both the authority and passive replica assert those values stick after later heartbeat ticks. The first attempt intentionally found that `SetColonyType` is not a stable homeworld assertion because the engine normalizes capitals back to `Core`; the final script uses canonical colony budget and empire budget fields instead. Proof: local TCP host/join command probe passed 120 turns with final digest `0x354CEBF624A53FDF:0x6CE492E5B2C9A97F/0xE5627AF987018091`; PC host + laptop join command probe passed 300 turns with final digest `0x67FD63356097D675:0x00AB75B5B2F36948/0xF77D9EA34B9F77B9`, including `assert host-authority` and `assert join-replica` lines for host planet 10 civilian 12.5, host budget 0.17/0.41, join planet 384 civilian 17.25, join budget 0.23/0.37; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed 108/108; full `FullyQualifiedName~Arena` passed 99/99. Laptop bundle refreshed at `D:\authoritative4x-probe`; local package refreshed at `dist/authoritative4x-probe.zip`.
- 2026-06-27 03:58 +03:00 - Sustained 4X MP chunk 89: added authoritative multiplayer session save/load metadata plus an 8-player in-process scale proof. `SavedGame` now has an explicit `SaveTo(FileInfo)` path for temp/test-safe saves, and `Authoritative4XSessionSave` writes the normal binary universe save plus a `.auth4x.yaml` sidecar carrying session id, start fingerprint, generation seed, human empire ids, and peer-to-empire mappings. Host live sessions expose `TrySaveSession`, and loaded sessions reapply human-control registration plus automation suppression without wiping loaded research/design state. The sidecar encodes `0x...` hash strings so the YAML parser cannot reinterpret them as numeric scalars. The new save/load proof creates a 3-human session, processes a command, saves to a temp root, reloads metadata+universe, restores human ownership, and processes another post-load command on the host. The new 8-player proof creates one authority plus eight in-process replicas from a tiny generated lobby, submits a real empire-budget command from every peer, and asserts every command sticks on the authority and all replicas stay canonically synced. Boundary: passive client resume from an arbitrary binary save still needs the future full snapshot/delta state-sync layer, because repeated same-process binary loads can diverge in volatile movement-solver scratch state. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused save/load + 8-player tests passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 110/110; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99.
- 2026-06-27 04:32 +03:00 - Sustained 4X MP chunk 90: added the host-owned save-transfer foundation for authoritative multiplayer resume/resync. `SDLockstep` now has chunked authoritative save-transfer messages plus a resync-request envelope, all covered by the transport codec. `Authoritative4XSessionSave` can serialize/deserialize its `.auth4x.yaml` metadata as text, and the new `Authoritative4XSaveTransfer` packages a real host `.sav` with metadata into bounded 256 KiB chunks with SHA-256 verification before the client writes it into a temp cache. The TCP host can send a save package to a peer; passive clients receive/verify/cache transferred saves; live sessions expose host send and client drain APIs. Sync mismatch handling now sends a resync request to the host with the client tick/digest/first-diff reason before the mismatch is reported, giving the host enough context to send an authoritative recovery package. Boundary: this chunk does not yet hot-swap the visible client `UniverseScreen` to the received save; it lands the transport, verification, cache, and request handshake that the next UI/runtime recovery pass can consume. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed after a serial retry cleared a parallel `SDUtils` obj-file lock; focused transfer/save/resync tests passed 5/5; focused `FullyQualifiedName~Authoritative4X` passed 111/111; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed local laptop deploy bundle at `dist/arena-pc-dlls.zip`.
- 2026-06-27 04:49 +03:00 - Sustained 4X MP chunk 91: fixed research menu pause behavior for authoritative multiplayer. The research command path was already authoritative and tested, but `ResearchScreenNew` still used the normal single-player `toPause: UniverseScreen` constructor path, which could locally pause one peer while the MP session should keep simulating unless the host broadcasts a pause. Research now keeps the single-player pause target when no authoritative 4X context is active and uses no pause target while an authoritative MP client/host context is active. The existing research queue/no-local-mutation test now also asserts that pause-target split, while diplomacy popup/proposal screens remain non-pausing. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed after a serial retry cleared a transient `SDUtils` obj-file lock; targeted research/diplomacy tests passed 6/6; focused `FullyQualifiedName~Authoritative4X` passed 111/111; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed local laptop deploy bundle at `dist/arena-pc-dlls.zip`.
- 2026-06-27 05:24 +03:00 - Sustained 4X MP chunk 92: fixed the remaining named management/menu pause surfaces for authoritative multiplayer. `ShipDesignScreen`, `FleetDesignScreen`, `EmpireManagementScreen`, `GamePlayMenuScreen`, generic load/save popups, and the base universe popup window now keep their existing single-player pause target outside MP but use no local pause target while an authoritative 4X session/context is active. The in-game menu now blocks unsafe stock local Load during MP, lets only the host Save, and routes menu Save plus quicksave/save-shortcut through the authoritative session-save path instead of opening the single-player save UI. Added a focused proof that every named screen preserves the single-player pause target when MP is inactive and switches to non-pausing under an authoritative context. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; targeted pause/research/live-control tests passed 3/3; focused `FullyQualifiedName~Authoritative4X` passed 112/112; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed local laptop deploy bundle at `dist/arena-pc-dlls.zip` (SHA-256 `159E8E858F14AAFCAE2EFD7FDE0B2F8BB717EC820F9F3D1590497AB9041CAEF7`). Follow-up: older legacy universe screens such as budget, ship list, planet list, espionage, patrols, blueprints, infiltration, and message boxes still have explicit pause targets and should get the same authoritative-MP split if live QA hits them next.
- 2026-06-27 06:07 +03:00 - Sustained 4X MP chunk 93: fixed the live tick-1575 authoritative mismatch and joined-client planet control blocker, and added zoom-out performance telemetry for the next laptop run. The pasted firstDiff decoded to only top-level `Ship.AI.MovePosition.X/Y` differing by two float bits while physical position was already raw telemetry and the durable queued order signature matched, so canonical ship rows now treat the current movement-solver scratch target as volatile while still hashing queued/durable movement orders exactly. `PlanetScreen` now binds its `Player` to the visible `UniverseScreen.Player` when present, so joined clients opening colony/planet UI validate ownership against their assigned human empire instead of `UState.Player` (the host empire), unblocking construction submission from the laptop. Authoritative live sessions now emit throttled `VIEW_PERF` telemetry at sector/galaxy zoom with draw/render/overlay/icon/fog timings plus visible-object counts, so the next laptop FPS tank can be diagnosed from the MP log. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; targeted movement-scratch and joined-client planet-screen proofs passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 112/112; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed clean laptop deploy bundle at `dist/arena-pc-dlls.zip` (SHA-256 `FBDCD9DE835CA82DEA280625608410B78A512559FDCE6E97B22BEE33052E8217`).
- 2026-06-27 06:49 +03:00 - Sustained 4X MP chunk 94: added repeatable authoritative 4X cross-machine QA automation so live PC/laptop failures can be found without manual screenshot/paste loops. `RunAuthoritative4XCrossMachineQA.ps1` builds/publishes the authoritative probe, refreshes the five-DLL deploy bundle, optionally deploys the same DLLs/probe to a laptop over SSH/SCP, launches host plus join scripted sessions, collects remote artifacts, and writes per-run/top-level summaries that classify connection failures, environment mismatches, command rejections, canonical sync mismatches, raw-hash drift, and `VIEW_PERF` zoom telemetry. A new `Authoritative4XQaSummarizer` provides the same classification in C# with focused headless tests for sync/perf evidence and connection/pass evidence. Local loopback proof ran the new script for a 2-turn host/join command-injection session and produced a clean summary (`FailureKind=None`, final `0xD4C83B72B8BADD9D:0xB2E3BF3920EF3186/0xE5627AF987018091`). Proof: PowerShell parser check passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `Authoritative4XQaSummarizer` passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 114/114; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `DFE1CBA62A624CC01CFADB5DE5592773198FFCD817799974A4AC34A4852E5C03`) and probe package `dist/authoritative4x-probe.zip` (SHA-256 `9517F6795E35A37407AB09B96A131E6E70B0D4D60C284A4113D8C262C911F370`).
- 2026-06-27 06:21 +03:00 - Sustained 4X MP chunk 95: completed the authoritative resync recovery path. Passive clients now stop applying normal traffic after a canonical mismatch, keep accepting save-transfer chunks, log `SYNC_MISMATCH` before requesting resync, load the received host save, reattach a fresh live client session to the same TCP transport, preserve the local peer/empire assignment, and resume UI command sequence numbers after the failed command. The host now answers resync requests by sending an authoritative save package and then adopting that same save/load-normalized universe image itself, preventing tiny save/load economic normalization from causing an immediate post-resync digest drift. `UniverseScreen.UpdateAuthoritative4XMultiplayer` hot-swaps recovered host/client universes through `GoToScreen` once the recovery package is ready. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed 0 warnings/0 errors; focused resync/save tests passed 4/4; full `dotnet test UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-build --filter "FullyQualifiedName~Arena"` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `CCAC1177E8DD587164126D5C1ADB2046C4049289FA548C18B8563624B891EA72`). Next: live QA should force or wait for a real mismatch and verify the client returns to the game instead of crashing; if multiple clients are present, non-requesting clients may need their own follow-up resync when the host adopts the save-normalized image.
- 2026-06-27 06:46 +03:00 - Sustained 4X MP chunk 96: hardened authoritative resync for multi-client sessions. The resync handshake now has an explicit epoch: the host broadcasts `AuthoritativeResyncBegin` to every remote peer, freezes host-local UI commands plus heartbeat ticks while the epoch is open, sends the same host save image to every peer, and waits for `AuthoritativeResyncAck` from all peers before adopting its own save/load-normalized image and resuming traffic. Passive clients enter waiting mode on the begin message, keep accepting save chunks, ACK after loading the host save and rebuilding the live session, and ignore normal command/result/snapshot/control traffic while waiting. Added a UI-command-context reactivation hook so in-process host/client recovery tests can focus the recovered client after the host reloads; in real two-machine play those contexts are already process-local. Proof: codec + two-peer live recovery + new eight-player TCP resync barrier passed 3/3; focused `FullyQualifiedName~Authoritative4X` passed 116/116; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `2516C5D2F30D99E5F6661682D746BE9E50823F0B897D6AF4EB829A220E79311B`). Next: live QA should confirm a real mismatch now recovers all connected peers instead of letting non-requesting clients continue on the old image.
- 2026-06-27 07:01 +03:00 - Sustained 4X MP chunk 97: fixed the live laptop post-resync local-view/fog regression. A passive client loading a host-authored save necessarily receives host-side `UState.Player` and host `FogMapBytes`; after a resync this could repaint the laptop with the host's fog/exploration mask even though the authoritative session still mapped the laptop peer to its own empire. `UniverseScreen.AttachAuthoritative4XMultiplayer` now rebinds the screen-local authoritative player idempotently, keeps `EmpireUI.Player` pointed at that local empire after screen reloads, clears imported host fog bytes/render targets for remote clients so fog rebuilds from the laptop empire's sensors, and refreshes local visibility. `UniverseObjectManager.UpdateAllShips` now also uses the screen-local `Universe.Player` viewer instead of serialized `UState.Player` when marking ships seen for UI visibility. Proof: targeted recovered-client + assigned-empire local-view tests passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 116/116; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `5164715266927B30B95E078BFA8BD6CF0E198616ACD827A4B2C3446EB765D31C`). Next: redeploy the refreshed DLL bundle and confirm the laptop keeps its own fog/control identity after any resync; co-op same-empire control is now a plausible explicit mode rather than an accidental host-view leak.
- 2026-06-27 08:00 +03:00 - Sustained 4X MP chunk 98: diagnosed the latest live PC/laptop run from game/sim-output and fixed the two logged identity/visibility leaks. The host log showed a resync at tick 2763 with firstDiff R|1|2 Known after stock first-contact diplomacy, and a later resync at tick 1463 immediately after a ShipPlanetOrder to planet 20; that matches the reported AI diplomacy flash + pause/resync. Stock DiplomacyScreen entry points now refuse human-vs-human pairs globally, so first contact/warnings/queued stock diplomacy cannot open AI diplomacy between human empires; authoritative diplomacy popups remain the human path. Empire border/sensor known-to-player state now uses the screen-local authoritative player (Universe.Screen.Player) instead of serialized host UState.Player, so a joined client keeps its own owned-system borders/sensors after resync. Proof: builds passed; targeted local-view + first-contact suppression tests passed 2/2; focused Authoritative4X passed 116/116; full Arena filter passed twice, 99/99 and 99/99. Refreshed deploy bundle dist/arena-pc-dlls.zip (SHA-256 0EF3373706EA54F2B930120B6CF2ABAC55DAA1E7644035E4C066B5616AA07BE6). Host-side VIEW_PERF did not show a draw-time spike, so the laptop FPS tank still needs the laptop-side VIEW_PERF log/artifact from the next run.
- 2026-06-27 08:37 +03:00 - Sustained 4X MP chunk 99: fixed the remaining human-empire colony automation leak behind the laptop empire's unexpected queued work. Empire-level AI managers were already suppressed for authoritative-human empires, but planet governance is separate: remote human planets were still non-local `isPlayer` planets on the host, so generated starts assigned them AI colony types and `Planet.DoGoverning()` could enqueue construction/troops/terraformers independently of authoritative player commands. Planet ownership now distinguishes local UI ownership from human-control ownership: authoritative MP screens resolve `OwnerIsPlayer` through the screen-local assigned player, while governor/production automation branches use `OwnerIsHumanControlled`. Generated MP human empires now start with manual colonies, no governor/orbital/troop flags, and no inherited governor queue; future human colonization also defaults to manual when auto-colonize is off. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused generated-lobby/local-view regressions passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 116/116; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `320CB397078B505C4E81B158B0C23F9EA75725B614B0563F5EC4EEBDF04BE0F9`). Next live QA should confirm the joined empire keeps manual queues unless the player explicitly queues work or enables governor options.
- 2026-06-27 08:58 +03:00 - Sustained 4X MP chunk 100: fixed the laptop zoom-out FPS tank caused by a draw-exception storm in solar-system name rendering. Laptop `blackbox.log` showed hundreds of `Draw Screen failed: UniverseScreen` null refs in `SolarSystem.GetKnownOwners` whenever zoomed far enough to draw system names. `GetKnownOwners` now tolerates a temporarily missing viewer during MP attach/resync, skips null owner entries, and treats the screen-local assigned empire as well-known even when the serialized host `isPlayer` flag belongs to another empire. This prevents repeated draw exceptions and keeps joined-client system-name coloring aligned with the local authoritative player. Proof: `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; targeted joined-client local-view/system-owner regression passed 1/1; focused `FullyQualifiedName~Authoritative4X` passed 116/116; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `A6A1A14D0F8EB08C85CD30BB9CD0FB82964FA68B9254B2953D94306495AC7C1A`). Next live QA should redeploy both machines and confirm zooming to sector/galaxy view no longer creates `DRAW_EXCEPTION_COUNT` rows or laptop FPS collapse.
- 2026-06-27 09:16 +03:00 - Sustained 4X MP chunk 101: expanded the deployable authoritative 4X QA from a two-command heartbeat into a functional multiplayer script. `Authoritative4XProbe` now runs ten scripted turns over the real TCP host/join path: both players disable automation, set colony budgets, set empire tax/treasury, set governor flags, set manual trade slots, set defense targets, submit/share a custom ship design, queue that design at their own planet, and the join player declares war through the human diplomacy command path. The probe asserts the mutations on both host authority and join replica after later heartbeat ticks, and the QA summarizer/script now fails an `OK` run that lacks the functional assertion categories. Local loopback QA also waits for host content-load warmup so two local processes do not fight over `StarDrive.user.config`. Proof: local two-process TCP QA passed 30 turns with `FailureKind=None`, 20 host-applied commands, 22 join-applied commands, 32 functional asserts, and final `0x74F3296BCACE3851:0x6082BA35B61C51A5/0xFE04D7BA4EC93141`; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused `FullyQualifiedName~Authoritative4X` passed 116/116; full `FullyQualifiedName~Arena` passed twice, 99/99 and 99/99. Refreshed deploy bundle `dist/arena-pc-dlls.zip` (SHA-256 `922CB7763FA4E8074424B29B3D1F41AB250ADD9A60CA63ADE36D9BF73CC2A3E3`) and probe package `dist/authoritative4x-probe.zip` (SHA-256 `7AB7102F18AB8555C83464B0B54FCE6FDCE76AD80E2490694AABEA0EC3D07435`). Next live QA can use the same script with `-ClientSsh` for unattended PC/laptop command-loop validation.
- 2026-06-27 09:44 +03:00 - Star Gladiator extension chunk 1: added deterministic Battle Codes for shareable arena replay descriptors. `ArenaBattleCodes` exports the existing `ArenaMultiplayerSettings`/`SessionStartMessage` match setup, resolved fleet manifests, build fingerprint, and turn-sequence SHA-256 into a copy-pasteable `SGB1-...` code; import reconstructs the settings, rejects malformed/truncated payloads, and warns on CA/BlackBox/Arena build-hash mismatch instead of silently risking a desync. The headless proof exports a PvP brawl, imports it, reruns the deterministic lockstep sim, and asserts the imported replay reproduces the original turn-hash sequence; it also verifies build mismatch warning and clean malformed-code rejection. Proof: targeted `ArenaBattleCodesRoundTripReplayFingerprint_Headless` passed 1/1; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 100/100 and 100/100. Live QA still needed for the eventual 3D re-watch/import UI; the replay descriptor and deterministic fingerprint path are headless-proven.
- 2026-06-27 10:05 +03:00 - Star Gladiator extension chunk 2: added the captain entity and nemesis arc data model. `ArenaCareer` now persists `ArenaCaptain` value records, assigns/synthesizes captain ids for old captain-less owned vessels, carries captain level/kills/alive/ejection/death fields through save/load, and can write a distinct `Captain` memorial kind. Contenders now carry `NemesisOfVesselId` plus `Grudge`; named memorial killers promote matching contenders to pinned nemeses, nemeses are excluded from ecosystem retirement, `ClearNemesis` settles a grudge, and fight generation injects a guaranteed `Reckoning: ...` contender challenge when a nemesis exists. The golden-run canonical state now hashes captain identities, owned-vessel captain ids, memorial kind/captain ids, and contender nemesis/grudge fields. Proof: targeted `ArenaCaptainNemesisArc_Headless` passed 1/1; targeted `ArenaGoldenRunStateHash_Headless` passed 1/1; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 101/101 and 101/101. Live QA later should add UI treatment for pinned nemesis dossiers and captain memorial presentation; the data/save/roulette behavior is headless-proven.
- 2026-06-27 10:36 +03:00 - Star Gladiator extension chunk 3: added arena anti-kite tuning plus the opt-in Gravity Well fight modifier. Arena-spawned ships now receive deterministic `ArenaEngagementBias=0.28` with a standoff-decay timer, and `Artillery` scales desired combat range toward a safer brawl range without using wall-clock or new RNG; career ladder `FairDuel` keeps its original unbiased resolver so persistent rival ratings stay golden-run stable while live arena fights and explicit sim probes exercise the new tuning. `ArenaFightModifier.GravityWellHazard` sets `GravityWellRange=12000`, creates/reuses a neutral center gravity body, resets the hazard on normal fights, and appears as a high-fame wildcard option. Balance readout from `sim-output/arena-anti-kite-balance.json`: `Scythe mk4-a` vs `Scythe Exceed mk3-b`, 4 seeds, 2400 ticks, kiter win-rate before=1.000 after=1.000, average kiter damage before=31113.79 after=27865.94. Mechanism is in and deterministic, but the first default is not yet a decisive anti-kite balance solve; morning tuning can raise bias/decay if the design goal is stronger pressure. Proof: targeted anti-kite/gravity + balance probe passed 2/2; deterministic fingerprint + in-process lockstep gate passed 2/2 after rerunning serially to avoid a config-file lock from parallel test startup; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Live QA still needed for the Gravity Well visual/readability and actual fight feel.
- 2026-06-27 18:52 +03:00 - Sustained 4X MP chunk 102: fixed the latest laptop resync-to-single-player failure by making authoritative empire runtime state actually authoritative. The laptop log's firstDiff decoded to empire money drift at tick 600 (`E|1` money 1012.042 vs 1008.028) followed by a failed resync where empire 2's tax rate reloaded as `0` instead of `0.14`; both fields are in the canonical `E|` row. Passive clients now reconcile the host snapshot's empire runtime row (money, tax rate, treasury goal, automation flags, and automation design picks) before computing their local canonical digest, so small economic float drift is corrected instead of forcing a resync. Authoritative save/resync sidecars now also carry exact empire runtime bits and reapply them after load on both host and clients, closing the post-resync tax-rate loss. The helper deliberately sets the `RushAllConstruction` flag without re-running `SwitchRushAllConstruction`, because queue rush state is a separate authoritative queue payload and re-running the command during reconciliation caused client-only queue mutation in tests. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused reconciliation/save/resync tests passed 5/5; focused `FullyQualifiedName~Authoritative4X` passed 117/117; local TCP command-injection QA ran 700 turns with `FailureKind=None`, 32 functional asserts, 20 host-applied commands, 34 join-applied commands, no sync mismatch/raw drift, final `0x6DE67009A4A16BCB:0x087C42FD37725DB5/0x5A52A72232C6C523`; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Refreshed local deploy bundle/probe outputs under `dist/`; live PC/laptop QA should redeploy both sides and verify the laptop no longer fogs into host/single-player view after the tick-600 area.
- 2026-06-27 20:04 +03:00 - Sustained 4X MP chunk 103: made cross-machine authoritative 4X QA repeatable over SSH by adding an interactive scheduled-task laptop client path to `RunAuthoritative4XCrossMachineQA.ps1`. Direct SSH execution cannot create the XNA graphics device, so the script can now deploy the DLL/probe bundle over SSH/SCP, launch the host locally, run the laptop join probe in the logged-in desktop session through Task Scheduler, collect artifacts, and summarize the same functional command assertions. Also fixed the remote deploy boolean so generated remote PowerShell emits `$true/$false` instead of invalid `if (true)` syntax. Proof: manual PC host + laptop scheduled join ran 700 turns with both sides final `0x6DE67009A4A16BCB:0x087C42FD37725DB5/0x5A52A72232C6C523`, no sync mismatch, 32 functional asserts, and host/join budget/automation/governor/trade/defense/shipyard/diplomacy effects confirmed; scripted `-UseInteractiveClientTask` run then passed 200 turns with `FailureKind=None`, 32 functional asserts, 20 host-applied commands, 24 join-applied commands, final `0x599A8BE25695CD0F:0x3B64E3A22B4AAFB5/0xFE04D7BA4EC93141`; PowerShell parser check passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Next: use this script while live testing to separate harness/deploy/network failures from real authoritative sync or UI-control bugs.
- 2026-06-27 21:04 +03:00 - Sustained 4X MP chunk 104: expanded the authoritative 4X QA probe into a longer "full-game interaction" script aimed at the live join-control failure. The probe now runs 16 required command turns per side (automation off, budgets/tax, governor flags, trade slots, defense targets, building if available, custom ship design/share, ship queue, human diplomacy, colony type, labor split, research queue, goods state, planet rename, and ground-defense budget) plus repeated late join-side control pulses through the full horizon (budget, tax/treasury, labor, rename). Final assertions now distinguish durable multiplayer invariants from normal long-run sim autonomy: ownership/human-control, budgets, automation, governor/trade/defense, shipyard, diplomacy, colony identity, research acceptance, command-stream acceptance, and late join control must hold, while volatile labor/goods changes are proven at command time instead of requiring governors to keep them frozen forever. `RunAuthoritative4XCrossMachineQA.ps1` now clears the laptop per-run artifact folder before launch so failed SSH/non-interactive attempts cannot report stale logs. Proof: local two-process TCP QA passed 700 turns with `FailureKind=None`, 52 functional asserts, 32 host-applied and 88 join-applied command observations, no sync mismatch/raw drift, final `0x07CF2A9B1728ACC4:0x01826BE8A13841C9/0x34E3A029B4FA9D7A`; SSH to laptop (`MSI`) worked; scripted PC-host/laptop-join `-UseInteractiveClientTask` QA passed the same 700-turn script with `FailureKind=None`, same final digest, same functional coverage, and no sync mismatch/raw drift. Direct non-interactive SSH still fails XNA graphics-device creation, so the scheduled desktop task path remains required for laptop probe runs. Verification: PowerShell parser check passed; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused `FullyQualifiedName~Authoritative4X` passed 117/117; full `FullyQualifiedName~Arena` passed twice after one unrelated native projectile-update access violation retry, 103/103 and 103/103. Next: use the refreshed QA wrapper during live sessions; the remaining risk is UI-specific command capture/visual state, not the underlying host-validated command stream.
- 2026-06-27 21:36 +03:00 - Sustained 4X MP chunk 105: closed the remaining human-empire automation leak behind joiner AI scout/military behavior. Authoritative empire automation now carries AutoMilitary through the command bitfield, host applicator, passive UI capture/restore, canonical snapshots, and save/resync sidecars; the Automation window exposes an explicit Auto Military toggle next to the other delegation controls. Remote human empires are now treated as human-controlled in the direct Empire exploration/scout planner as well as EmpireAI, so stock AI cannot queue BuildScout/ScoutSystem for a joined human when AutoExplore is off; turning AutoExplore on remains an intentional delegation path. Proof: StarDrive.csproj and SDUnitTests.csproj built cleanly; focused automation tests passed 3/3 including the new remote-human scout suppression regression; focused FullyQualifiedName~Authoritative4X passed 118/118; full FullyQualifiedName~Arena passed twice, 103/103 and 103/103 after one non-counted test-host crash retry. Next live QA should verify the joiner can toggle AutoExplore/AutoMilitary deliberately and that no scout/military queues appear while those flags are off.
- 2026-06-27 22:11 +03:00 - Sustained 4X MP chunk 106: hardened the remaining direct `isPlayer` automation gates for authoritative remote-human empires. `Empire.IsHumanControlled` is now the shared authority-aware identity check and `IsAIControlled` respects it; trade/manual-tax semantics, ship picker selection, scout selection, colonization goals/rush behavior, research/mining station planners, and space-road creation now treat a joined human as human-controlled unless that player explicitly enables the relevant automation flag. The economic planner's direct budget-command path now uses human-style math for remote humans, so a joiner's manual tax/budget settings are not rewritten by AI-only risk logic. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused remote-human automation tests passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 119/119; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103; `git diff --check` clean apart from line-ending normalization warnings. Next live QA should verify the joiner can keep manual taxes/trade/scout/colonization/space-road behavior while explicit automation toggles still delegate those slices when desired.
- 2026-06-28 00:12 +03:00 - Sustained 4X MP chunk 107: broadened the authoritative 4X fail-closed guard and QA harness after live research-selection desyncs. Passive/stale authoritative clients now block direct local mutation paths across research, colony, governor/trade/defense, automation, fleet/patrol/formation, ship design/load/refit, ship orders, combat/troop/planet utility panels, and selected universe input paths unless an active host-validated command context is present; single-player retains direct behavior. Added stale-context regression coverage so a disposed live MP session cannot silently mutate the local replica, and expanded Authoritative4XProbe to force a safe building queue path when generated starts expose none, then assert queued-or-completed building effects instead of falsely failing after cheap buildings finish. Proof: probe build passed; local TCP host/join QA ran 700 turns with FailureKind=None, SyncMismatch=0, RawHashDrift=0, 52 functional asserts, BuildingQueueAsserts=4, ResearchAsserts=4, ShipyardAsserts=4, DiplomacyAsserts=4, HostApplied=32, JoinApplied=88, final 0x803134BE06CDCEEF:0xEBD10AFCC5E41B9E/0x168C201777B3AF00; targeted stale-context/research tests passed 3/3; focused FullyQualifiedName~Authoritative4X passed 121/121; full FullyQualifiedName~Arena passed twice, 103/103 and 103/103. Next live QA should retry joiner research, construction, ship-design/load, fleet, and automation UI actions; remaining failures should now be either missing command vocabulary or visible UI wiring gaps rather than silent local-replica drift.
- 2026-06-28 00:56 +03:00 - Sustained 4X MP chunk 108: added authoritative human-to-human technology trade and broadened live-style command QA for colonization/trade interactions. Human diplomacy now has `TechnologyTrade`: the host validates that the proposer owns a non-multilevel tech, the target can use it and lacks it, routes the offer as a targeted human popup, and applies `UnlockTech(..., Diplomacy, proposer)` only after target acceptance. The canonical authoritative payload now emits unlocked-tech `U|` rows so future sync mismatches name the exact tech UID. The proposal UI exposes a TECH TRADE action with deterministic first-offerable-tech selection. `Authoritative4XProbe` now forces deterministic QA colonization opportunities when the generated tiny map lacks them, injects ship-bound colonize commands for host and join, accepts a host-to-join tech-trade popup, and the QA summarizer reports `ColonizationAsserts` and `TechTradeAsserts`. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused `FullyQualifiedName~Authoritative4X` passed 121/121; local TCP host/join QA ran 700 turns with `FailureKind=None`, `SyncMismatch=0`, `RawHashDrift=0`, `ColonizationAsserts=4`, `TechTradeAsserts=4`, `HostApplied=36`, `JoinApplied=92`, final `0xCE37F312FC62246E:0xC75EB321ACF25273/0x3F3EE2F37D8485F4`; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Headless coverage now explicitly exercises colonization, governor assignment, construction queues, manual colony trade slots, research queueing, human diplomacy, and tech trade over the TCP authoritative path; remaining live QA is UI feel and longer cross-machine play.
- 2026-06-28 00:58 +03:00 - Refreshed local deployment artifacts for chunk 108: `dist/arena-pc-dlls.zip` SHA-256 `6D8C9A3AE3BE76062136F82BA2D1D9DC2A2F3235EA215EE9DBC8B693E62A4EC3`; `dist/authoritative4x-probe.zip` SHA-256 `5D05AFC939B7CAF94F739C9EC7F863651D694D12645A4B24A8B32C861FC88A3E`. These artifacts remain local/untracked for laptop deployment parity.
- 2026-06-28 01:31 +03:00 - Sustained 4X MP chunk 109: diagnosed the latest live desync loop from `game/sim-output/authoritative-4x-last-host.log`. The first repeated mismatch after each resync was canonical line 7: host had `U|1|Colonization|0` while the client jumped to `R|1|2|...`, meaning unlocked-tech rows introduced for tech-trade/research coverage were not restored/applied on the passive client after a host-authored resync save. Authoritative snapshots now apply `U|` unlocked-tech rows before digest comparison, and authoritative session save sidecars now capture/apply per-empire unlocked tech state so host/client loads converge before the first post-resync heartbeat. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed after a transient Defender file lock retry; focused tech-resync regressions passed 2/2; focused `FullyQualifiedName~Authoritative4X` passed 122/122; full `FullyQualifiedName~Arena` completed green twice, 103/103 and 103/103, with one non-counted test-host crash retry between them. Refreshed local `dist/arena-pc-dlls.zip` for laptop deployment parity.
- 2026-06-28 02:16 +03:00 - Sustained 4X MP visibility chunk 110: added opt-in authoritative wire-frame tracing to the TCP transport and wired it into both live 4X telemetry and the portable authoritative probe. Live/probe logs now include `WIRE` rows for authoritative command requests, command results, and state snapshots at `send-remote`, `send-remote-pending`, `write-remote`, `read-remote`, and `deliver`, with sequence, peer, empire, command kind, snapshot hash/digest, payload length, transport counters, pending queue depth, inbound queue depth, connection state, and write errors. This is aimed at the latest cross-machine timeout where the host accepted join seq 1 but the laptop never observed the result; the next run should show whether the result was not written, not read, or not delivered. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore /p:UseSharedCompilation=false /nr:false` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore /p:UseSharedCompilation=false /nr:false` passed; published `dist/authoritative4x-probe`; local TCP host/join probe ran 60 turns with matching final `0xA3C30CC830E0AECC:0x67A156CB72B8513C/0xBC8E4A60B687D780` and 1,176 `wire` rows including seq-1 read/deliver/write evidence; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Refreshed `dist/arena-pc-dlls.zip` and `dist/authoritative4x-probe.zip` for laptop deployment parity.
- 2026-06-28 03:38 +03:00 - Sustained 4X MP QA chunk 111: ran the SSH laptop authoritative probe and fixed the real state mismatch it exposed. Wire telemetry proved seq-1 result/snapshot frames were delivered to the client; the client then mismatched because the authority canonical payload included a `D|` player-design row (`Combatl Freighter`) that the passive replica had never imported. Authoritative snapshots now serialize player-design rows with the full base64 design payload and passive replicas import/register those designs before hashing, keeping custom/generated MP designs authoritative instead of machine-local. `Authoritative4XNetworkClient` now pairs queued command results with snapshots FIFO so TCP burst delivery cannot overwrite `LastResult`, and the probe now throws `LastSyncMismatch` immediately instead of masking it as a later timeout. Also fixed the portable arena lockstep loopback harness: host/join now write separate artifact files, and the standalone probe project includes `SDLockstep/AuthoritativeMessages.cs` so it publishes after the authoritative message expansion. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore /p:UseSharedCompilation=false /nr:false` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore /p:UseSharedCompilation=false /nr:false` passed; targeted loopback lockstep test passed 1/1; local two-process authoritative TCP QA passed 700 turns with final `0xCE37F312FC62246E:0xC75EB321ACF25273/0x724496D568FB34E7`; PC-host/laptop-join SSH scheduled-task QA passed 60 turns with final `0xA3C30CC830E0AECC:0x67A156CB72B8513C/0xF4AD2A2ED942B0BB`, then 700 turns with final `0xCE37F312FC62246E:0xC75EB321ACF25273/0x724496D568FB34E7`; final refreshed-zips SSH smoke passed 60 turns with the same short-run final digest; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103, after fixing the loopback artifact race. Refreshed local deploy artifacts: `dist/arena-pc-dlls.zip` SHA-256 `C63D56A90AE815EC34EAD3D7446E170C74D310E3938935A4934E44CBC29DBA5A`; `dist/authoritative4x-probe.zip` SHA-256 `E27B79AEACB84FDB39C3232582C95F275A836BC8E5BA741BBA8408A66D4E5BEF`; `dist/arena-lockstep-probe.zip` SHA-256 `8874085457B8F1059EF282F16C3ABD6D8B757A82C516C4DB47BCFE3198BBB510`.
- 2026-06-28 04:10 +03:00 - Sustained 4X MP live-log fix chunk 112: diagnosed the latest host crawl/resync from `game/sim-output`. The live host log showed a resync at tick 3611 with firstDiff `Q|907|...|Cordrazine Colonizer` where only `ProductionSpent` differed (`11.55` authority vs `12.99375` client), and the pre-resync host log also contained 21,864 `WIRE` rows in roughly 108 seconds with 72k-character snapshots, making live wire tracing a credible host-FPS killer. Live `TcpLockstepTransport.AuthoritativeFrameTrace` is now opt-in via `SD_AUTH4X_WIRE_TRACE=1` or test override, while normal command/result/snapshot/mismatch/resync telemetry remains enabled. Passive replicas now apply authority queue runtime scalars (`Cost`, `ProductionSpent`, `Rush`, `IsCancelled`) from matching `Q|` rows before canonical digest comparison, so local economic/production drift is corrected without hiding queue-order or identity mismatches. The portable authoritative probe now isolates `APPDATA`/`LOCALAPPDATA` per host/join artifact folder so local two-process QA no longer fights over `StarDrive.user.config`. Proof: targeted telemetry/queue tests passed 3/3; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103; focused in-process authoritative coverage passed 7/7 across live generated start, lobby self-test/TCP flow, 8-player routing, research, construction queue, and diplomacy; local two-process authoritative probe passed 700 turns with `FailureKind=None`, `ok=6`, final `0xCE37F312FC62246E:0xC75EB321ACF25273/0x724496D568FB34E7`. Refreshed local deploy artifacts: `dist/arena-pc-dlls.zip` SHA-256 `D8A703E5C4E1FC2FAA28481A5D2FA628B5FA23E5F4E8CCD5E5FEDCE985A9AFC7`; `dist/authoritative4x-probe.zip` SHA-256 `1D1E4323AA3930B9048DDA02CBB6D6EA00CB16F618A9C7E4B1016859B82A4036`.
- 2026-06-28 05:07 +03:00 - Sustained 4X MP PvP/diplomacy chunk 113: checked the latest host/laptop live logs after the neutral human-ship attack and joiner diplomacy attempt. The desync rows in the host log were the pre-fix `Q|` construction-runtime drift already addressed by chunk 112, and the latest client log had no join-origin `COMMAND` rows for diplomacy, so the joiner proposal never reached the host in that run. Neutral human-vs-human ship attack now auto-declares war through the authoritative diplomacy manager, queues the normal human popup for the target, bypasses the stock AI `DiplomacyScreen`, and then applies the attack/board order. The passive-client input gate now allows hostile ship orders against neutral human-controlled empires so the command reaches the host instead of being blocked by the local `IsEmpireAttackable` check. Added a joiner proposal-screen regression proving the peer-3 diplomacy button submits an authoritative `DiplomacyProposal` command. Proof: focused PvP/diplomacy regressions passed 5/5; focused `FullyQualifiedName~Authoritative4X|FullyQualifiedName~AuthoritativeHumanDiplomacy|FullyQualifiedName~AuthoritativeDiplomacy` passed 125/125; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Next live QA must redeploy the refreshed DLL bundle to both machines; if joiner diplomacy still appears inert, keep wire trace off first and use the normal `COMMAND source=network peer=3 kind=DiplomacyProposal` log row as the expected signal.
- 2026-06-28 06:04 +03:00 - Sustained 4X MP replay-QA chunk 114: upgraded the authoritative multiplayer QA to catch the latest live crash class directly. Passive replicas now apply authoritative `R|` relationship rows before replaying an accepted command, so a human-vs-human attack that auto-declared war on the host is not rejected locally as "cannot attack ship" on the joiner. Client replicas also initialize and replay using the host-owned pause/speed state, closing the StartPaused/alt-tab drift where a passive client could advance while the authority stayed paused and then grow extra local-only ship rows. Added TCP loopback regressions for host-attacks-joiner and joiner-attacks-host neutral human ships, a live joiner diplomacy button submission over TCP, and a stale local pause-drift replay guard; repaired the older live-host diplomacy proof to use authoritative session-control pause instead of directly mutating local `UState.Paused`. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; focused PvP/diplomacy/pause regressions passed 7/7; broad authoritative multiplayer filter `FullyQualifiedName~Authoritative4X|FullyQualifiedName~AuthoritativeHumanDiplomacy|FullyQualifiedName~AuthoritativeDiplomacy` passed 129/129; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Next live QA should redeploy both endpoints and retry joiner diplomacy plus neutral ship attacks; expected logs are host/join `DiplomacyProposal` or `AttackShip` command rows followed by matching `R|` war rows before the accepted attack snapshot.
- 2026-06-28 06:51 +03:00 - Sustained 4X MP long-run QA chunk 115: kept the overnight probe loop running against the latest authoritative build and fixed the long-horizon issues it exposed. A 1200-turn local TCP host/join run first crashed the headless authority in `SBProduction.OnBuildingComplete` when a player-added building completed without a live `NotificationManager`; building-complete notifications are now null-safe, with a focused regression proving the completed building leaves the queue and is placed. Extended the standalone `Authoritative4XProbe` proof so long runs accept colonization/build commands that have already completed by final assertion time (`MarkForColonization` goal or owned target planet; queued or owned completed ship), and made verbose transport wire tracing explicit opt-in via `SD_AUTH4X_WIRE_TRACE=1` instead of always-on probe spam. Proof: focused `TestRefitQueuePlacement` passed 5/5; `dotnet build StarDrive.csproj -c Debug -p:Platform=x64 --no-restore` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64 --no-restore` passed; broad authoritative multiplayer filter `FullyQualifiedName~Authoritative4X|FullyQualifiedName~AuthoritativeHumanDiplomacy|FullyQualifiedName~AuthoritativeDiplomacy` passed 129/129; local two-process TCP probe passed 1800 turns with isolated per-role `APPDATA`/`LOCALAPPDATA`, seed `67891`, races `Cordrazine|Human`, game speed `2`, final `0x4826D26B8691F6CE:0x8E2E9285A989DDD9/0x27EB0546AD4E950F`, and authority+replica asserts for automation, budgets, governor/trade/defense, colony/research, colonization, tech trade, shipyard, diplomacy, and late join-control pulses; full `FullyQualifiedName~Arena` passed twice, 103/103 and 103/103. Refreshed local deployment artifacts: `dist/arena-pc-dlls.zip` SHA-256 `BB534996D0DE25E220426EE321790913CBEEB3BF70388875CECF6C6170655A84`; `dist/authoritative4x-probe.zip` SHA-256 `4C10121D66E4BA42239122573022F7442E6E0B20BAFB6F4F12CE003B61D36373`. Next live QA should use the refreshed DLL bundle on both machines; keep wire trace off unless a command/result delivery failure recurs.
- 2026-06-28 07:09 +03:00 - Sustained 4X MP overnight QA chunk 116: widened the post-fix local TCP trial matrix without additional production edits. A 2400-turn Human/Cordrazine isolated-config run at seed `24680`, game speed `0.5`, passed with matching host/join final `0x8E86F7961262C8BB:0xDA6F8DF38E65842A/0x8D0560C4DC20D2E9` and repeated late join-control pulses through budget, labor, rename, governor/trade/defense, research, colonization, tech trade, shipyard, and diplomacy assertions. Three shorter config-variance runs also passed: Human/Cordrazine seed `13579` speed `1` final `0x8C2E9C994D535829:0x77E5632F62AF5CC4/0x7DFA5117D8F7F6B6`; Kulrathi/Draylok with `Smart`/`Fertile` traits seed `97531` speed `0.75` final `0x151CE22AEE78A00D:0xCDC5C6CE89342C9D/0x973E4776D2B760A5`; Pollops/Vulfen seed `86420` speed `1.5` final `0x14A1CB71B1B4AFF7:0xDFF2F50328C4E32A/0x1A31041255AD4D30`. A deeper 3000-turn Ralyeh/Opteris run with `Smart`/`Fertile`, seed `314159`, speed `2`, also passed with matching final `0x00EF8689283BF413:0xC064015E93B11DA8/0x6990817B58B97608`. Focused multiplayer slices passed 9/9 for 8-player/resync/attack/diplomacy/save/research and 24/24 for colonization, governor, trade, construction queue, fleet, shipyard, and diplomacy interactions. No new desync, raw-hash drift, or command-ownership failure reproduced locally; the remaining risk is live UI capture/state presentation on real machines, not the host-validated command stream covered by the probe.
- 2026-06-28 07:13 +03:00 - Sustained 4X MP cross-machine QA chunk 117: ran the established SSH/scheduled-task PC-host/laptop-join authoritative probe against the current build after the local matrix passed. SSH deployment to `MSI` succeeded, the laptop client ran from `D:\dev\stardrive` via the interactive scheduled-task path, and the 700-turn cross-machine session completed with `FailureKind=None`, `SyncMismatch=0`, `RawHashDrift=0`, `NetworkError=0`, `HostApplied=36`, `JoinApplied=92`, and final `0xCE37F312FC62246E:0xC75EB321ACF25273/0x724496D568FB34E7`. The summary recorded 60 functional assertions across budget, automation, governor, trade, defense, building queue, shipyard, diplomacy, control, colony, colonization, research, tech trade, command stream, and late-control coverage. Artifacts: `sim-output/auth4x-cross-machine-qa/20260628-071200/qa-summary.txt`. Local deploy zips were regenerated for parity during the run: `dist/arena-pc-dlls.zip` SHA-256 `C7F0E43E8A810B730487C645E0AE07F9296F09CDC5C025539478A2A34DE6B4CD`; `dist/authoritative4x-probe.zip` SHA-256 `E959FF860AC4B7A2E0099682D5FA264158433750FD06164348E6AB81536A2E1E`.
- 2026-06-28 07:16 +03:00 - Sustained 4X MP cross-machine QA chunk 118: ran a longer no-redeploy PC-host/laptop-join authoritative probe through the same SSH/scheduled-task path. The 1200-turn session completed with `FailureKind=None`, `SyncMismatch=0`, `RawHashDrift=0`, `NetworkError=0`, `HostApplied=36`, `JoinApplied=132`, 60 functional assertions, and final `0xAD93AFF490924BDF:0x2F114010501BE4FD/0x376E95014BA4C015`. Artifacts: `sim-output/auth4x-cross-machine-qa/20260628-071442/qa-summary.txt`. This gives two consecutive same-build laptop runs green after the local 2400/3000-turn stress runs.
- 2026-06-28 20:18 +03:00 - Sustained 4X MP lobby usability chunk: made the multiplayer lobby friendlier for live PC/laptop trials. `UITextEntry` now handles Ctrl+A/C/V through a testable clipboard seam, so the host/IP field can be pasted instead of retyped. The lobby now persists host, port, seed, speed, race/traits, map settings, turn timer, richness, pace, and start-paused to a small arena-owned config file with test override isolation; dotted IPs are base64-encoded in the YAML so they round-trip exactly. Removed the visible `TURNS` field from the live lobby and send `MaxTurns=0` for authoritative 4X starts, keeping finite turns only for probes/self-tests. Broadened authoritative `VIEW_PERF` telemetry to all camera views and added sim timing/catch-up fields (`simMs`, `processMs`, `currentSim`, `targetSim`, sim FPS) because the latest host log had no sync loop, no draw exceptions, no wire flood, and no view-perf rows to explain the reported FPS crawl. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64` passed; `dotnet build StarDriveArena.csproj -c Debug -p:Platform=x64` passed; `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused lobby/clipboard tests passed 3/3; full `FullyQualifiedName~Arena` passed twice, 105/105 and 105/105. Refreshed local deploy artifacts: `dist/arena-pc-dlls.zip` and `dist/stardrive-arena-share.zip`.
- 2026-06-28 21:08 +03:00 - Sustained 4X MP 8-player lobby chunk: widened the authoritative 4X live lobby/start path from the hard-coded 2-player host/join payload to an explicit roster payload supporting one host plus up to seven remote human players (peer slots P3-P9). `SessionStartMessage` now carries a backward-compatible `AuthoritativePlayerRoster` field; `Authoritative4XLobbyNetworkFlow` serializes/deserializes the full roster, validates the local peer, reconstructs identical generated games for every player, and still fills the legacy host/join fields for existing two-player paths. The live multiplayer lobby now exposes a persistent SLOT selector for joiners, tracks multiple remote lobby peers, requires all connected peers ready, validates every peer environment, broadcasts start/error messages to all remotes, and attaches each client using its selected peer id. Proof: `dotnet build StarDrive.csproj -c Debug -p:Platform=x64`, `dotnet build StarDriveArena.csproj -c Debug -p:Platform=x64`, and `dotnet build UnitTests/SDUnitTests.csproj -c Debug -p:Platform=x64` passed; focused roster/codec/8-player/lobby tests passed 8/8; full `FullyQualifiedName~Arena` passed twice, 105/105 and 105/105; refreshed and SSH-deployed the DLL/probe bundle to `MSI`, then PC-host/laptop-join interactive QA passed 600 turns with `FailureKind=None`, `SyncMismatch=0`, `RawHashDrift=0`, `NetworkError=0`, 60 functional assertions, and final `0x166DCBC88CF7B4F1:0x026CE5E565F080B1/0xEB6DDD8013F8C964`. Artifact: `sim-output/auth4x-cross-machine-qa/20260628-210739/qa-summary.txt`. Human live QA still needed for actual 3-8 simultaneous devices; headless coverage proves the roster/start/session layers and existing 8-player command/resync mechanics.
